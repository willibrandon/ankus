---
title: 'PgPath'
description: 'API reference for Ankus.PgPath.'
slug: api/ankus.pgpath
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns an immutable ordered sequence of points and an open/closed flag for PostgreSQL path.
Empty paths can be constructed and exchanged even though PostgreSQL's text parser requires points.

```csharp
public sealed class PgPath : IReadOnlyList<PgPoint>, IReadOnlyCollection<PgPoint>, IEnumerable<PgPoint>, IEnumerable
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IReadOnlyList&lt;PgPoint&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1), [IReadOnlyCollection&lt;PgPoint&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlycollection-1), [IEnumerable&lt;PgPoint&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerable-1), [IEnumerable](https://learn.microsoft.com/dotnet/api/system.collections.ienumerable)

## Constructors

<a id="member-6a03aa3ef48cf9d5"></a>

### PgPath(IEnumerable&lt;PgPoint&gt;, bool)

Copies the supplied points and records whether the last point connects back to the first.

```csharp
public PgPath(IEnumerable<PgPoint> points, bool isClosed = false)
```

Parameters:

`points` — [IEnumerable&lt;PgPoint&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerable-1)

The ordered vertices.

`isClosed` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the path is closed.


## Properties

<a id="member-38bef13ef453361b"></a>

### Count

Gets the number of vertices.

```csharp
public int Count { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-c9e817119365cc0a"></a>

### IsClosed

Gets whether the path is closed.

```csharp
public bool IsClosed { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-40a09fe5adaa89c7"></a>

### this[int]

Gets a vertex by its zero-based index.

```csharp
public PgPoint this[int index] { get; }
```

Parameters:

`index` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The vertex index.

Value: [PgPoint](/api/ankus.pgpoint/)

The point.

<a id="member-cf4ccd56b5fb5452"></a>

### Points

Gets the vertices without exposing mutable storage.

```csharp
public ReadOnlySpan<PgPoint> Points { get; }
```

Value: [ReadOnlySpan&lt;PgPoint&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)


## Methods

<a id="member-556e66012721fc3e"></a>

### GetEnumerator()

Enumerates vertices in order.

```csharp
public IEnumerator<PgPoint> GetEnumerator()
```

Returns: [IEnumerator&lt;PgPoint&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerator-1)

The vertex enumerator.

<a id="member-f2b32c1f1be5f155"></a>

### Parse(string)

Parses a path using PostgreSQL on the active backend.

```csharp
public static PgPath Parse(string text)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The path text.

Returns: [PgPath](/api/ankus.pgpath/)

The owned path.

<a id="member-b9bc6a510db196e5"></a>

### ToString()

Formats vertices with the path's opening and closing delimiters without backend access.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The path text.

<a id="member-74b28f7a241398ec"></a>

### TryParse(string?, out PgPath?)

Tries PostgreSQL parsing without swallowing backend-access or operational errors.

```csharp
public static bool TryParse(string? text, out PgPath? value)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The path text.

`value` — [PgPath](/api/ankus.pgpath/)

The result, or null.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether parsing succeeded.

<a id="member-40cb338150419f17"></a>

### WithClosed(bool)

Copies this path with a new open/closed flag.

```csharp
public PgPath WithClosed(bool isClosed)
```

Parameters:

`isClosed` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

The new flag.

Returns: [PgPath](/api/ankus.pgpath/)

The independent path.
