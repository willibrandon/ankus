---
title: 'PgBuiltInOids'
description: 'API reference for Ankus.PgBuiltInOids.'
slug: api/ankus.pgbuiltinoids
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Converts and enumerates pgrx built-in OID constants using an explicit or active PostgreSQL major version.

```csharp
public static class PgBuiltInOids
```

Explicit-version overloads work outside PostgreSQL. Other overloads require the calling backend thread
and read the selected native headers' major version. Versions 13 through 19 are supported.
Membership is a constant classification, not proof of catalog existence, permissions, or object type.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Methods

<a id="member-c196d8c1873dd35c"></a>

### GetNativeName(PgBuiltInOid)

Gets the exact native symbol for a recognized value in the active backend version.

```csharp
public static string? GetNativeName(PgBuiltInOid value)
```

Parameters:

`value` — [PgBuiltInOid](/api/ankus.pgbuiltinoid/)

The numeric built-in candidate.

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The native name, or null when this version does not recognize the value.

<a id="member-fee2fd98be2d16f1"></a>

### GetNativeName(PgBuiltInOid, int)

Gets the exact selected-version native symbol, including historical renames.

```csharp
public static string? GetNativeName(PgBuiltInOid value, int postgresMajor)
```

Parameters:

`value` — [PgBuiltInOid](/api/ankus.pgbuiltinoid/)

The numeric built-in candidate, including an explicitly cast unknown value.

`postgresMajor` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The PostgreSQL major version, from 13 through 19.

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The native name, or null when this version does not recognize the value.

<a id="member-22dbec00238ce7c6"></a>

### GetValues()

Gets an immutable snapshot of the active version's recognized values in numeric order.

```csharp
public static IReadOnlyList<PgBuiltInOid> GetValues()
```

Returns: [IReadOnlyList&lt;PgBuiltInOid&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1)

The selected catalog members.

<a id="member-ac352d916ff5a85b"></a>

### GetValues(int)

Gets an immutable snapshot of one version's recognized values in numeric order.

```csharp
public static IReadOnlyList<PgBuiltInOid> GetValues(int postgresMajor)
```

Parameters:

`postgresMajor` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The PostgreSQL major version, from 13 through 19.

Returns: [IReadOnlyList&lt;PgBuiltInOid&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1)

Every recognized member exactly once.

<a id="member-4deebeb0e38698c2"></a>

### TryFromValue(ulong, out PgBuiltInOid, out PgOidLookupError)

Converts an unsigned datum-sized value without truncation using the active backend version.

```csharp
public static bool TryFromValue(ulong value, out PgBuiltInOid builtIn, out PgOidLookupError error)
```

Parameters:

`value` — [ulong](https://learn.microsoft.com/dotnet/api/system.uint64)

The unsigned input.

`builtIn` — [PgBuiltInOid](/api/ankus.pgbuiltinoid/)

The recognized numeric member, or zero on failure.

`error` — [PgOidLookupError](/api/ankus.pgoidlookuperror/)

The specific conversion outcome.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the selected version recognizes the value.

<a id="member-3764c57cf2e0e1b1"></a>

### TryFromValue(ulong, int, out PgBuiltInOid, out PgOidLookupError)

Converts an unsigned value using a supported major version without requiring a backend.

```csharp
public static bool TryFromValue(ulong value, int postgresMajor, out PgBuiltInOid builtIn, out PgOidLookupError error)
```

Parameters:

`value` — [ulong](https://learn.microsoft.com/dotnet/api/system.uint64)

The unsigned input; values above uint.MaxValue are rejected without truncation.

`postgresMajor` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The PostgreSQL major version, from 13 through 19.

`builtIn` — [PgBuiltInOid](/api/ankus.pgbuiltinoid/)

The recognized numeric member, or zero on failure.

`error` — [PgOidLookupError](/api/ankus.pgoidlookuperror/)

None, Invalid, Ambiguous, or TooBig.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether conversion succeeded.
