---
title: 'PostgresInstallation'
description: 'API reference for Ankus.PgConfig.PostgresInstallation.'
slug: api/ankus.pgconfig.postgresinstallation
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus.PgConfig](/api/ankus.pgconfig/)

Assembly: `Ankus.PgConfig.dll`

Describes one PostgreSQL installation by querying its authoritative
<code>pg_config</code> executable.

```csharp
public sealed class PostgresInstallation
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Properties

<a id="member-72e7376346d5cbc0"></a>

### BinDirectory

Gets the installation directory containing PostgreSQL executables.

```csharp
public string BinDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-bcde651eb960ebb4"></a>

### CreateDbPath

Gets the platform-correct path to PostgreSQL's <code>createdb</code> executable.

```csharp
public string CreateDbPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-ecc3ac8632f7926f"></a>

### DropDbPath

Gets the platform-correct path to PostgreSQL's <code>dropdb</code> executable.

```csharp
public string DropDbPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-e0747ac4c4ad39a1"></a>

### IncludeDirectory

Gets the public header directory, including dependencies of the server headers.

```csharp
public string IncludeDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-d5e8427baa86a672"></a>

### InitDbPath

Gets the platform-correct path to PostgreSQL's <code>initdb</code> executable.

```csharp
public string InitDbPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-34ac773099ec2768"></a>

### Label

Gets the Ankus version selector for this installation, such as <code>pg18</code>.

```csharp
public string Label { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-1a2c88b1371058f2"></a>

### LibraryDirectory

Gets PostgreSQL's native extension library directory.

```csharp
public string LibraryDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-0de2f52fafe7617e"></a>

### PgConfigPath

Gets the absolute path to <code>pg_config</code>.

```csharp
public string PgConfigPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-d99521cd45da1cc1"></a>

### PgCtlPath

Gets the platform-correct path to PostgreSQL's <code>pg_ctl</code> executable.

```csharp
public string PgCtlPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-07e3de557bc57ea7"></a>

### PreprocessorFlags

Gets the preprocessor arguments reported by this installation's build configuration.

```csharp
public string PreprocessorFlags { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-64464e9d943d85d8"></a>

### PsqlPath

Gets the platform-correct path to PostgreSQL's <code>psql</code> executable.

```csharp
public string PsqlPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-b1c60215c17345b9"></a>

### ServerIncludeDirectory

Gets the server header directory for compiling native extension boundaries against this installation.

```csharp
public string ServerIncludeDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-cd50cc633baee1f3"></a>

### SharedDirectory

Gets PostgreSQL's architecture-independent shared-data directory.

```csharp
public string SharedDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-418857931de723ae"></a>

### Version

Gets the PostgreSQL version reported by this installation.

```csharp
public PostgresVersion Version { get; }
```

Value: [PostgresVersion](/api/ankus.pgconfig.postgresversion/)


## Methods

<a id="member-8750358bbda10a9c"></a>

### CreateAsync(string, CancellationToken)

Creates an installation descriptor by querying an explicit
<code>pg_config</code> executable.

```csharp
public static Task<PostgresInstallation> CreateAsync(string pgConfigPath, CancellationToken cancellationToken = default)
```

Parameters:

`pgConfigPath` — [string](https://learn.microsoft.com/dotnet/api/system.string)

Path to <code>pg_config</code>.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancels <code>pg_config</code> queries.

Returns: [Task&lt;PostgresInstallation&gt;](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task-1)

The queried PostgreSQL installation.

<a id="member-947d24349ac1418b"></a>

### DiscoverAsync(int, CancellationToken)

Discovers a specific PostgreSQL major without requiring environment variables.
Nonstandard installations can be registered as pgXX paths in <code>~/.ankus/config.json</code>.

```csharp
public static Task<PostgresInstallation> DiscoverAsync(int major, CancellationToken cancellationToken = default)
```

Parameters:

`major` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The required PostgreSQL major version.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancels installation queries.

Returns: [Task&lt;PostgresInstallation&gt;](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task-1)

The first matching installation in discovery order.

<a id="member-e860adcbc659aea3"></a>

### DiscoverAsync(CancellationToken)

Discovers PostgreSQL 18 from persisted configuration, managed installations, PATH, and platform installation locations.

```csharp
public static Task<PostgresInstallation> DiscoverAsync(CancellationToken cancellationToken = default)
```

Parameters:

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancels <code>pg_config</code> queries.

Returns: [Task&lt;PostgresInstallation&gt;](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task-1)

The discovered PostgreSQL installation.
