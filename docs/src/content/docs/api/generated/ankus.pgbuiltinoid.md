---
title: 'PgBuiltInOid'
description: 'API reference for Ankus.PgBuiltInOid.'
slug: api/ankus.pgbuiltinoid
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Names numeric constants from pgrx's PostgreSQL 13–19 built-in OID catalogs.

```csharp
public enum PgBuiltInOid : uint
```

Members retain exact unsigned values. Renamed symbols share one member, named after the newest source spelling.
Use PgBuiltInOids for version-aware conversion and native names. A member does not imply availability in every
server version, catalog existence, or object category. The source naming heuristic also includes non-object constants.
Zero is not a defined member. Ordinary PostgreSQL oid datums continue to use uint, independently of this catalog.

## Fields

<a id="member-3470fcfef439fa61"></a>

### AccessMethodOperatorRelationId

The <code>AccessMethodOperatorRelationId</code> constant (<code>2602</code>).

```csharp
AccessMethodOperatorRelationId = 2602
```

<code>AccessMethodOperatorRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-effd1535d457dc18"></a>

### AccessMethodProcedureRelationId

The <code>AccessMethodProcedureRelationId</code> constant (<code>2603</code>).

```csharp
AccessMethodProcedureRelationId = 2603
```

<code>AccessMethodProcedureRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-677c8ed6efb429f3"></a>

### AccessMethodRelationId

The <code>AccessMethodRelationId</code> constant (<code>2601</code>).

```csharp
AccessMethodRelationId = 2601
```

<code>AccessMethodRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-599c40a6b1b8b77f"></a>

### AclItemArrayOid

The <code>ACLITEMARRAYOID</code> constant (<code>1034</code>).

```csharp
AclItemArrayOid = 1034
```

<code>ACLITEMARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8dcd9893e5a9b936"></a>

### AclItemOid

The <code>ACLITEMOID</code> constant (<code>1033</code>).

```csharp
AclItemOid = 1033
```

<code>ACLITEMOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ba022d74850cbf10"></a>

### AnyArrayOid

The <code>ANYARRAYOID</code> constant (<code>2277</code>).

```csharp
AnyArrayOid = 2277
```

<code>ANYARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3675caf2290ac6b2"></a>

### AnyCompatibleArrayOid

The <code>ANYCOMPATIBLEARRAYOID</code> constant (<code>5078</code>).

```csharp
AnyCompatibleArrayOid = 5078
```

<code>ANYCOMPATIBLEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bf033106757d3d28"></a>

### AnyCompatibleMultirangeOid

The <code>ANYCOMPATIBLEMULTIRANGEOID</code> constant (<code>4538</code>).

```csharp
AnyCompatibleMultirangeOid = 4538
```

<code>ANYCOMPATIBLEMULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-a9c33abb4e03a4f9"></a>

### AnyCompatibleNonArrayOid

The <code>ANYCOMPATIBLENONARRAYOID</code> constant (<code>5079</code>).

```csharp
AnyCompatibleNonArrayOid = 5079
```

<code>ANYCOMPATIBLENONARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e66f0e811f027561"></a>

### AnyCompatibleOid

The <code>ANYCOMPATIBLEOID</code> constant (<code>5077</code>).

```csharp
AnyCompatibleOid = 5077
```

<code>ANYCOMPATIBLEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ea1fce04b00e7a75"></a>

### AnyCompatibleRangeOid

The <code>ANYCOMPATIBLERANGEOID</code> constant (<code>5080</code>).

```csharp
AnyCompatibleRangeOid = 5080
```

<code>ANYCOMPATIBLERANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-195ec5b5dc6cfe16"></a>

### AnyElementOid

The <code>ANYELEMENTOID</code> constant (<code>2283</code>).

```csharp
AnyElementOid = 2283
```

<code>ANYELEMENTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e0f59b6914f37df3"></a>

### AnyEnumOid

The <code>ANYENUMOID</code> constant (<code>3500</code>).

```csharp
AnyEnumOid = 3500
```

<code>ANYENUMOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e44c4cc89e84a959"></a>

### AnyMultirangeOid

The <code>ANYMULTIRANGEOID</code> constant (<code>4537</code>).

```csharp
AnyMultirangeOid = 4537
```

<code>ANYMULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-027042f430a8f300"></a>

### AnyNonArrayOid

The <code>ANYNONARRAYOID</code> constant (<code>2776</code>).

```csharp
AnyNonArrayOid = 2776
```

<code>ANYNONARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c8ff32f16e47b219"></a>

### AnyOid

The <code>ANYOID</code> constant (<code>2276</code>).

```csharp
AnyOid = 2276
```

<code>ANYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9984a41608451982"></a>

### AnyRangeOid

The <code>ANYRANGEOID</code> constant (<code>3831</code>).

```csharp
AnyRangeOid = 3831
```

<code>ANYRANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0490656c918744fc"></a>

### AttributeRelationId

The <code>AttributeRelationId</code> constant (<code>1249</code>).

```csharp
AttributeRelationId = 1249
```

<code>AttributeRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-a86d7282880b310b"></a>

### AuthIdRelationId

The <code>AuthIdRelationId</code> constant (<code>1260</code>).

```csharp
AuthIdRelationId = 1260
```

<code>AuthIdRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-aeb02ce44a5ec35f"></a>

### AuthMemRelationId

The <code>AuthMemRelationId</code> constant (<code>1261</code>).

```csharp
AuthMemRelationId = 1261
```

<code>AuthMemRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-34a81bb88b80d258"></a>

### BTreeAmOid

The <code>BTREE_AM_OID</code> constant (<code>403</code>).

```csharp
BTreeAmOid = 403
```

<code>BTREE_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bf4c6af54e7e09da"></a>

### BitArrayOid

The <code>BITARRAYOID</code> constant (<code>1561</code>).

```csharp
BitArrayOid = 1561
```

<code>BITARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-6d91ac67d5100426"></a>

### BitOid

The <code>BITOID</code> constant (<code>1560</code>).

```csharp
BitOid = 1560
```

<code>BITOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-554d4f1d59993478"></a>

### BoolArrayOid

The <code>BOOLARRAYOID</code> constant (<code>1000</code>).

```csharp
BoolArrayOid = 1000
```

<code>BOOLARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e7fcbc74c0a87e05"></a>

### BoolBTreeFamOid

The <code>BOOL_BTREE_FAM_OID</code> constant (<code>424</code>).

```csharp
BoolBTreeFamOid = 424
```

<code>BOOL_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-67ae672bbcf527d2"></a>

### BoolHashFamOid

The <code>BOOL_HASH_FAM_OID</code> constant (<code>2222</code>).

```csharp
BoolHashFamOid = 2222
```

<code>BOOL_HASH_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-44c7880f81ecdd16"></a>

### BoolOid

The <code>BOOLOID</code> constant (<code>16</code>).

```csharp
BoolOid = 16
```

<code>BOOLOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-59938aa1f3f72f7e"></a>

### BoxArrayOid

The <code>BOXARRAYOID</code> constant (<code>1020</code>).

```csharp
BoxArrayOid = 1020
```

<code>BOXARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-027eb4281d9a5af1"></a>

### BoxOid

The <code>BOXOID</code> constant (<code>603</code>).

```csharp
BoxOid = 603
```

<code>BOXOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-364b077cd38edf6b"></a>

### BpCharArrayOid

The <code>BPCHARARRAYOID</code> constant (<code>1014</code>).

```csharp
BpCharArrayOid = 1014
```

<code>BPCHARARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d83a24b70687bfa5"></a>

### BpCharBTreeFamOid

The <code>BPCHAR_BTREE_FAM_OID</code> constant (<code>426</code>).

```csharp
BpCharBTreeFamOid = 426
```

<code>BPCHAR_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-95149a255a4239f7"></a>

### BpCharBTreePatternOpsOid

The <code>BPCHAR_BTREE_PATTERN_OPS_OID</code> constant (<code>4219</code>).

```csharp
BpCharBTreePatternOpsOid = 4219
```

<code>BPCHAR_BTREE_PATTERN_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9513f85ea52b13c0"></a>

### BpCharOid

The <code>BPCHAROID</code> constant (<code>1042</code>).

```csharp
BpCharOid = 1042
```

<code>BPCHAROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d98507968eb9df2c"></a>

### BpCharPatternBTreeFamOid

The <code>BPCHAR_PATTERN_BTREE_FAM_OID</code> constant (<code>2097</code>).

```csharp
BpCharPatternBTreeFamOid = 2097
```

<code>BPCHAR_PATTERN_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e80f6fbb42f5647b"></a>

### BrinAmOid

The <code>BRIN_AM_OID</code> constant (<code>3580</code>).

```csharp
BrinAmOid = 3580
```

<code>BRIN_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e06c091a20f22f2f"></a>

### ByteaArrayOid

The <code>BYTEAARRAYOID</code> constant (<code>1001</code>).

```csharp
ByteaArrayOid = 1001
```

<code>BYTEAARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-456d40fe1d3b6d14"></a>

### ByteaBTreeFamOid

The <code>BYTEA_BTREE_FAM_OID</code> constant (<code>428</code>).

```csharp
ByteaBTreeFamOid = 428
```

<code>BYTEA_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-5b1683fabb935721"></a>

### ByteaOid

The <code>BYTEAOID</code> constant (<code>17</code>).

```csharp
ByteaOid = 17
```

<code>BYTEAOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-6afbf4fcfe2be65f"></a>

### CCollationOid

The <code>C_COLLATION_OID</code> constant (<code>950</code>).

```csharp
CCollationOid = 950
```

<code>C_COLLATION_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-efdd9acdd7028ad5"></a>

### CStringArrayOid

The <code>CSTRINGARRAYOID</code> constant (<code>1263</code>).

```csharp
CStringArrayOid = 1263
```

<code>CSTRINGARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-be3616175803ab12"></a>

### CStringOid

The <code>CSTRINGOID</code> constant (<code>2275</code>).

```csharp
CStringOid = 2275
```

<code>CSTRINGOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0d1d7804a67cadea"></a>

### CharArrayOid

The <code>CHARARRAYOID</code> constant (<code>1002</code>).

```csharp
CharArrayOid = 1002
```

<code>CHARARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-46da3b081fe40385"></a>

### CharOid

The <code>CHAROID</code> constant (<code>18</code>).

```csharp
CharOid = 18
```

<code>CHAROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3485e02b1bdc600e"></a>

### CidArrayOid

The <code>CIDARRAYOID</code> constant (<code>1012</code>).

```csharp
CidArrayOid = 1012
```

<code>CIDARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-710449f52d52be3a"></a>

### CidOid

The <code>CIDOID</code> constant (<code>29</code>).

```csharp
CidOid = 29
```

<code>CIDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1242f4eec56d0dea"></a>

### CidrArrayOid

The <code>CIDRARRAYOID</code> constant (<code>651</code>).

```csharp
CidrArrayOid = 651
```

<code>CIDRARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7ff3dfed6d9270ee"></a>

### CidrOid

The <code>CIDROID</code> constant (<code>650</code>).

```csharp
CidrOid = 650
```

<code>CIDROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-afe7b607bff96a1f"></a>

### CircleArrayOid

The <code>CIRCLEARRAYOID</code> constant (<code>719</code>).

```csharp
CircleArrayOid = 719
```

<code>CIRCLEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-61b7ef4263939a8e"></a>

### CircleOid

The <code>CIRCLEOID</code> constant (<code>718</code>).

```csharp
CircleOid = 718
```

<code>CIRCLEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-42c2d1b70f0a31b7"></a>

### CollationRelationId

The <code>CollationRelationId</code> constant (<code>3456</code>).

```csharp
CollationRelationId = 3456
```

<code>CollationRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7a164af865bec8ae"></a>

### DatabaseRelationId

The <code>DatabaseRelationId</code> constant (<code>1262</code>).

```csharp
DatabaseRelationId = 1262
```

<code>DatabaseRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d4e4dcf6e00a4294"></a>

### DateArrayOid

The <code>DATEARRAYOID</code> constant (<code>1182</code>).

```csharp
DateArrayOid = 1182
```

<code>DATEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1ca2e286913c0ba7"></a>

### DateBTreeOpsOid

The <code>DATE_BTREE_OPS_OID</code> constant (<code>3122</code>).

```csharp
DateBTreeOpsOid = 3122
```

<code>DATE_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3b35d97a99558db6"></a>

### DateMultirangeArrayOid

The <code>DATEMULTIRANGEARRAYOID</code> constant (<code>6155</code>).

```csharp
DateMultirangeArrayOid = 6155
```

<code>DATEMULTIRANGEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-3ba8e45492f564ee"></a>

### DateMultirangeOid

The <code>DATEMULTIRANGEOID</code> constant (<code>4535</code>).

```csharp
DateMultirangeOid = 4535
```

<code>DATEMULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-7789b87ded6512f4"></a>

### DateOid

The <code>DATEOID</code> constant (<code>1082</code>).

```csharp
DateOid = 1082
```

<code>DATEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-2129d62d98b3c362"></a>

### DateRangeArrayOid

The <code>DATERANGEARRAYOID</code> constant (<code>3913</code>).

```csharp
DateRangeArrayOid = 3913
```

<code>DATERANGEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0d9dbfa830113504"></a>

### DateRangeOid

The <code>DATERANGEOID</code> constant (<code>3912</code>).

```csharp
DateRangeOid = 3912
```

<code>DATERANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0d134b1c5a9c215d"></a>

### DefaultCollationOid

The <code>DEFAULT_COLLATION_OID</code> constant (<code>100</code>).

```csharp
DefaultCollationOid = 100
```

<code>DEFAULT_COLLATION_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-421dd26cc50dfbba"></a>

### DefaultTablespaceOid

The <code>DEFAULTTABLESPACE_OID</code> constant (<code>1663</code>).

```csharp
DefaultTablespaceOid = 1663
```

<code>DEFAULTTABLESPACE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-60fed65988470ef4"></a>

### EnumRelationId

The <code>EnumRelationId</code> constant (<code>3501</code>).

```csharp
EnumRelationId = 3501
```

<code>EnumRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f342af3e119670f6"></a>

### EventTriggerOid

The <code>EVENT_TRIGGEROID</code> constant (<code>3838</code>).

```csharp
EventTriggerOid = 3838
```

<code>EVTTRIGGEROID</code> in PostgreSQL 13.
<code>EVENT_TRIGGEROID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-5db316739761c3f3"></a>

### EventTriggerRelationId

The <code>EventTriggerRelationId</code> constant (<code>3466</code>).

```csharp
EventTriggerRelationId = 3466
```

<code>EventTriggerRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-6093c5bedeaf6a47"></a>

### ExtensionRelationId

The <code>ExtensionRelationId</code> constant (<code>3079</code>).

```csharp
ExtensionRelationId = 3079
```

<code>ExtensionRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b7bdf01cf8d0fa8e"></a>

### FdwHandlerOid

The <code>FDW_HANDLEROID</code> constant (<code>3115</code>).

```csharp
FdwHandlerOid = 3115
```

<code>FDW_HANDLEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-aff47c978001469f"></a>

### Float4ArrayOid

The <code>FLOAT4ARRAYOID</code> constant (<code>1021</code>).

```csharp
Float4ArrayOid = 1021
```

<code>FLOAT4ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8e96a8c1f8442707"></a>

### Float4Oid

The <code>FLOAT4OID</code> constant (<code>700</code>).

```csharp
Float4Oid = 700
```

<code>FLOAT4OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9d1cd316cdf59347"></a>

### Float8ArrayOid

The <code>FLOAT8ARRAYOID</code> constant (<code>1022</code>).

```csharp
Float8ArrayOid = 1022
```

<code>FLOAT8ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b58a3ac665dace1e"></a>

### Float8BTreeOpsOid

The <code>FLOAT8_BTREE_OPS_OID</code> constant (<code>3123</code>).

```csharp
Float8BTreeOpsOid = 3123
```

<code>FLOAT8_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-a1b77f739a9eb75f"></a>

### Float8Oid

The <code>FLOAT8OID</code> constant (<code>701</code>).

```csharp
Float8Oid = 701
```

<code>FLOAT8OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ccd342bd190ff44b"></a>

### ForeignDataWrapperRelationId

The <code>ForeignDataWrapperRelationId</code> constant (<code>2328</code>).

```csharp
ForeignDataWrapperRelationId = 2328
```

<code>ForeignDataWrapperRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3766714d719dbd12"></a>

### ForeignServerRelationId

The <code>ForeignServerRelationId</code> constant (<code>1417</code>).

```csharp
ForeignServerRelationId = 1417
```

<code>ForeignServerRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f6de5d12f4508cfb"></a>

### ForeignTableRelationId

The <code>ForeignTableRelationId</code> constant (<code>3118</code>).

```csharp
ForeignTableRelationId = 3118
```

<code>ForeignTableRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c72cbed062cab4d3"></a>

### FunctionBinaryUpgradeSetNextArrayPgTypeOid

The <code>F_BINARY_UPGRADE_SET_NEXT_ARRAY_PG_TYPE_OID</code> constant (<code>3584</code>).

```csharp
FunctionBinaryUpgradeSetNextArrayPgTypeOid = 3584
```

<code>F_BINARY_UPGRADE_SET_NEXT_ARRAY_PG_TYPE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-cea91600b48fe93c"></a>

### FunctionBinaryUpgradeSetNextHeapPgClassOid

The <code>F_BINARY_UPGRADE_SET_NEXT_HEAP_PG_CLASS_OID</code> constant (<code>3586</code>).

```csharp
FunctionBinaryUpgradeSetNextHeapPgClassOid = 3586
```

<code>F_BINARY_UPGRADE_SET_NEXT_HEAP_PG_CLASS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-49125aec5ee1ca76"></a>

### FunctionBinaryUpgradeSetNextIndexPgClassOid

The <code>F_BINARY_UPGRADE_SET_NEXT_INDEX_PG_CLASS_OID</code> constant (<code>3587</code>).

```csharp
FunctionBinaryUpgradeSetNextIndexPgClassOid = 3587
```

<code>F_BINARY_UPGRADE_SET_NEXT_INDEX_PG_CLASS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8cf00c9438d08d65"></a>

### FunctionBinaryUpgradeSetNextMultirangeArrayPgTypeOid

The <code>F_BINARY_UPGRADE_SET_NEXT_MULTIRANGE_ARRAY_PG_TYPE_OID</code> constant (<code>4391</code>).

```csharp
FunctionBinaryUpgradeSetNextMultirangeArrayPgTypeOid = 4391
```

<code>F_BINARY_UPGRADE_SET_NEXT_MULTIRANGE_ARRAY_PG_TYPE_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-6278de7b38a42eec"></a>

### FunctionBinaryUpgradeSetNextMultirangePgTypeOid

The <code>F_BINARY_UPGRADE_SET_NEXT_MULTIRANGE_PG_TYPE_OID</code> constant (<code>4390</code>).

```csharp
FunctionBinaryUpgradeSetNextMultirangePgTypeOid = 4390
```

<code>F_BINARY_UPGRADE_SET_NEXT_MULTIRANGE_PG_TYPE_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-831fe7ad1f8b7bb4"></a>

### FunctionBinaryUpgradeSetNextPgAuthIdOid

The <code>F_BINARY_UPGRADE_SET_NEXT_PG_AUTHID_OID</code> constant (<code>3590</code>).

```csharp
FunctionBinaryUpgradeSetNextPgAuthIdOid = 3590
```

<code>F_BINARY_UPGRADE_SET_NEXT_PG_AUTHID_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-cd6d2e521c794db6"></a>

### FunctionBinaryUpgradeSetNextPgEnumOid

The <code>F_BINARY_UPGRADE_SET_NEXT_PG_ENUM_OID</code> constant (<code>3589</code>).

```csharp
FunctionBinaryUpgradeSetNextPgEnumOid = 3589
```

<code>F_BINARY_UPGRADE_SET_NEXT_PG_ENUM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d64a8b2759d860a1"></a>

### FunctionBinaryUpgradeSetNextPgTablespaceOid

The <code>F_BINARY_UPGRADE_SET_NEXT_PG_TABLESPACE_OID</code> constant (<code>4548</code>).

```csharp
FunctionBinaryUpgradeSetNextPgTablespaceOid = 4548
```

<code>F_BINARY_UPGRADE_SET_NEXT_PG_TABLESPACE_OID</code> in PostgreSQL 15, 16, 17, 18, 19.

<a id="member-8d483aa1b4e608b8"></a>

### FunctionBinaryUpgradeSetNextPgTypeOid

The <code>F_BINARY_UPGRADE_SET_NEXT_PG_TYPE_OID</code> constant (<code>3582</code>).

```csharp
FunctionBinaryUpgradeSetNextPgTypeOid = 3582
```

<code>F_BINARY_UPGRADE_SET_NEXT_PG_TYPE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-87bdb337c015fe2d"></a>

### FunctionBinaryUpgradeSetNextToastPgClassOid

The <code>F_BINARY_UPGRADE_SET_NEXT_TOAST_PG_CLASS_OID</code> constant (<code>3588</code>).

```csharp
FunctionBinaryUpgradeSetNextToastPgClassOid = 3588
```

<code>F_BINARY_UPGRADE_SET_NEXT_TOAST_PG_CLASS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-5fe6d1e0884b3dc6"></a>

### FunctionBinaryUpgradeSetNextToastPgTypeOid

The <code>F_BINARY_UPGRADE_SET_NEXT_TOAST_PG_TYPE_OID</code> constant (<code>3585</code>).

```csharp
FunctionBinaryUpgradeSetNextToastPgTypeOid = 3585
```

<code>F_BINARY_UPGRADE_SET_NEXT_TOAST_PG_TYPE_OID</code> in PostgreSQL 13.

<a id="member-96d63e6eb3bec564"></a>

### FunctionCurrtidByrelOid

The <code>F_CURRTID_BYRELOID</code> constant (<code>1293</code>).

```csharp
FunctionCurrtidByrelOid = 1293
```

<code>F_CURRTID_BYRELOID</code> in PostgreSQL 13.

<a id="member-ec0e74edf9d6fac2"></a>

### FunctionCurrvalOid

The <code>F_CURRVAL_OID</code> constant (<code>1575</code>).

```csharp
FunctionCurrvalOid = 1575
```

<code>F_CURRVAL_OID</code> in PostgreSQL 13.

<a id="member-221d745b3f337deb"></a>

### FunctionHashOid

The <code>F_HASHOID</code> constant (<code>453</code>).

```csharp
FunctionHashOid = 453
```

<code>F_HASHOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-18498eb4e99f44d8"></a>

### FunctionInt8Oid

The <code>F_INT8_OID</code> constant (<code>1288</code>).

```csharp
FunctionInt8Oid = 1288
```

<code>F_INT8_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-d4bc8fc6471b3988"></a>

### FunctionLoGetOid

The <code>F_LO_GET_OID</code> constant (<code>3458</code>).

```csharp
FunctionLoGetOid = 3458
```

<code>F_LO_GET_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-9a3f796f4b889948"></a>

### FunctionLoImportTextOid

The <code>F_LO_IMPORT_TEXT_OID</code> constant (<code>767</code>).

```csharp
FunctionLoImportTextOid = 767
```

<code>F_BE_LO_IMPORT_WITH_OID</code> in PostgreSQL 13.
<code>F_LO_IMPORT_TEXT_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-d8faec506479cfbf"></a>

### FunctionMaxOid

The <code>F_MAX_OID</code> constant (<code>2118</code>).

```csharp
FunctionMaxOid = 2118
```

<code>F_MAX_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-e466722864f81942"></a>

### FunctionMinOid

The <code>F_MIN_OID</code> constant (<code>2134</code>).

```csharp
FunctionMinOid = 2134
```

<code>F_MIN_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-cc0ffc070c218e0a"></a>

### FunctionNameconcatOid

The <code>F_NAMECONCATOID</code> constant (<code>266</code>).

```csharp
FunctionNameconcatOid = 266
```

<code>F_NAMECONCATOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-98dc24ad12b796a6"></a>

### FunctionNextvalOid

The <code>F_NEXTVAL_OID</code> constant (<code>1574</code>).

```csharp
FunctionNextvalOid = 1574
```

<code>F_NEXTVAL_OID</code> in PostgreSQL 13.

<a id="member-a09735ba6d4af50a"></a>

### FunctionObjDescriptionOid

The <code>F_OBJ_DESCRIPTION_OID</code> constant (<code>1348</code>).

```csharp
FunctionObjDescriptionOid = 1348
```

<code>F_OBJ_DESCRIPTION_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-79580f0adcaf1869"></a>

### FunctionOid

The <code>F_OID</code> constant (<code>1287</code>).

```csharp
FunctionOid = 1287
```

<code>F_I8TOOID</code> in PostgreSQL 13.
<code>F_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-4049a3085e52df35"></a>

### FunctionPgDatabaseSizeOid

The <code>F_PG_DATABASE_SIZE_OID</code> constant (<code>2324</code>).

```csharp
FunctionPgDatabaseSizeOid = 2324
```

<code>F_PG_DATABASE_SIZE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-06fdb78a169156b9"></a>

### FunctionPgEventTriggerTableRewriteOid

The <code>F_PG_EVENT_TRIGGER_TABLE_REWRITE_OID</code> constant (<code>4566</code>).

```csharp
FunctionPgEventTriggerTableRewriteOid = 4566
```

<code>F_PG_EVENT_TRIGGER_TABLE_REWRITE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bb84c37c889415d6"></a>

### FunctionPgGetConstraintdefOid

The <code>F_PG_GET_CONSTRAINTDEF_OID</code> constant (<code>1387</code>).

```csharp
FunctionPgGetConstraintdefOid = 1387
```

<code>F_PG_GET_CONSTRAINTDEF_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-6f7cbffb3870ff0d"></a>

### FunctionPgGetExprPgNodeTreeOid

The <code>F_PG_GET_EXPR_PG_NODE_TREE_OID</code> constant (<code>1716</code>).

```csharp
FunctionPgGetExprPgNodeTreeOid = 1716
```

<code>F_PG_GET_EXPR_PG_NODE_TREE_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-6db85c975ba6045d"></a>

### FunctionPgGetIndexdefOid

The <code>F_PG_GET_INDEXDEF_OID</code> constant (<code>1643</code>).

```csharp
FunctionPgGetIndexdefOid = 1643
```

<code>F_PG_GET_INDEXDEF_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-c6bd41248d80d1d7"></a>

### FunctionPgGetPublicationTablesTextOid

The <code>F_PG_GET_PUBLICATION_TABLES__TEXT_OID</code> constant (<code>6435</code>).

```csharp
FunctionPgGetPublicationTablesTextOid = 6435
```

<code>F_PG_GET_PUBLICATION_TABLES__TEXT_OID</code> in PostgreSQL 19.

<a id="member-fd400b5610fdf0de"></a>

### FunctionPgGetRuledefOid

The <code>F_PG_GET_RULEDEF_OID</code> constant (<code>1573</code>).

```csharp
FunctionPgGetRuledefOid = 1573
```

<code>F_PG_GET_RULEDEF_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-07f949ddd162b05c"></a>

### FunctionPgGetTriggerdefOid

The <code>F_PG_GET_TRIGGERDEF_OID</code> constant (<code>1662</code>).

```csharp
FunctionPgGetTriggerdefOid = 1662
```

<code>F_PG_GET_TRIGGERDEF_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-880fabcdbb08a193"></a>

### FunctionPgGetViewdefOid

The <code>F_PG_GET_VIEWDEF_OID</code> constant (<code>1641</code>).

```csharp
FunctionPgGetViewdefOid = 1641
```

<code>F_PG_GET_VIEWDEF_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-db1ae19f7842cdf5"></a>

### FunctionPgLsTmpdirOid

The <code>F_PG_LS_TMPDIR_OID</code> constant (<code>5030</code>).

```csharp
FunctionPgLsTmpdirOid = 5030
```

<code>F_PG_LS_TMPDIR_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-0aae1cde7d035697"></a>

### FunctionPgNextOid

The <code>F_PG_NEXTOID</code> constant (<code>275</code>).

```csharp
FunctionPgNextOid = 275
```

<code>F_PG_NEXTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1f52406876edc6c4"></a>

### FunctionPgReplicationOriginOid

The <code>F_PG_REPLICATION_ORIGIN_OID</code> constant (<code>6005</code>).

```csharp
FunctionPgReplicationOriginOid = 6005
```

<code>F_PG_REPLICATION_ORIGIN_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-df5c0963695d5890"></a>

### FunctionPgTablespaceSizeOid

The <code>F_PG_TABLESPACE_SIZE_OID</code> constant (<code>2322</code>).

```csharp
FunctionPgTablespaceSizeOid = 2322
```

<code>F_PG_TABLESPACE_SIZE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-899ed814e25c0cf6"></a>

### FunctionRowSecurityActiveOid

The <code>F_ROW_SECURITY_ACTIVE_OID</code> constant (<code>3298</code>).

```csharp
FunctionRowSecurityActiveOid = 3298
```

<code>F_ROW_SECURITY_ACTIVE_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-102b486918da086f"></a>

### FunctionSetval3Oid

The <code>F_SETVAL3_OID</code> constant (<code>1765</code>).

```csharp
FunctionSetval3Oid = 1765
```

<code>F_SETVAL3_OID</code> in PostgreSQL 13.

<a id="member-b4a28a90af41d650"></a>

### FunctionSetvalOid

The <code>F_SETVAL_OID</code> constant (<code>1576</code>).

```csharp
FunctionSetvalOid = 1576
```

<code>F_SETVAL_OID</code> in PostgreSQL 13.

<a id="member-0d42457cdb4adb24"></a>

### FunctionTsTokenTypeOid

The <code>F_TS_TOKEN_TYPE_OID</code> constant (<code>3713</code>).

```csharp
FunctionTsTokenTypeOid = 3713
```

<code>F_TS_TOKEN_TYPE_OID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-99c57f5fa83b259b"></a>

### GinAmOid

The <code>GIN_AM_OID</code> constant (<code>2742</code>).

```csharp
GinAmOid = 2742
```

<code>GIN_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-4a6ff5b23ff64ea0"></a>

### GistAmOid

The <code>GIST_AM_OID</code> constant (<code>783</code>).

```csharp
GistAmOid = 783
```

<code>GIST_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-5af083febe736fec"></a>

### GlobalTablespaceOid

The <code>GLOBALTABLESPACE_OID</code> constant (<code>1664</code>).

```csharp
GlobalTablespaceOid = 1664
```

<code>GLOBALTABLESPACE_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c15222651a5e8555"></a>

### GtsVectorArrayOid

The <code>GTSVECTORARRAYOID</code> constant (<code>3644</code>).

```csharp
GtsVectorArrayOid = 3644
```

<code>GTSVECTORARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-6c4b97876cdd9659"></a>

### GtsVectorOid

The <code>GTSVECTOROID</code> constant (<code>3642</code>).

```csharp
GtsVectorOid = 3642
```

<code>GTSVECTOROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-33b9afc5c64005a4"></a>

### HashAmOid

The <code>HASH_AM_OID</code> constant (<code>405</code>).

```csharp
HashAmOid = 405
```

<code>HASH_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7d4a96ebdf703842"></a>

### HeapTableAmHandlerOid

The <code>HEAP_TABLE_AM_HANDLER_OID</code> constant (<code>3</code>).

```csharp
HeapTableAmHandlerOid = 3
```

<code>HEAP_TABLE_AM_HANDLER_OID</code> in PostgreSQL 13.

<a id="member-5b2aeacfbb239ab0"></a>

### HeapTableAmOid

The <code>HEAP_TABLE_AM_OID</code> constant (<code>2</code>).

```csharp
HeapTableAmOid = 2
```

<code>HEAP_TABLE_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-32514b8968fe5f7c"></a>

### IndexAmHandlerOid

The <code>INDEX_AM_HANDLEROID</code> constant (<code>325</code>).

```csharp
IndexAmHandlerOid = 325
```

<code>INDEX_AM_HANDLEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-243e6a33a406a865"></a>

### IndexRelationId

The <code>IndexRelationId</code> constant (<code>2610</code>).

```csharp
IndexRelationId = 2610
```

<code>IndexRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-940f7d3f84e888c0"></a>

### InetArrayOid

The <code>INETARRAYOID</code> constant (<code>1041</code>).

```csharp
InetArrayOid = 1041
```

<code>INETARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f0fce8e92a2dd0b2"></a>

### InetOid

The <code>INETOID</code> constant (<code>869</code>).

```csharp
InetOid = 869
```

<code>INETOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f119b96a802ebefa"></a>

### Int2ArrayOid

The <code>INT2ARRAYOID</code> constant (<code>1005</code>).

```csharp
Int2ArrayOid = 1005
```

<code>INT2ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-67fc8cae1198d6b8"></a>

### Int2BTreeOpsOid

The <code>INT2_BTREE_OPS_OID</code> constant (<code>1979</code>).

```csharp
Int2BTreeOpsOid = 1979
```

<code>INT2_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-09a21fa2c33d3550"></a>

### Int2Oid

The <code>INT2OID</code> constant (<code>21</code>).

```csharp
Int2Oid = 21
```

<code>INT2OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-daa0eceea6c9723d"></a>

### Int2VectorArrayOid

The <code>INT2VECTORARRAYOID</code> constant (<code>1006</code>).

```csharp
Int2VectorArrayOid = 1006
```

<code>INT2VECTORARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-078bdd81883110f3"></a>

### Int2VectorOid

The <code>INT2VECTOROID</code> constant (<code>22</code>).

```csharp
Int2VectorOid = 22
```

<code>INT2VECTOROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-95c12fe67841d541"></a>

### Int4ArrayOid

The <code>INT4ARRAYOID</code> constant (<code>1007</code>).

```csharp
Int4ArrayOid = 1007
```

<code>INT4ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-fe7768487add0bea"></a>

### Int4BTreeOpsOid

The <code>INT4_BTREE_OPS_OID</code> constant (<code>1978</code>).

```csharp
Int4BTreeOpsOid = 1978
```

<code>INT4_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b403ee1fb9218c9b"></a>

### Int4MultirangeArrayOid

The <code>INT4MULTIRANGEARRAYOID</code> constant (<code>6150</code>).

```csharp
Int4MultirangeArrayOid = 6150
```

<code>INT4MULTIRANGEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-019e4fa88df74d25"></a>

### Int4MultirangeOid

The <code>INT4MULTIRANGEOID</code> constant (<code>4451</code>).

```csharp
Int4MultirangeOid = 4451
```

<code>INT4MULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-4db2609bdd4b6879"></a>

### Int4Oid

The <code>INT4OID</code> constant (<code>23</code>).

```csharp
Int4Oid = 23
```

<code>INT4OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8c1b60aaf1cae740"></a>

### Int4RangeArrayOid

The <code>INT4RANGEARRAYOID</code> constant (<code>3905</code>).

```csharp
Int4RangeArrayOid = 3905
```

<code>INT4RANGEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8a3160118b9f9967"></a>

### Int4RangeOid

The <code>INT4RANGEOID</code> constant (<code>3904</code>).

```csharp
Int4RangeOid = 3904
```

<code>INT4RANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ef17b0b831931bcf"></a>

### Int8ArrayOid

The <code>INT8ARRAYOID</code> constant (<code>1016</code>).

```csharp
Int8ArrayOid = 1016
```

<code>INT8ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3d7757e721d428aa"></a>

### Int8BTreeOpsOid

The <code>INT8_BTREE_OPS_OID</code> constant (<code>3124</code>).

```csharp
Int8BTreeOpsOid = 3124
```

<code>INT8_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7b58a433ce275c17"></a>

### Int8MultirangeArrayOid

The <code>INT8MULTIRANGEARRAYOID</code> constant (<code>6157</code>).

```csharp
Int8MultirangeArrayOid = 6157
```

<code>INT8MULTIRANGEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-54c638b22e43d55e"></a>

### Int8MultirangeOid

The <code>INT8MULTIRANGEOID</code> constant (<code>4536</code>).

```csharp
Int8MultirangeOid = 4536
```

<code>INT8MULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-5b5f96b7aea55ad8"></a>

### Int8Oid

The <code>INT8OID</code> constant (<code>20</code>).

```csharp
Int8Oid = 20
```

<code>INT8OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1c49bfa015825e32"></a>

### Int8RangeArrayOid

The <code>INT8RANGEARRAYOID</code> constant (<code>3927</code>).

```csharp
Int8RangeArrayOid = 3927
```

<code>INT8RANGEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-54143826a2ff157b"></a>

### Int8RangeOid

The <code>INT8RANGEOID</code> constant (<code>3926</code>).

```csharp
Int8RangeOid = 3926
```

<code>INT8RANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0b3b987a45ef1b49"></a>

### IntegerBTreeFamOid

The <code>INTEGER_BTREE_FAM_OID</code> constant (<code>1976</code>).

```csharp
IntegerBTreeFamOid = 1976
```

<code>INTEGER_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-44949c7b571e7d43"></a>

### InternalOid

The <code>INTERNALOID</code> constant (<code>2281</code>).

```csharp
InternalOid = 2281
```

<code>INTERNALOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-a838792f97144f8f"></a>

### IntervalArrayOid

The <code>INTERVALARRAYOID</code> constant (<code>1187</code>).

```csharp
IntervalArrayOid = 1187
```

<code>INTERVALARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9a8b970f0de615f0"></a>

### IntervalBTreeFamOid

The <code>INTERVAL_BTREE_FAM_OID</code> constant (<code>1982</code>).

```csharp
IntervalBTreeFamOid = 1982
```

<code>INTERVAL_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0a35c739e18d0e95"></a>

### IntervalOid

The <code>INTERVALOID</code> constant (<code>1186</code>).

```csharp
IntervalOid = 1186
```

<code>INTERVALOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-5f38070eddbd83d5"></a>

### JsonArrayOid

The <code>JSONARRAYOID</code> constant (<code>199</code>).

```csharp
JsonArrayOid = 199
```

<code>JSONARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-179832633342ca58"></a>

### JsonOid

The <code>JSONOID</code> constant (<code>114</code>).

```csharp
JsonOid = 114
```

<code>JSONOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-dc88693c42aed471"></a>

### JsonPathArrayOid

The <code>JSONPATHARRAYOID</code> constant (<code>4073</code>).

```csharp
JsonPathArrayOid = 4073
```

<code>JSONPATHARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f485c2e3ef147909"></a>

### JsonPathOid

The <code>JSONPATHOID</code> constant (<code>4072</code>).

```csharp
JsonPathOid = 4072
```

<code>JSONPATHOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-4db1dba6a08a38f9"></a>

### JsonbArrayOid

The <code>JSONBARRAYOID</code> constant (<code>3807</code>).

```csharp
JsonbArrayOid = 3807
```

<code>JSONBARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-62cca6265a1b492e"></a>

### JsonbOid

The <code>JSONBOID</code> constant (<code>3802</code>).

```csharp
JsonbOid = 3802
```

<code>JSONBOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f536f9bc44285a72"></a>

### LanguageHandlerOid

The <code>LANGUAGE_HANDLEROID</code> constant (<code>2280</code>).

```csharp
LanguageHandlerOid = 2280
```

<code>LANGUAGE_HANDLEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1980416a67565929"></a>

### LineArrayOid

The <code>LINEARRAYOID</code> constant (<code>629</code>).

```csharp
LineArrayOid = 629
```

<code>LINEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-08c8b92417f66f56"></a>

### LineOid

The <code>LINEOID</code> constant (<code>628</code>).

```csharp
LineOid = 628
```

<code>LINEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f13980bc9ce865de"></a>

### LsegArrayOid

The <code>LSEGARRAYOID</code> constant (<code>1018</code>).

```csharp
LsegArrayOid = 1018
```

<code>LSEGARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-06cde6215d3c284e"></a>

### LsegOid

The <code>LSEGOID</code> constant (<code>601</code>).

```csharp
LsegOid = 601
```

<code>LSEGOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-eddff5832dfc62f6"></a>

### Macaddr8ArrayOid

The <code>MACADDR8ARRAYOID</code> constant (<code>775</code>).

```csharp
Macaddr8ArrayOid = 775
```

<code>MACADDR8ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e5d9006416a907de"></a>

### Macaddr8Oid

The <code>MACADDR8OID</code> constant (<code>774</code>).

```csharp
Macaddr8Oid = 774
```

<code>MACADDR8OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ce262b7e0fe9bdfd"></a>

### MacaddrArrayOid

The <code>MACADDRARRAYOID</code> constant (<code>1040</code>).

```csharp
MacaddrArrayOid = 1040
```

<code>MACADDRARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-2b77258950e8d1eb"></a>

### MacaddrOid

The <code>MACADDROID</code> constant (<code>829</code>).

```csharp
MacaddrOid = 829
```

<code>MACADDROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d052e750b0aea070"></a>

### MoneyArrayOid

The <code>MONEYARRAYOID</code> constant (<code>791</code>).

```csharp
MoneyArrayOid = 791
```

<code>MONEYARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-42c4b3c828c3aa77"></a>

### MoneyOid

The <code>MONEYOID</code> constant (<code>790</code>).

```csharp
MoneyOid = 790
```

<code>CASHOID</code> in PostgreSQL 13.
<code>MONEYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-bc4cb000816ea2ff"></a>

### NameArrayOid

The <code>NAMEARRAYOID</code> constant (<code>1003</code>).

```csharp
NameArrayOid = 1003
```

<code>NAMEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8c4f1012d2235f3c"></a>

### NameOid

The <code>NAMEOID</code> constant (<code>19</code>).

```csharp
NameOid = 19
```

<code>NAMEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-312015ba9fa088f9"></a>

### NamespaceRelationId

The <code>NamespaceRelationId</code> constant (<code>2615</code>).

```csharp
NamespaceRelationId = 2615
```

<code>NamespaceRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c81e673a23cf530f"></a>

### NetworkBTreeFamOid

The <code>NETWORK_BTREE_FAM_OID</code> constant (<code>1974</code>).

```csharp
NetworkBTreeFamOid = 1974
```

<code>NETWORK_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bdfce5457123a45b"></a>

### NumMultirangeArrayOid

The <code>NUMMULTIRANGEARRAYOID</code> constant (<code>6151</code>).

```csharp
NumMultirangeArrayOid = 6151
```

<code>NUMMULTIRANGEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-c9173c0826664aca"></a>

### NumMultirangeOid

The <code>NUMMULTIRANGEOID</code> constant (<code>4532</code>).

```csharp
NumMultirangeOid = 4532
```

<code>NUMMULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-239a19dcc1a4647b"></a>

### NumRangeArrayOid

The <code>NUMRANGEARRAYOID</code> constant (<code>3907</code>).

```csharp
NumRangeArrayOid = 3907
```

<code>NUMRANGEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d5a14687e9b6db8e"></a>

### NumRangeOid

The <code>NUMRANGEOID</code> constant (<code>3906</code>).

```csharp
NumRangeOid = 3906
```

<code>NUMRANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-827340310cd079e8"></a>

### NumericArrayOid

The <code>NUMERICARRAYOID</code> constant (<code>1231</code>).

```csharp
NumericArrayOid = 1231
```

<code>NUMERICARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d1df44ce4542a805"></a>

### NumericBTreeOpsOid

The <code>NUMERIC_BTREE_OPS_OID</code> constant (<code>3125</code>).

```csharp
NumericBTreeOpsOid = 3125
```

<code>NUMERIC_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-61f3b5427c9066d6"></a>

### NumericOid

The <code>NUMERICOID</code> constant (<code>1700</code>).

```csharp
NumericOid = 1700
```

<code>NUMERICOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7968f6c98a3d41d4"></a>

### Oid8ArrayOid

The <code>OID8ARRAYOID</code> constant (<code>6442</code>).

```csharp
Oid8ArrayOid = 6442
```

<code>OID8ARRAYOID</code> in PostgreSQL 19.

<a id="member-d6c2347f3d82f5dd"></a>

### Oid8Oid

The <code>OID8OID</code> constant (<code>6437</code>).

```csharp
Oid8Oid = 6437
```

<code>OID8OID</code> in PostgreSQL 19.

<a id="member-dbbebfe8ed8cad87"></a>

### OidArrayOid

The <code>OIDARRAYOID</code> constant (<code>1028</code>).

```csharp
OidArrayOid = 1028
```

<code>OIDARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e7bb2a17a9997cb9"></a>

### OidBTreeFamOid

The <code>OID_BTREE_FAM_OID</code> constant (<code>1989</code>).

```csharp
OidBTreeFamOid = 1989
```

<code>OID_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-de61acb33fd5388c"></a>

### OidBTreeOpsOid

The <code>OID_BTREE_OPS_OID</code> constant (<code>1981</code>).

```csharp
OidBTreeOpsOid = 1981
```

<code>OID_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-00403e4870731acc"></a>

### OidOid

The <code>OIDOID</code> constant (<code>26</code>).

```csharp
OidOid = 26
```

<code>OIDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-6b75a8c2eafc1d37"></a>

### OidVectorArrayOid

The <code>OIDVECTORARRAYOID</code> constant (<code>1013</code>).

```csharp
OidVectorArrayOid = 1013
```

<code>OIDVECTORARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-86442c967764263f"></a>

### OidVectorOid

The <code>OIDVECTOROID</code> constant (<code>30</code>).

```csharp
OidVectorOid = 30
```

<code>OIDVECTOROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7b8587203efd96ca"></a>

### OperatorClassRelationId

The <code>OperatorClassRelationId</code> constant (<code>2616</code>).

```csharp
OperatorClassRelationId = 2616
```

<code>OperatorClassRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-00c4a5eb1be48a24"></a>

### OperatorFamilyRelationId

The <code>OperatorFamilyRelationId</code> constant (<code>2753</code>).

```csharp
OperatorFamilyRelationId = 2753
```

<code>OperatorFamilyRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f8c0a448fdb93bc8"></a>

### OperatorRelationId

The <code>OperatorRelationId</code> constant (<code>2617</code>).

```csharp
OperatorRelationId = 2617
```

<code>OperatorRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-590d7d73f7d31e6f"></a>

### PathArrayOid

The <code>PATHARRAYOID</code> constant (<code>1019</code>).

```csharp
PathArrayOid = 1019
```

<code>PATHARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3b23dbeb39c3e858"></a>

### PathOid

The <code>PATHOID</code> constant (<code>602</code>).

```csharp
PathOid = 602
```

<code>PATHOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0d8174b9c47c5507"></a>

### PgAttributeArrayOid

The <code>PG_ATTRIBUTEARRAYOID</code> constant (<code>270</code>).

```csharp
PgAttributeArrayOid = 270
```

<code>PG_ATTRIBUTEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-388b20e6ad611a80"></a>

### PgBrinBloomSummaryOid

The <code>PG_BRIN_BLOOM_SUMMARYOID</code> constant (<code>4600</code>).

```csharp
PgBrinBloomSummaryOid = 4600
```

<code>PG_BRIN_BLOOM_SUMMARYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-dc05602d1d4490ca"></a>

### PgBrinMinMaxMultiSummaryOid

The <code>PG_BRIN_MINMAX_MULTI_SUMMARYOID</code> constant (<code>4601</code>).

```csharp
PgBrinMinMaxMultiSummaryOid = 4601
```

<code>PG_BRIN_MINMAX_MULTI_SUMMARYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-b008738c93ad6935"></a>

### PgClassArrayOid

The <code>PG_CLASSARRAYOID</code> constant (<code>273</code>).

```csharp
PgClassArrayOid = 273
```

<code>PG_CLASSARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-03333a9f1574fba1"></a>

### PgDdlCommandOid

The <code>PG_DDL_COMMANDOID</code> constant (<code>32</code>).

```csharp
PgDdlCommandOid = 32
```

<code>PGDDLCOMMANDOID</code> in PostgreSQL 13.
<code>PG_DDL_COMMANDOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-3ccde25b006a367d"></a>

### PgDependenciesOid

The <code>PG_DEPENDENCIESOID</code> constant (<code>3402</code>).

```csharp
PgDependenciesOid = 3402
```

<code>PGDEPENDENCIESOID</code> in PostgreSQL 13.
<code>PG_DEPENDENCIESOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-e36de6756bdde6db"></a>

### PgLsnArrayOid

The <code>PG_LSNARRAYOID</code> constant (<code>3221</code>).

```csharp
PgLsnArrayOid = 3221
```

<code>PG_LSNARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-dd65da80b416a462"></a>

### PgLsnOid

The <code>PG_LSNOID</code> constant (<code>3220</code>).

```csharp
PgLsnOid = 3220
```

<code>LSNOID</code> in PostgreSQL 13.
<code>PG_LSNOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-aa3a7d29414422c0"></a>

### PgMcvListOid

The <code>PG_MCV_LISTOID</code> constant (<code>5017</code>).

```csharp
PgMcvListOid = 5017
```

<code>PGMCVLISTOID</code> in PostgreSQL 13.
<code>PG_MCV_LISTOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-1904840c4f45631c"></a>

### PgNDistinctOid

The <code>PG_NDISTINCTOID</code> constant (<code>3361</code>).

```csharp
PgNDistinctOid = 3361
```

<code>PGNDISTINCTOID</code> in PostgreSQL 13.
<code>PG_NDISTINCTOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-0453cee01693715f"></a>

### PgNodeTreeOid

The <code>PG_NODE_TREEOID</code> constant (<code>194</code>).

```csharp
PgNodeTreeOid = 194
```

<code>PGNODETREEOID</code> in PostgreSQL 13.
<code>PG_NODE_TREEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-2457027d9169acec"></a>

### PgProcArrayOid

The <code>PG_PROCARRAYOID</code> constant (<code>272</code>).

```csharp
PgProcArrayOid = 272
```

<code>PG_PROCARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-1260f5fcb16e31c4"></a>

### PgSnapshotArrayOid

The <code>PG_SNAPSHOTARRAYOID</code> constant (<code>5039</code>).

```csharp
PgSnapshotArrayOid = 5039
```

<code>PG_SNAPSHOTARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-dd3d376effb06f3b"></a>

### PgSnapshotOid

The <code>PG_SNAPSHOTOID</code> constant (<code>5038</code>).

```csharp
PgSnapshotOid = 5038
```

<code>PG_SNAPSHOTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3e7686a9193948a4"></a>

### PgTypeArrayOid

The <code>PG_TYPEARRAYOID</code> constant (<code>210</code>).

```csharp
PgTypeArrayOid = 210
```

<code>PG_TYPEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-f77a40be9238733b"></a>

### PointArrayOid

The <code>POINTARRAYOID</code> constant (<code>1017</code>).

```csharp
PointArrayOid = 1017
```

<code>POINTARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-10e2f0a2001d3fb2"></a>

### PointOid

The <code>POINTOID</code> constant (<code>600</code>).

```csharp
PointOid = 600
```

<code>POINTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9066b0b0f1791104"></a>

### PolygonArrayOid

The <code>POLYGONARRAYOID</code> constant (<code>1027</code>).

```csharp
PolygonArrayOid = 1027
```

<code>POLYGONARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7b01c686856bc920"></a>

### PolygonOid

The <code>POLYGONOID</code> constant (<code>604</code>).

```csharp
PolygonOid = 604
```

<code>POLYGONOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-70380c113eedf1ee"></a>

### PosixCollationOid

The <code>POSIX_COLLATION_OID</code> constant (<code>951</code>).

```csharp
PosixCollationOid = 951
```

<code>POSIX_COLLATION_OID</code> in PostgreSQL 13, 14, 15, 16, 17.

<a id="member-46a9be4bd687c1a8"></a>

### ProcedureRelationId

The <code>ProcedureRelationId</code> constant (<code>1255</code>).

```csharp
ProcedureRelationId = 1255
```

<code>ProcedureRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c413bf0a384b4107"></a>

### ProgressCreateidxAccessMethodOid

The <code>PROGRESS_CREATEIDX_ACCESS_METHOD_OID</code> constant (<code>8</code>).

```csharp
ProgressCreateidxAccessMethodOid = 8
```

<code>PROGRESS_CREATEIDX_ACCESS_METHOD_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-aa39616a8db91c99"></a>

### ProgressCreateidxIndexOid

The <code>PROGRESS_CREATEIDX_INDEX_OID</code> constant (<code>6</code>).

```csharp
ProgressCreateidxIndexOid = 6
```

<code>PROGRESS_CREATEIDX_INDEX_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-89c4d9e0ac330530"></a>

### PublicationRelationId

The <code>PublicationRelationId</code> constant (<code>6104</code>).

```csharp
PublicationRelationId = 6104
```

<code>PublicationRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0e1fe1d7c86f6339"></a>

### RecordArrayOid

The <code>RECORDARRAYOID</code> constant (<code>2287</code>).

```csharp
RecordArrayOid = 2287
```

<code>RECORDARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-63ab74f9225bc01e"></a>

### RecordOid

The <code>RECORDOID</code> constant (<code>2249</code>).

```csharp
RecordOid = 2249
```

<code>RECORDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9f4b8002fa89c02b"></a>

### RefCursorArrayOid

The <code>REFCURSORARRAYOID</code> constant (<code>2201</code>).

```csharp
RefCursorArrayOid = 2201
```

<code>REFCURSORARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-d362e38defa85771"></a>

### RefCursorOid

The <code>REFCURSOROID</code> constant (<code>1790</code>).

```csharp
RefCursorOid = 1790
```

<code>REFCURSOROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9e7b76362f1b0837"></a>

### RegClassArrayOid

The <code>REGCLASSARRAYOID</code> constant (<code>2210</code>).

```csharp
RegClassArrayOid = 2210
```

<code>REGCLASSARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ec2e1396ef42c46e"></a>

### RegClassOid

The <code>REGCLASSOID</code> constant (<code>2205</code>).

```csharp
RegClassOid = 2205
```

<code>REGCLASSOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c39d9d2d0e7c8298"></a>

### RegCollationArrayOid

The <code>REGCOLLATIONARRAYOID</code> constant (<code>4192</code>).

```csharp
RegCollationArrayOid = 4192
```

<code>REGCOLLATIONARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c46fde027c5ee35d"></a>

### RegCollationOid

The <code>REGCOLLATIONOID</code> constant (<code>4191</code>).

```csharp
RegCollationOid = 4191
```

<code>REGCOLLATIONOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e2d4a7fe4594a8fc"></a>

### RegConfigArrayOid

The <code>REGCONFIGARRAYOID</code> constant (<code>3735</code>).

```csharp
RegConfigArrayOid = 3735
```

<code>REGCONFIGARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f907afd603cebf7c"></a>

### RegConfigOid

The <code>REGCONFIGOID</code> constant (<code>3734</code>).

```csharp
RegConfigOid = 3734
```

<code>REGCONFIGOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c1f19415f0d4bcec"></a>

### RegDatabaseArrayOid

The <code>REGDATABASEARRAYOID</code> constant (<code>6491</code>).

```csharp
RegDatabaseArrayOid = 6491
```

<code>REGDATABASEARRAYOID</code> in PostgreSQL 19.

<a id="member-08128dfc0e806f6f"></a>

### RegDatabaseOid

The <code>REGDATABASEOID</code> constant (<code>6490</code>).

```csharp
RegDatabaseOid = 6490
```

<code>REGDATABASEOID</code> in PostgreSQL 19.

<a id="member-55536c55f6aab134"></a>

### RegDictionaryArrayOid

The <code>REGDICTIONARYARRAYOID</code> constant (<code>3770</code>).

```csharp
RegDictionaryArrayOid = 3770
```

<code>REGDICTIONARYARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-69ecbe4562bac5f0"></a>

### RegDictionaryOid

The <code>REGDICTIONARYOID</code> constant (<code>3769</code>).

```csharp
RegDictionaryOid = 3769
```

<code>REGDICTIONARYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-9a314a5490e65098"></a>

### RegNamespaceArrayOid

The <code>REGNAMESPACEARRAYOID</code> constant (<code>4090</code>).

```csharp
RegNamespaceArrayOid = 4090
```

<code>REGNAMESPACEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f5172186560721de"></a>

### RegNamespaceOid

The <code>REGNAMESPACEOID</code> constant (<code>4089</code>).

```csharp
RegNamespaceOid = 4089
```

<code>REGNAMESPACEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e73d353fbbac7317"></a>

### RegOperArrayOid

The <code>REGOPERARRAYOID</code> constant (<code>2208</code>).

```csharp
RegOperArrayOid = 2208
```

<code>REGOPERARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c8c7cedba1eb416b"></a>

### RegOperOid

The <code>REGOPEROID</code> constant (<code>2203</code>).

```csharp
RegOperOid = 2203
```

<code>REGOPEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-eab125ca10e11396"></a>

### RegOperatorArrayOid

The <code>REGOPERATORARRAYOID</code> constant (<code>2209</code>).

```csharp
RegOperatorArrayOid = 2209
```

<code>REGOPERATORARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-4372bbe8192bf135"></a>

### RegOperatorOid

The <code>REGOPERATOROID</code> constant (<code>2204</code>).

```csharp
RegOperatorOid = 2204
```

<code>REGOPERATOROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c129ff67634176c5"></a>

### RegProcArrayOid

The <code>REGPROCARRAYOID</code> constant (<code>1008</code>).

```csharp
RegProcArrayOid = 1008
```

<code>REGPROCARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-98a9e6f1d4b2cf36"></a>

### RegProcOid

The <code>REGPROCOID</code> constant (<code>24</code>).

```csharp
RegProcOid = 24
```

<code>REGPROCOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1bd5dd4f06ab7b8e"></a>

### RegProcedureArrayOid

The <code>REGPROCEDUREARRAYOID</code> constant (<code>2207</code>).

```csharp
RegProcedureArrayOid = 2207
```

<code>REGPROCEDUREARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-4f0d9672e882fd05"></a>

### RegProcedureOid

The <code>REGPROCEDUREOID</code> constant (<code>2202</code>).

```csharp
RegProcedureOid = 2202
```

<code>REGPROCEDUREOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-84f169c0c0a65868"></a>

### RegRoleArrayOid

The <code>REGROLEARRAYOID</code> constant (<code>4097</code>).

```csharp
RegRoleArrayOid = 4097
```

<code>REGROLEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-6fe7b38e5f5169a5"></a>

### RegRoleOid

The <code>REGROLEOID</code> constant (<code>4096</code>).

```csharp
RegRoleOid = 4096
```

<code>REGROLEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ee399d5fe7d55cbf"></a>

### RegTypeArrayOid

The <code>REGTYPEARRAYOID</code> constant (<code>2211</code>).

```csharp
RegTypeArrayOid = 2211
```

<code>REGTYPEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-fb40d0ab9572ba0b"></a>

### RegTypeOid

The <code>REGTYPEOID</code> constant (<code>2206</code>).

```csharp
RegTypeOid = 2206
```

<code>REGTYPEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7c3061f714e3a520"></a>

### RelationRelationId

The <code>RelationRelationId</code> constant (<code>1259</code>).

```csharp
RelationRelationId = 1259
```

<code>RelationRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-dddd80c24cc21b41"></a>

### ReplicationOriginRelationId

The <code>ReplicationOriginRelationId</code> constant (<code>6000</code>).

```csharp
ReplicationOriginRelationId = 6000
```

<code>ReplicationOriginRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-518b5848362a6bd8"></a>

### SecLabelRelationId

The <code>SecLabelRelationId</code> constant (<code>3596</code>).

```csharp
SecLabelRelationId = 3596
```

<code>SecLabelRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bba2e69fa2d2d523"></a>

### SpGistAmOid

The <code>SPGIST_AM_OID</code> constant (<code>4000</code>).

```csharp
SpGistAmOid = 4000
```

<code>SPGIST_AM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b693dd1455cdc951"></a>

### StatisticExtRelationId

The <code>StatisticExtRelationId</code> constant (<code>3381</code>).

```csharp
StatisticExtRelationId = 3381
```

<code>StatisticExtRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e3c758e94ecceaf8"></a>

### StatisticRelationId

The <code>StatisticRelationId</code> constant (<code>2619</code>).

```csharp
StatisticRelationId = 2619
```

<code>StatisticRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b9a249648dee09f8"></a>

### TableAmHandlerOid

The <code>TABLE_AM_HANDLEROID</code> constant (<code>269</code>).

```csharp
TableAmHandlerOid = 269
```

<code>TABLE_AM_HANDLEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-847884718752861e"></a>

### TableSpaceRelationId

The <code>TableSpaceRelationId</code> constant (<code>1213</code>).

```csharp
TableSpaceRelationId = 1213
```

<code>TableSpaceRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8d34dcfd2787577e"></a>

### TemplateDbOid

The <code>TemplateDbOid</code> constant (<code>1</code>).

```csharp
TemplateDbOid = 1
```

<code>TemplateDbOid</code> in PostgreSQL 13, 14.

<a id="member-7eab32d199e55ad6"></a>

### TextArrayOid

The <code>TEXTARRAYOID</code> constant (<code>1009</code>).

```csharp
TextArrayOid = 1009
```

<code>TEXTARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-409e8224b4657f5e"></a>

### TextBTreeFamOid

The <code>TEXT_BTREE_FAM_OID</code> constant (<code>1994</code>).

```csharp
TextBTreeFamOid = 1994
```

<code>TEXT_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3675274175fe057b"></a>

### TextBTreeOpsOid

The <code>TEXT_BTREE_OPS_OID</code> constant (<code>3126</code>).

```csharp
TextBTreeOpsOid = 3126
```

<code>TEXT_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-1e1566623e5fc4c5"></a>

### TextBTreePatternOpsOid

The <code>TEXT_BTREE_PATTERN_OPS_OID</code> constant (<code>4217</code>).

```csharp
TextBTreePatternOpsOid = 4217
```

<code>TEXT_BTREE_PATTERN_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-69320db11b89a38d"></a>

### TextOid

The <code>TEXTOID</code> constant (<code>25</code>).

```csharp
TextOid = 25
```

<code>TEXTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-70155fc06f2393f2"></a>

### TextPatternBTreeFamOid

The <code>TEXT_PATTERN_BTREE_FAM_OID</code> constant (<code>2095</code>).

```csharp
TextPatternBTreeFamOid = 2095
```

<code>TEXT_PATTERN_BTREE_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-839dca007e5b0bf0"></a>

### TextSpGistFamOid

The <code>TEXT_SPGIST_FAM_OID</code> constant (<code>4017</code>).

```csharp
TextSpGistFamOid = 4017
```

<code>TEXT_SPGIST_FAM_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-82eaa614b74cacd0"></a>

### TidArrayOid

The <code>TIDARRAYOID</code> constant (<code>1010</code>).

```csharp
TidArrayOid = 1010
```

<code>TIDARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8db82b0a57884efd"></a>

### TidOid

The <code>TIDOID</code> constant (<code>27</code>).

```csharp
TidOid = 27
```

<code>TIDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b550e7987eb08a8b"></a>

### TimeArrayOid

The <code>TIMEARRAYOID</code> constant (<code>1183</code>).

```csharp
TimeArrayOid = 1183
```

<code>TIMEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-577ab686a40fdb15"></a>

### TimeOid

The <code>TIMEOID</code> constant (<code>1083</code>).

```csharp
TimeOid = 1083
```

<code>TIMEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-4929c944e1144c9a"></a>

### TimestampArrayOid

The <code>TIMESTAMPARRAYOID</code> constant (<code>1115</code>).

```csharp
TimestampArrayOid = 1115
```

<code>TIMESTAMPARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0c590ca0cabadf20"></a>

### TimestampBTreeOpsOid

The <code>TIMESTAMP_BTREE_OPS_OID</code> constant (<code>3128</code>).

```csharp
TimestampBTreeOpsOid = 3128
```

<code>TIMESTAMP_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b2f671ea2876e2ad"></a>

### TimestampOid

The <code>TIMESTAMPOID</code> constant (<code>1114</code>).

```csharp
TimestampOid = 1114
```

<code>TIMESTAMPOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0f8453a7992cf684"></a>

### TimestamptzArrayOid

The <code>TIMESTAMPTZARRAYOID</code> constant (<code>1185</code>).

```csharp
TimestamptzArrayOid = 1185
```

<code>TIMESTAMPTZARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-227678558bb248a3"></a>

### TimestamptzBTreeOpsOid

The <code>TIMESTAMPTZ_BTREE_OPS_OID</code> constant (<code>3127</code>).

```csharp
TimestamptzBTreeOpsOid = 3127
```

<code>TIMESTAMPTZ_BTREE_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3d57f0900088743a"></a>

### TimestamptzOid

The <code>TIMESTAMPTZOID</code> constant (<code>1184</code>).

```csharp
TimestamptzOid = 1184
```

<code>TIMESTAMPTZOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bc0539aade38f82e"></a>

### TimetzArrayOid

The <code>TIMETZARRAYOID</code> constant (<code>1270</code>).

```csharp
TimetzArrayOid = 1270
```

<code>TIMETZARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7f8c563014cec813"></a>

### TimetzOid

The <code>TIMETZOID</code> constant (<code>1266</code>).

```csharp
TimetzOid = 1266
```

<code>TIMETZOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-2205bdc5e7c536d7"></a>

### TriggerOid

The <code>TRIGGEROID</code> constant (<code>2279</code>).

```csharp
TriggerOid = 2279
```

<code>TRIGGEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-e22472f55ac392f6"></a>

### TriggerRelationId

The <code>TriggerRelationId</code> constant (<code>2620</code>).

```csharp
TriggerRelationId = 2620
```

<code>TriggerRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-4434e0951f54430e"></a>

### TsMultirangeArrayOid

The <code>TSMULTIRANGEARRAYOID</code> constant (<code>6152</code>).

```csharp
TsMultirangeArrayOid = 6152
```

<code>TSMULTIRANGEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-b579f4de8d2a7ffe"></a>

### TsMultirangeOid

The <code>TSMULTIRANGEOID</code> constant (<code>4533</code>).

```csharp
TsMultirangeOid = 4533
```

<code>TSMULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-693021575644ede1"></a>

### TsQueryArrayOid

The <code>TSQUERYARRAYOID</code> constant (<code>3645</code>).

```csharp
TsQueryArrayOid = 3645
```

<code>TSQUERYARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ca92cdc58663f3ae"></a>

### TsQueryOid

The <code>TSQUERYOID</code> constant (<code>3615</code>).

```csharp
TsQueryOid = 3615
```

<code>TSQUERYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-0a9e34bdbfe9484e"></a>

### TsRangeArrayOid

The <code>TSRANGEARRAYOID</code> constant (<code>3909</code>).

```csharp
TsRangeArrayOid = 3909
```

<code>TSRANGEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-7e65862f174fb1f0"></a>

### TsRangeOid

The <code>TSRANGEOID</code> constant (<code>3908</code>).

```csharp
TsRangeOid = 3908
```

<code>TSRANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-8fd8e5fc4a71d53c"></a>

### TsVectorArrayOid

The <code>TSVECTORARRAYOID</code> constant (<code>3643</code>).

```csharp
TsVectorArrayOid = 3643
```

<code>TSVECTORARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-067620ac455f3606"></a>

### TsVectorOid

The <code>TSVECTOROID</code> constant (<code>3614</code>).

```csharp
TsVectorOid = 3614
```

<code>TSVECTOROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-bf6a87c1eca4f73f"></a>

### TsmHandlerOid

The <code>TSM_HANDLEROID</code> constant (<code>3310</code>).

```csharp
TsmHandlerOid = 3310
```

<code>TSM_HANDLEROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-3293df4c3cf53375"></a>

### TstzMultirangeArrayOid

The <code>TSTZMULTIRANGEARRAYOID</code> constant (<code>6153</code>).

```csharp
TstzMultirangeArrayOid = 6153
```

<code>TSTZMULTIRANGEARRAYOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-378c16e73bc3b4a6"></a>

### TstzMultirangeOid

The <code>TSTZMULTIRANGEOID</code> constant (<code>4534</code>).

```csharp
TstzMultirangeOid = 4534
```

<code>TSTZMULTIRANGEOID</code> in PostgreSQL 14, 15, 16, 17, 18, 19.

<a id="member-0c2fc558d11a3725"></a>

### TstzRangeArrayOid

The <code>TSTZRANGEARRAYOID</code> constant (<code>3911</code>).

```csharp
TstzRangeArrayOid = 3911
```

<code>TSTZRANGEARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f16c94be88abc4aa"></a>

### TstzRangeOid

The <code>TSTZRANGEOID</code> constant (<code>3910</code>).

```csharp
TstzRangeOid = 3910
```

<code>TSTZRANGEOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-74967f5b9f201b28"></a>

### TxidSnapshotArrayOid

The <code>TXID_SNAPSHOTARRAYOID</code> constant (<code>2949</code>).

```csharp
TxidSnapshotArrayOid = 2949
```

<code>TXID_SNAPSHOTARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-06cde0e9605d1a8d"></a>

### TxidSnapshotOid

The <code>TXID_SNAPSHOTOID</code> constant (<code>2970</code>).

```csharp
TxidSnapshotOid = 2970
```

<code>TXID_SNAPSHOTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-b9ba83563f1aec8e"></a>

### TypeRelationId

The <code>TypeRelationId</code> constant (<code>1247</code>).

```csharp
TypeRelationId = 1247
```

<code>TypeRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f1ee5ba311b885f8"></a>

### UnknownOid

The <code>UNKNOWNOID</code> constant (<code>705</code>).

```csharp
UnknownOid = 705
```

<code>UNKNOWNOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-c2a1494d55d51fb7"></a>

### UserMappingRelationId

The <code>UserMappingRelationId</code> constant (<code>1418</code>).

```csharp
UserMappingRelationId = 1418
```

<code>UserMappingRelationId</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-44ac9e549195aa04"></a>

### UuidArrayOid

The <code>UUIDARRAYOID</code> constant (<code>2951</code>).

```csharp
UuidArrayOid = 2951
```

<code>UUIDARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-69d46bf662ffb1da"></a>

### UuidOid

The <code>UUIDOID</code> constant (<code>2950</code>).

```csharp
UuidOid = 2950
```

<code>UUIDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-f0d2051296aa1a50"></a>

### VarbitArrayOid

The <code>VARBITARRAYOID</code> constant (<code>1563</code>).

```csharp
VarbitArrayOid = 1563
```

<code>VARBITARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-5ef0b13c82d69171"></a>

### VarbitOid

The <code>VARBITOID</code> constant (<code>1562</code>).

```csharp
VarbitOid = 1562
```

<code>VARBITOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-dbd607cf841b367a"></a>

### VarcharArrayOid

The <code>VARCHARARRAYOID</code> constant (<code>1015</code>).

```csharp
VarcharArrayOid = 1015
```

<code>VARCHARARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-65ce283aec868d78"></a>

### VarcharBTreePatternOpsOid

The <code>VARCHAR_BTREE_PATTERN_OPS_OID</code> constant (<code>4218</code>).

```csharp
VarcharBTreePatternOpsOid = 4218
```

<code>VARCHAR_BTREE_PATTERN_OPS_OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-549eee07f65f49b8"></a>

### VarcharOid

The <code>VARCHAROID</code> constant (<code>1043</code>).

```csharp
VarcharOid = 1043
```

<code>VARCHAROID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-a7be991f9816b38d"></a>

### VoidOid

The <code>VOIDOID</code> constant (<code>2278</code>).

```csharp
VoidOid = 2278
```

<code>VOIDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-45833648a4110385"></a>

### Xid8ArrayOid

The <code>XID8ARRAYOID</code> constant (<code>271</code>).

```csharp
Xid8ArrayOid = 271
```

<code>XID8ARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-317b41c55b7efb41"></a>

### Xid8Oid

The <code>XID8OID</code> constant (<code>5069</code>).

```csharp
Xid8Oid = 5069
```

<code>XID8OID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-af110e6cde60832c"></a>

### XidArrayOid

The <code>XIDARRAYOID</code> constant (<code>1011</code>).

```csharp
XidArrayOid = 1011
```

<code>XIDARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-5ee17067a5540840"></a>

### XidOid

The <code>XIDOID</code> constant (<code>28</code>).

```csharp
XidOid = 28
```

<code>XIDOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-14f86f1b8d6e4551"></a>

### XlogNextOid

The <code>XLOG_NEXTOID</code> constant (<code>48</code>).

```csharp
XlogNextOid = 48
```

<code>XLOG_NEXTOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-fb7a61b2bb8b68f1"></a>

### XmlArrayOid

The <code>XMLARRAYOID</code> constant (<code>143</code>).

```csharp
XmlArrayOid = 143
```

<code>XMLARRAYOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.

<a id="member-ac2f5c8f8b6b577d"></a>

### XmlOid

The <code>XMLOID</code> constant (<code>142</code>).

```csharp
XmlOid = 142
```

<code>XMLOID</code> in PostgreSQL 13, 14, 15, 16, 17, 18, 19.
