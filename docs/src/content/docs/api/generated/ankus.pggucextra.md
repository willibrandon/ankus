---
title: 'PgGucExtra'
description: 'API reference for Ankus.PgGucExtra.'
slug: api/ankus.pggucextra
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns immutable bytes prepared by a configuration check hook and supplied to assignment and display hooks.
Retained instances remain valid after PostgreSQL changes or restores the setting.

```csharp
public sealed class PgGucExtra
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Constructors

<a id="member-1a4f2e06fcbeafea"></a>

### PgGucExtra(ReadOnlySpan&lt;byte&gt;)

Owns immutable bytes prepared by a configuration check hook and supplied to assignment and display hooks.
Retained instances remain valid after PostgreSQL changes or restores the setting.

```csharp
public PgGucExtra(ReadOnlySpan<byte> bytes)
```

Parameters:

`bytes` — [ReadOnlySpan&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The bytes to copy into managed ownership.


## Methods

<a id="member-61291bf984f9569e"></a>

### AsSpan()

Views the owned bytes without permitting mutation.

```csharp
public ReadOnlySpan<byte> AsSpan()
```

Returns: [ReadOnlySpan&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

A read-only view of this instance's bytes.

<a id="member-798a3938c694a160"></a>

### ToArray()

Copies the owned bytes to a separately mutable array.

```csharp
public byte[] ToArray()
```

Returns: [byte[]](https://learn.microsoft.com/dotnet/api/system.byte)

An independent copy.


## Properties

<a id="member-ee95a22bf1df0f7a"></a>

### this[int]

Gets the byte at a zero-based position.

```csharp
public byte this[int index] { get; }
```

Parameters:

`index` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based byte position.

Value: [byte](https://learn.microsoft.com/dotnet/api/system.byte)

The stored byte.

<a id="member-44ef06bd5109275e"></a>

### Length

Gets the number of owned bytes. An empty instance remains distinct from absent extra data.

```csharp
public int Length { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)
