---
title: 'PgCircle'
description: 'API reference for Ankus.PgCircle.'
slug: api/ankus.pgcircle
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

A PostgreSQL circle with double-precision center and radius. PostgreSQL validates the radius on datum conversion.
Equality compares the stored center and radius, not area or geometric tolerance.

```csharp
public readonly struct PgCircle : IEquatable<PgCircle>
```

Implements: [IEquatable&lt;PgCircle&gt;](https://learn.microsoft.com/dotnet/api/system.iequatable-1)

## Constructors

<a id="member-f141348695363606"></a>

### PgCircle(PgPoint, double)

A PostgreSQL circle with double-precision center and radius. PostgreSQL validates the radius on datum conversion.
Equality compares the stored center and radius, not area or geometric tolerance.

```csharp
public PgCircle(PgPoint Center, double Radius)
```

Parameters:

`Center` — [PgPoint](/api/ankus.pgpoint/)

The center point.

`Radius` — [double](https://learn.microsoft.com/dotnet/api/system.double)

The radius. Negative radii cannot be stored in PostgreSQL.


## Properties

<a id="member-20b61e35c3b8ba17"></a>

### Center

The center point.

```csharp
public PgPoint Center { get; init; }
```

Value: [PgPoint](/api/ankus.pgpoint/)

<a id="member-ba33da3aa0c44a25"></a>

### Radius

The radius. Negative radii cannot be stored in PostgreSQL.

```csharp
public double Radius { get; init; }
```

Value: [double](https://learn.microsoft.com/dotnet/api/system.double)


## Methods

<a id="member-8692a4f0696c4d8f"></a>

### Deconstruct(out PgPoint, out double)

```csharp
public void Deconstruct(out PgPoint Center, out double Radius)
```

Parameters:

`Center` — [PgPoint](/api/ankus.pgpoint/)

`Radius` — [double](https://learn.microsoft.com/dotnet/api/system.double)

<a id="member-8cd28d5cf54ab25a"></a>

### Equals(PgCircle)

```csharp
public bool Equals(PgCircle other)
```

Parameters:

`other` — [PgCircle](/api/ankus.pgcircle/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-0783ada265b54039"></a>

### Equals(object)

```csharp
public override bool Equals(object obj)
```

Parameters:

`obj` — [object](https://learn.microsoft.com/dotnet/api/system.object)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-700b82b0fa860fe0"></a>

### GetHashCode()

```csharp
public override int GetHashCode()
```

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-d36c00eeada0601d"></a>

### Parse(string)

Parses a circle using PostgreSQL on the active backend.

```csharp
public static PgCircle Parse(string text)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The circle text.

Returns: [PgCircle](/api/ankus.pgcircle/)

The parsed circle.

<a id="member-9804a3956aeed3d0"></a>

### ToString()

Formats the center and radius without backend access.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The circle text.

<a id="member-434ad9a51972f1f1"></a>

### TryParse(string?, out PgCircle)

Tries PostgreSQL parsing without swallowing backend-access or operational errors.

```csharp
public static bool TryParse(string? text, out PgCircle value)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The circle text.

`value` — [PgCircle](/api/ankus.pgcircle/)

The result, or default.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether parsing succeeded.


## Operators

<a id="member-77b606f0af8099cc"></a>

### operator ==(PgCircle, PgCircle)

```csharp
public static bool operator ==(PgCircle left, PgCircle right)
```

Parameters:

`left` — [PgCircle](/api/ankus.pgcircle/)

`right` — [PgCircle](/api/ankus.pgcircle/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-0fbec914439b526a"></a>

### operator !=(PgCircle, PgCircle)

```csharp
public static bool operator !=(PgCircle left, PgCircle right)
```

Parameters:

`left` — [PgCircle](/api/ankus.pgcircle/)

`right` — [PgCircle](/api/ankus.pgcircle/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)
