---
title: 'PgCidr'
description: 'API reference for Ankus.PgCidr.'
slug: api/ankus.pgcidr
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

An immutable PostgreSQL cidr network. Host bits must be zero; the default is 0.0.0.0/0.

```csharp
[JsonConverter(typeof(PgCidrConverter))]
public readonly struct PgCidr : IComparable<PgCidr>, IEquatable<PgCidr>
```

Implements: [IComparable&lt;PgCidr&gt;](https://learn.microsoft.com/dotnet/api/system.icomparable-1), [IEquatable&lt;PgCidr&gt;](https://learn.microsoft.com/dotnet/api/system.iequatable-1)

## Constructors

<a id="member-e37a5139ecc55348"></a>

### PgCidr(PgInet)

Creates a network from an inet value whose host bits are already zero.
Use PgInet.Network to explicitly clear host bits first.

```csharp
public PgCidr(PgInet value)
```

Parameters:

`value` — [PgInet](/api/ankus.pginet/)

The network value.

<a id="member-913879793c9a3186"></a>

### PgCidr(IPAddress, int)

Creates a network without silently discarding host bits.

```csharp
public PgCidr(IPAddress address, int prefixLength)
```

Parameters:

`address` — [IPAddress](https://learn.microsoft.com/dotnet/api/system.net.ipaddress)

The network address.

`prefixLength` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The network prefix length.

<a id="member-f1bbd5c3055534f0"></a>

### PgCidr(IPNetwork)

Copies a .NET IPNetwork, rejecting scoped IPv6 addresses.

```csharp
public PgCidr(IPNetwork network)
```

Parameters:

`network` — [IPNetwork](https://learn.microsoft.com/dotnet/api/system.net.ipnetwork)

The network to copy.


## Properties

<a id="member-48e0da101b86feb4"></a>

### Address

Gets a detached copy of the network address.

```csharp
public IPAddress Address { get; }
```

Value: [IPAddress](https://learn.microsoft.com/dotnet/api/system.net.ipaddress)

<a id="member-1691066e36f6404c"></a>

### Inet

Gets this network as an inet value without information loss.

```csharp
public PgInet Inet { get; }
```

Value: [PgInet](/api/ankus.pginet/)

<a id="member-32dfd1b668b59ab3"></a>

### PrefixLength

Gets the number of network bits.

```csharp
public int PrefixLength { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)


## Methods

<a id="member-cea3e7155c027584"></a>

### CompareTo(PgCidr)

Compares using PostgreSQL network ordering.

```csharp
public int CompareTo(PgCidr other)
```

Parameters:

`other` — [PgCidr](/api/ankus.pgcidr/)

The network to compare.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The comparison result.

<a id="member-2214ee6c6005778c"></a>

### Equals(PgCidr)

```csharp
public bool Equals(PgCidr other)
```

Parameters:

`other` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-a7b653bc94542a4f"></a>

### Equals(object)

```csharp
public override bool Equals(object obj)
```

Parameters:

`obj` — [object](https://learn.microsoft.com/dotnet/api/system.object)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-7d2ee24a1f574f16"></a>

### GetHashCode()

```csharp
public override int GetHashCode()
```

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-140602e93744cf30"></a>

### Parse(string)

Parses PostgreSQL cidr text on the active backend, enforcing zero host bits.

```csharp
public static PgCidr Parse(string text)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The network text.

Returns: [PgCidr](/api/ankus.pgcidr/)

The parsed network.

<a id="member-86a20098fe54d715"></a>

### ToIPNetwork()

Converts to a .NET IPNetwork without loss of address family or prefix.

```csharp
public IPNetwork ToIPNetwork()
```

Returns: [IPNetwork](https://learn.microsoft.com/dotnet/api/system.net.ipnetwork)

The equivalent .NET network.

<a id="member-08f43d62467877c0"></a>

### ToString()

Formats a detached network with an explicit prefix, without backend access.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The round-trippable network text.

<a id="member-a0e6501c766c86f4"></a>

### TryParse(string?, out PgCidr)

Tries backend parsing without swallowing backend-access or operational failures.

```csharp
public static bool TryParse(string? text, out PgCidr value)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The network text.

`value` — [PgCidr](/api/ankus.pgcidr/)

The parsed network, or default.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the text is valid.

<a id="member-3b31002952d51dd4"></a>

### WithPrefixLength(int)

Changes the prefix and clears any host bits exposed by the change, like set_masklen(cidr, integer).

```csharp
public PgCidr WithPrefixLength(int prefixLength)
```

Parameters:

`prefixLength` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The new prefix length.

Returns: [PgCidr](/api/ankus.pgcidr/)

The normalized network.


## Operators

<a id="member-ada083361c79aa6e"></a>

### operator ==(PgCidr, PgCidr)

```csharp
public static bool operator ==(PgCidr left, PgCidr right)
```

Parameters:

`left` — [PgCidr](/api/ankus.pgcidr/)

`right` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-0b4132423ab6baf9"></a>

### operator &gt;(PgCidr, PgCidr)

Compares two networks in reverse PostgreSQL order.

```csharp
public static bool operator >(PgCidr left, PgCidr right)
```

Parameters:

`left` — [PgCidr](/api/ankus.pgcidr/)

`right` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-d45a168dee1d21ff"></a>

### operator &gt;=(PgCidr, PgCidr)

Compares two networks in reverse PostgreSQL order, including equality.

```csharp
public static bool operator >=(PgCidr left, PgCidr right)
```

Parameters:

`left` — [PgCidr](/api/ankus.pgcidr/)

`right` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-de3aecf61baeffdc"></a>

### operator !=(PgCidr, PgCidr)

```csharp
public static bool operator !=(PgCidr left, PgCidr right)
```

Parameters:

`left` — [PgCidr](/api/ankus.pgcidr/)

`right` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-6ba862e7cd003316"></a>

### operator &lt;(PgCidr, PgCidr)

Compares two networks in PostgreSQL order.

```csharp
public static bool operator <(PgCidr left, PgCidr right)
```

Parameters:

`left` — [PgCidr](/api/ankus.pgcidr/)

`right` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-d00561e66a351bce"></a>

### operator &lt;=(PgCidr, PgCidr)

Compares two networks in PostgreSQL order, including equality.

```csharp
public static bool operator <=(PgCidr left, PgCidr right)
```

Parameters:

`left` — [PgCidr](/api/ankus.pgcidr/)

`right` — [PgCidr](/api/ankus.pgcidr/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)
