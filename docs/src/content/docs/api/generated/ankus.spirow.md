---
title: 'SpiRow'
description: 'API reference for Ankus.SpiRow.'
slug: api/ankus.spirow
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Contains a managed copy of one SPI result row, independent of PostgreSQL memory contexts.

```csharp
public sealed class SpiRow
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Properties

<a id="member-0d06865b736b1bc8"></a>

### Count

Gets the number of cells in this row.

```csharp
public int Count { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-e0445382f8c642bc"></a>

### this[int]

Gets a cell by its zero-based column ordinal. SQL NULL is represented by null.

```csharp
public object? this[int ordinal] { get; }
```

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based column ordinal.

Value: [object](https://learn.microsoft.com/dotnet/api/system.object)

The managed cell.

<a id="member-0428fbba62518a50"></a>

### this[string]

Gets a cell by its exact, case-sensitive column name. Duplicate names resolve to the first column.

```csharp
public object? this[string name] { get; }
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name.

Value: [object](https://learn.microsoft.com/dotnet/api/system.object)

The managed cell or null for SQL NULL.


## Methods

<a id="member-a24ae4f8cc7cf2fd"></a>

### GetOrdinal(string)

Resolves an exact, case-sensitive column name to its zero-based ordinal. Duplicate names resolve to the first column.

```csharp
public int GetOrdinal(string name)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based ordinal.

<a id="member-e7f0e7973547e3da"></a>

### GetTypeOid(int)

Gets a cell's PostgreSQL type OID, including its original domain OID until replaced by Set.

```csharp
public uint GetTypeOid(int ordinal)
```

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based column ordinal.

Returns: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The cell's current type OID.

<a id="member-7afecf5347fd4a42"></a>

### GetTypeOid(string)

Gets a cell's current PostgreSQL type OID by exact column name.

```csharp
public uint GetTypeOid(string name)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name.

Returns: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The cell's current type OID.

<a id="member-49f0ceb726601f2a"></a>

### Get&lt;T&gt;(int)

Gets a typed cell without implicit numeric or textual conversion. Temporal cells also accept exact .NET conversions.
SQL NULL is accepted for nullable value types and reference types.

```csharp
public T Get<T>(int ordinal)
```

Type parameters:

`T`

The expected managed type.

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based column ordinal.

Returns: <code>T</code>

The cell value.

<a id="member-0edcd84ef1800d94"></a>

### Get&lt;T&gt;(string)

Gets a typed cell by an exact, case-sensitive column name. Duplicate names resolve to the first column.

```csharp
public T Get<T>(string name)
```

Type parameters:

`T`

The expected managed type.

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name.

Returns: <code>T</code>

The cell value.

<a id="member-2c9b2706c25da6f3"></a>

### Set&lt;T&gt;(int, T)

Replaces a local cell and its type with a supported managed datum. The edit does not update PostgreSQL or result metadata.

```csharp
public void Set<T>(int ordinal, T value)
```

Type parameters:

`T`

The replacement type, including its type when the value is null.

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based column ordinal.

`value` — <code>T</code>

The replacement value.

<a id="member-67702d66196d3ab5"></a>

### Set&lt;T&gt;(string, T)

Replaces a local cell by exact name. The replacement's declared CLR type determines its PostgreSQL type OID.

```csharp
public void Set<T>(string name, T value)
```

Type parameters:

`T`

The replacement managed type.

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name; duplicate names resolve to the first column.

`value` — <code>T</code>

The replacement value.
