---
title: 'PgAnyElement'
description: 'API reference for Ankus.PgAnyElement.'
slug: api/ankus.pganyelement
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Represents a non-null PostgreSQL anyelement value with its resolved type and checked native lifetime.

```csharp
public sealed class PgAnyElement
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Constructors

<a id="member-9e333688dee67c62"></a>

### PgAnyElement(PgDatum)

Represents a non-null PostgreSQL anyelement value with its resolved type and checked native lifetime.

```csharp
public PgAnyElement(PgDatum datum)
```

Parameters:

`datum` — [PgDatum](/api/ankus.pgdatum/)

The present native value.


## Methods

<a id="member-a36df88cb3ffda22"></a>

### CopyTo(PgMemoryContext)

Copies the value into a different native owner.

```csharp
public PgAnyElement CopyTo(PgMemoryContext context)
```

Parameters:

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination memory context.

Returns: [PgAnyElement](/api/ankus.pganyelement/)

The independently owned value.

<a id="member-d759649b970888ea"></a>

### Read&lt;T&gt;()

Reads an independent managed value with exact type checking.

```csharp
public T Read<T>()
```

Type parameters:

`T`

The managed representation.

Returns: <code>T</code>

The copied value.


## Properties

<a id="member-75206b0674f2ff36"></a>

### Datum

Gets the checked native value and its actual PostgreSQL type, including domain identity.

```csharp
public PgDatum Datum { get; }
```

Value: [PgDatum](/api/ankus.pgdatum/)

<a id="member-97b14bf71cfcc44f"></a>

### TypeOid

Gets the resolved PostgreSQL type OID.

```csharp
public uint TypeOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)
