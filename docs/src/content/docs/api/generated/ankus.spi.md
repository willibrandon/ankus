---
title: 'Spi'
description: 'API reference for Ankus.Spi.'
slug: api/ankus.spi
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Executes SQL inside the current PostgreSQL backend through the Server Programming Interface.

```csharp
public static class Spi
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Methods

<a id="member-756e9298316619e1"></a>

### Connect(Action&lt;SpiSession&gt;)

Runs a synchronous callback using one scoped SPI connection. Session-bound plans expire when the callback exits.

```csharp
public static void Connect(Action<SpiSession> action)
```

Parameters:

`action` — [Action&lt;SpiSession&gt;](https://learn.microsoft.com/dotnet/api/system.action-1)

The synchronous backend-thread callback.

<a id="member-8f8486ec844daf92"></a>

### Connect&lt;TResult&gt;(Func&lt;SpiSession, TResult&gt;)

Runs a synchronous callback using one scoped SPI connection and returns its managed result.

```csharp
public static TResult Connect<TResult>(Func<SpiSession, TResult> action)
```

Type parameters:

`TResult`

The callback result type.

Parameters:

`action` — [Func&lt;SpiSession, TResult&gt;](https://learn.microsoft.com/dotnet/api/system.func-2)

The synchronous backend-thread callback. It must not start asynchronous session operations.

Returns: <code>TResult</code>

The callback result, whose owned rows and retained plans can outlive the session.

<a id="member-6d758069d706ff12"></a>

### Execute(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Executes SQL and returns the rows processed by its final statement.
The call uses an internal subtransaction: success retains changes in the enclosing transaction;
failure rolls back this call's changes before throwing a managed PostgreSQL error.

```csharp
public static long Execute(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL commands to execute.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Typed positional parameters referenced as $1, $2, and so on.

Returns: [long](https://learn.microsoft.com/dotnet/api/system.int64)

The number of rows processed by the final statement.

Exceptions:

- [PgException](/api/ankus.pgexception/): PostgreSQL rejected the command.
- [InvalidOperationException](https://learn.microsoft.com/dotnet/api/system.invalidoperationexception): The caller is not on the active PostgreSQL backend thread.

<a id="member-082acc51b3e26077"></a>

### ExecuteScalar&lt;T&gt;(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Reads the first column of the first result row without implicit type conversion.
SQL NULL and an empty result return null for nullable or reference types and reject non-nullable value types.

```csharp
public static T ExecuteScalar<T>(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Type parameters:

`T`

The expected managed result type.

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL command.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Typed positional parameters.

Returns: <code>T</code>

The scalar result.

<a id="member-07d8cac35378d595"></a>

### ExecuteScalars&lt;TFirst, TSecond&gt;(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Reads the first two columns of the first row without limiting command execution.
SQL NULL or an empty result requires nullable value types or reference types.

```csharp
public static (TFirst First, TSecond Second) ExecuteScalars<TFirst, TSecond>(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Type parameters:

`TFirst`

The first column's managed type.

`TSecond`

The second column's managed type.

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL commands to execute.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Typed positional parameters.

Returns: <code>(TFirst First, TSecond Second)</code>

The first two values from the final statement, without implicit type conversions.

Exceptions:

- [InvalidOperationException](https://learn.microsoft.com/dotnet/api/system.invalidoperationexception): A returned row has fewer than two columns, or SQL NULL is read as a non-nullable value type.
- [InvalidCastException](https://learn.microsoft.com/dotnet/api/system.invalidcastexception): A column cannot be read as its requested managed type.

<a id="member-68cf41fb44187d3d"></a>

### ExecuteScalars&lt;TFirst, TSecond, TThird&gt;(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Reads the first three columns of the first row without limiting command execution.
SQL NULL or an empty result requires nullable value types or reference types.

```csharp
public static (TFirst First, TSecond Second, TThird Third) ExecuteScalars<TFirst, TSecond, TThird>(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Type parameters:

`TFirst`

The first column's managed type.

`TSecond`

The second column's managed type.

`TThird`

The third column's managed type.

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL commands to execute.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Typed positional parameters.

Returns: <code>(TFirst First, TSecond Second, TThird Third)</code>

The first three values from the final statement, without implicit type conversions.

Exceptions:

- [InvalidOperationException](https://learn.microsoft.com/dotnet/api/system.invalidoperationexception): A returned row has fewer than three columns, or SQL NULL is read as a non-nullable value type.
- [InvalidCastException](https://learn.microsoft.com/dotnet/api/system.invalidcastexception): A column cannot be read as its requested managed type.

<a id="member-7ad0a664880a0df2"></a>

### Explain(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Plans one SQL statement and returns PostgreSQL's JSON EXPLAIN output. The statement is not executed by EXPLAIN ANALYZE.

```csharp
public static PgJson Explain(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The statement to explain.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The typed positional parameters.

Returns: [PgJson](/api/ankus.pgjson/)

The owned JSON query plan.

<a id="member-bf9c60d5111dc21f"></a>

### FindCursor(string)

Takes ownership of an existing PostgreSQL cursor by its exact portal name.

```csharp
public static SpiCursor FindCursor(string name)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The portal name, including names returned by SpiCursor.Detach.

Returns: [SpiCursor](/api/ankus.spicursor/)

An owned cursor.

<a id="member-bc701e549317a59c"></a>

### OpenCursor(string, bool, params ReadOnlySpan&lt;SpiParameter&gt;)

Opens a cursor with explicit read-only SPI execution mode.

```csharp
public static SpiCursor OpenCursor(string commandText, bool readOnly, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

One SQL command that returns rows.

`readOnly` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether PostgreSQL should use read-only execution.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The positional parameter values.

Returns: [SpiCursor](/api/ankus.spicursor/)

An owned transaction-bound cursor.

<a id="member-bc7aff5005fe45df"></a>

### OpenCursor(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Opens a transaction-bound cursor with optional typed positional parameters.

```csharp
public static SpiCursor OpenCursor(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

One SQL command that returns rows.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The positional parameter values.

Returns: [SpiCursor](/api/ankus.spicursor/)

An owned cursor that should be disposed or detached.

<a id="member-09b24c05d9940e1e"></a>

### Prepare(string, params ReadOnlySpan&lt;Type&gt;)

Prepares a reusable statement with explicitly declared managed parameter types.
The statement survives SPI calls and transaction boundaries until disposed on the owning backend thread.

```csharp
public static SpiPreparedStatement Prepare(string commandText, params ReadOnlySpan<Type> parameterTypes)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL commands to prepare.

`parameterTypes` — [ReadOnlySpan&lt;Type&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The declared CLR types of $1, $2, and subsequent parameters.

Returns: [SpiPreparedStatement](/api/ankus.spipreparedstatement/)

A statement that must be disposed from an extension callback on its owning backend.

<a id="member-e1912af1861360e2"></a>

### PrepareWithTypeOids(string, params ReadOnlySpan&lt;uint&gt;)

Prepares a reusable statement with explicit PostgreSQL parameter type identities, including named composites and domains.

```csharp
public static SpiPreparedStatement PrepareWithTypeOids(string commandText, params ReadOnlySpan<uint> parameterTypeOids)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL commands to prepare.

`parameterTypeOids` — [ReadOnlySpan&lt;uint&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The nonzero catalog type OIDs for $1, $2, and subsequent parameters.

Returns: [SpiPreparedStatement](/api/ankus.spipreparedstatement/)

A statement that must be disposed on its owning backend.

<a id="member-c079e7d607415a5a"></a>

### Query(string, bool, int, params ReadOnlySpan&lt;SpiParameter&gt;)

Executes SQL with explicit snapshot mode and a row limit, copying result data into managed memory.

```csharp
public static SpiResult Query(string commandText, bool readOnly, int limit, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL command.

`readOnly` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether PostgreSQL should use read-only SPI execution.

`limit` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The maximum returned rows, or zero for no limit.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Typed positional parameters.

Returns: [SpiResult](/api/ankus.spiresult/)

The materialized result.

<a id="member-f7ce4f592439d027"></a>

### Query(string, params ReadOnlySpan&lt;SpiParameter&gt;)

Executes SQL and copies all result rows and metadata into managed objects.

```csharp
public static SpiResult Query(string commandText, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`commandText` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The SQL command.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Typed positional parameters.

Returns: [SpiResult](/api/ankus.spiresult/)

The materialized result.

<a id="member-63b75803eec5f3a2"></a>

### QuoteIdentifier(string)

Quotes a single SQL identifier using PostgreSQL's keyword rules and quote_all_identifiers setting.

```csharp
public static string QuoteIdentifier(string identifier)
```

Parameters:

`identifier` — [string](https://learn.microsoft.com/dotnet/api/system.string)

One identifier; dots are part of its name.

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The quoted identifier or the original spelling when quoting is unnecessary.

<a id="member-b06197fde1284699"></a>

### QuoteLiteral(string)

Quotes a text literal with escaping that is valid regardless of standard_conforming_strings.

```csharp
public static string QuoteLiteral(string value)
```

Parameters:

`value` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The literal text.

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The quoted SQL literal.

<a id="member-263487422e3e5f11"></a>

### QuoteQualifiedIdentifier(string?, string)

Quotes a schema or other qualifier and an identifier independently, joining them with a dot.

```csharp
public static string QuoteQualifiedIdentifier(string? qualifier, string identifier)
```

Parameters:

`qualifier` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The qualifier, or null to omit it.

`identifier` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The identifier.

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The qualified SQL name.
