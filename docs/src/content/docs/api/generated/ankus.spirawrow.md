---
title: 'SpiRawRow'
description: 'API reference for Ankus.SpiRawRow.'
slug: api/ankus.spirawrow
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Provides raw datums with explicit PostgreSQL types and NULL flags for one SPI row.

```csharp
public sealed class SpiRawRow : IReadOnlyList<PgDatum>, IReadOnlyCollection<PgDatum>, IEnumerable<PgDatum>, IEnumerable
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IReadOnlyList&lt;PgDatum&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1), [IReadOnlyCollection&lt;PgDatum&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlycollection-1), [IEnumerable&lt;PgDatum&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerable-1), [IEnumerable](https://learn.microsoft.com/dotnet/api/system.collections.ienumerable)

## Properties

<a id="member-455a0f68ab9cc62c"></a>

### Count

Gets the number of cells.

```csharp
public int Count { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-898912baa550ce95"></a>

### this[int]

Gets a raw value by its zero-based ordinal. SQL NULL has IsNull set on its datum.

```csharp
public PgDatum this[int index] { get; }
```

Parameters:

`index` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The column ordinal.

Value: [PgDatum](/api/ankus.pgdatum/)

The raw value.

<a id="member-596f70b46e40f948"></a>

### this[string]

Gets the first column with an exact case-sensitive name.

```csharp
public PgDatum this[string name] { get; }
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name.

Value: [PgDatum](/api/ankus.pgdatum/)

The raw value, including its SQL NULL flag.


## Methods

<a id="member-eb1560b3414b3da1"></a>

### GetEnumerator()

Enumerates datums in column order.

```csharp
public IEnumerator<PgDatum> GetEnumerator()
```

Returns: [IEnumerator&lt;PgDatum&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerator-1)

The value enumerator.

<a id="member-817b051aae059e17"></a>

### Get&lt;T&gt;(int)

Reads a column as a managed value or a polymorphic wrapper sharing this result's lifetime.

```csharp
public T Get<T>(int ordinal)
```

Type parameters:

`T`

The requested representation.

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based column ordinal.

Returns: <code>T</code>

The copied managed value or checked native wrapper.

<a id="member-1e368fc68baae45b"></a>

### Get&lt;T&gt;(string)

Reads the first column with an exact case-sensitive name.

```csharp
public T Get<T>(string name)
```

Type parameters:

`T`

The requested representation.

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The column name.

Returns: <code>T</code>

The copied managed value or a wrapper sharing this result's lifetime.
