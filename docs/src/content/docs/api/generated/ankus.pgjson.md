---
title: 'PgJson'
description: 'API reference for Ankus.PgJson.'
slug: api/ankus.pgjson
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns PostgreSQL json text, preserving whitespace, key order, duplicate keys, and numeric spelling.
The default value represents JSON null; use PgJson? to represent SQL NULL. Equality compares the text ordinally.

```csharp
[JsonConverter(typeof(PgJsonConverter))]
public readonly struct PgJson : IEquatable<PgJson>
```

Implements: [IEquatable&lt;PgJson&gt;](https://learn.microsoft.com/dotnet/api/system.iequatable-1)

## Constructors

<a id="member-6fef73f2df89a984"></a>

### PgJson(string)

Creates a JSON value after validating its syntax without converting numeric tokens to floating-point values.

```csharp
public PgJson(string text)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

One complete JSON value.


## Methods

<a id="member-b111a09b0b0433ff"></a>

### Deserialize&lt;T&gt;(JsonTypeInfo&lt;T&gt;)

Deserializes this value using explicit metadata suitable for Native AOT.

```csharp
public T? Deserialize<T>(JsonTypeInfo<T> typeInfo)
```

Type parameters:

`T`

The target managed type.

Parameters:

`typeInfo` — [JsonTypeInfo&lt;T&gt;](https://learn.microsoft.com/dotnet/api/system.text.json.serialization.metadata.jsontypeinfo-1)

Usually a source-generated serialization context's type metadata.

Returns: <code>T</code>

The deserialized value.

<a id="member-8caff6bd4e4b641f"></a>

### Equals(PgJson)

Compares the exact JSON text, without imposing PostgreSQL jsonb structural equality.

```csharp
public bool Equals(PgJson other)
```

Parameters:

`other` — [PgJson](/api/ankus.pgjson/)

The other value.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether both values contain the same text.

<a id="member-025f8ee2ea0a7f93"></a>

### Equals(object)

```csharp
public override bool Equals(object obj)
```

Parameters:

`obj` — [object](https://learn.microsoft.com/dotnet/api/system.object)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-d81415054a519d04"></a>

### GetHashCode()

Gets an ordinal text hash consistent with equality.

```csharp
public override int GetHashCode()
```

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The text hash.

<a id="member-4e6f58f8006e0353"></a>

### Parse()

Parses an independently owned document. The caller must dispose it after use.

```csharp
public JsonDocument Parse()
```

Returns: [JsonDocument](https://learn.microsoft.com/dotnet/api/system.text.json.jsondocument)

The owned JSON document.

<a id="member-4ecbca64ef3df5bc"></a>

### Serialize&lt;T&gt;(T, JsonTypeInfo&lt;T&gt;)

Serializes a managed value using explicit metadata suitable for Native AOT.

```csharp
public static PgJson Serialize<T>(T value, JsonTypeInfo<T> typeInfo)
```

Type parameters:

`T`

The managed type.

Parameters:

`value` — <code>T</code>

The value to serialize.

`typeInfo` — [JsonTypeInfo&lt;T&gt;](https://learn.microsoft.com/dotnet/api/system.text.json.serialization.metadata.jsontypeinfo-1)

The serialization metadata.

Returns: [PgJson](/api/ankus.pgjson/)

An owned JSON value.

<a id="member-91a29aac912f6cc0"></a>

### ToString()

Returns the original JSON text.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The JSON text.


## Properties

<a id="member-104f99f06a204581"></a>

### Text

Gets the original JSON text, or null's JSON spelling for the default value.

```csharp
public string Text { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)


## Operators

<a id="member-0284abe70f94582a"></a>

### operator ==(PgJson, PgJson)

```csharp
public static bool operator ==(PgJson left, PgJson right)
```

Parameters:

`left` — [PgJson](/api/ankus.pgjson/)

`right` — [PgJson](/api/ankus.pgjson/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-eeb3046c166c0ae4"></a>

### operator !=(PgJson, PgJson)

```csharp
public static bool operator !=(PgJson left, PgJson right)
```

Parameters:

`left` — [PgJson](/api/ankus.pgjson/)

`right` — [PgJson](/api/ankus.pgjson/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)
