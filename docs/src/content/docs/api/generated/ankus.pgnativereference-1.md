---
title: 'PgNativeReference<T>'
description: 'API reference for Ankus.PgNativeReference<T>.'
slug: api/ankus.pgnativereference-1
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Borrows a copied unmanaged view without acquiring allocation or release rights.

```csharp
public sealed class PgNativeReference<T> where T : unmanaged
```

Type parameters:

`T`

The complete unmanaged representation exposed by the view.

Checked allocation views observe resizing and share invalidation with their owner. Dangerous raw
views use a context as a lifetime anchor; that context is not inferred to own the referenced bytes.
The caller must still honor shorter external lifetimes such as stack return or resource closure.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Methods

<a id="member-27bfa2f719347c2a"></a>

### CloneInto(PgMemoryContext?)

Copies every representation byte, including padding and pointer fields, into independent context-owned storage.

```csharp
public PgContextValue<T> CloneInto(PgMemoryContext? context = null)
```

Parameters:

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination context, or null for the current context.

Returns: <code>PgContextValue&lt;T&gt;</code>

A shallow context-owned copy with default allocation policies and alignment.

<a id="member-4dde0f629d8cecf5"></a>

### CloneOwnedInto(PgMemoryContext?)

Copies every representation byte, including padding and pointer fields, into independent individually owned storage.

```csharp
public PgNativeBox<T> CloneOwnedInto(PgMemoryContext? context = null)
```

Parameters:

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination context, or null for the current context.

Returns: <code>PgNativeBox&lt;T&gt;</code>

A shallow individually owned copy with default allocation policies and alignment.


## Properties

<a id="member-5cc8279f17e33673"></a>

### LifetimeContext

Gets the checked allocation owner or the explicitly supplied raw-reference lifetime anchor.

```csharp
public PgMemoryContext LifetimeContext { get; }
```

Value: [PgMemoryContext](/api/ankus.pgmemorycontext/)

For a raw reference this is a lifetime anchor, not a claim about native allocator ownership.

<a id="member-ead6c7e3774c8253"></a>

### Value

Gets or sets a copied unmanaged value after checking its bounds and lifetime anchor.

```csharp
public T Value { get; set; }
```

Value: <code>T</code>
