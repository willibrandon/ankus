---
title: 'PgException'
description: 'API reference for Ankus.PgException.'
slug: api/ankus.pgexception
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Represents a PostgreSQL error raised by a guarded server call or deliberately reported by an extension.

```csharp
public sealed class PgException : Exception, ISerializable
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object), [Exception](https://learn.microsoft.com/dotnet/api/system.exception)

Implements: [ISerializable](https://learn.microsoft.com/dotnet/api/system.runtime.serialization.iserializable)

## Constructors

<a id="member-78a6cfbcc9c158a5"></a>

### PgException()

Creates an external-routine error with a default message.

```csharp
public PgException()
```

<a id="member-cbcbc36810e05017"></a>

### PgException(string)

Creates an external-routine error.

```csharp
public PgException(string message)
```

Parameters:

`message` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The primary error message.

<a id="member-4c2012eca9cd5c8e"></a>

### PgException(string, Exception?)

Creates an external-routine error with a managed cause.

```csharp
public PgException(string message, Exception? innerException)
```

Parameters:

`message` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The primary error message.

`innerException` — [Exception](https://learn.microsoft.com/dotnet/api/system.exception)

The original managed error.

<a id="member-bb367e385dc4a031"></a>

### PgException(string, string, string?, string?, Exception?)

Creates an error with explicit PostgreSQL diagnostics.

```csharp
public PgException(string sqlState, string message, string? detail = null, string? hint = null, Exception? innerException = null)
```

Parameters:

`sqlState` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The five-character uppercase SQLSTATE code.

`message` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The primary error message.

`detail` — [string](https://learn.microsoft.com/dotnet/api/system.string)

Additional error detail.

`hint` — [string](https://learn.microsoft.com/dotnet/api/system.string)

A suggested corrective action.

`innerException` — [Exception](https://learn.microsoft.com/dotnet/api/system.exception)

The original managed error, if any.


## Properties

<a id="member-12c838f7285c625a"></a>

### Backtrace

Gets the native backtrace when PostgreSQL collected one for this error.

```csharp
public string? Backtrace { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-44fbad9532f0a863"></a>

### ColumnName

Gets the column associated with the error.

```csharp
public string? ColumnName { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-20a3efdad1391030"></a>

### ConstraintName

Gets the constraint associated with the error.

```csharp
public string? ConstraintName { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-ce31cb348731a945"></a>

### Context

Gets the PostgreSQL execution context, including SQL and procedural call frames when supplied by the server.

```csharp
public string? Context { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-1dbe7399d28f2e41"></a>

### DataTypeName

Gets the data type associated with the error.

```csharp
public string? DataTypeName { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-1ea05045a6e09719"></a>

### Detail

Gets additional error detail.

```csharp
public string? Detail { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-b477cde09a0a2076"></a>

### DetailLog

Gets additional detail intended for the PostgreSQL server log rather than the client error response.

```csharp
public string? DetailLog { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-3eb2f2511663c96b"></a>

### DiagnosticsIncomplete

Gets whether allocating or encoding diagnostic transport failed, leaving only partially copied diagnostics.

```csharp
public bool DiagnosticsIncomplete { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-cbc226c23081a9fb"></a>

### File

Gets the source file that reported the error.

```csharp
public string? File { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-8eab79b0c364a864"></a>

### Hint

Gets a suggested corrective action.

```csharp
public string? Hint { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-f01951179b90aec0"></a>

### InternalPosition

Gets the one-based character position in InternalQuery, or zero when no internal position is supplied.

```csharp
public int InternalPosition { get; init; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-e80fb2c27351f775"></a>

### InternalQuery

Gets the internally executed query associated with InternalPosition.

```csharp
public string? InternalQuery { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-37e271d5557e6fbf"></a>

### Line

Gets the source line that reported the error, or zero when unavailable.

```csharp
public int Line { get; init; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-2c742db89754dd49"></a>

### Position

Gets the one-based character position in the client query, or zero when no position is supplied.

```csharp
public int Position { get; init; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-405d9ad8a6c68aed"></a>

### Routine

Gets the source routine that reported the error.

```csharp
public string? Routine { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-0e5e9530b00e0bef"></a>

### SchemaName

Gets the schema associated with the error.

```csharp
public string? SchemaName { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-41ca8d41036f0aa4"></a>

### SqlState

Gets the PostgreSQL SQLSTATE code.

```csharp
public string SqlState { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-431f55f771d356fd"></a>

### TableName

Gets the table associated with the error.

```csharp
public string? TableName { get; init; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)
