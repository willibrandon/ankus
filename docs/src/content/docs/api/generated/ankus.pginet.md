---
title: 'PgInet'
description: 'API reference for Ankus.PgInet.'
slug: api/ankus.pginet
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

An immutable PostgreSQL inet address and prefix, preserving IPv4 and IPv6 identity.
The default value is 0.0.0.0/0. Constructors and address operations work outside PostgreSQL.

```csharp
[JsonConverter(typeof(PgInetConverter))]
public readonly struct PgInet : IComparable<PgInet>, IEquatable<PgInet>
```

Implements: [IComparable&lt;PgInet&gt;](https://learn.microsoft.com/dotnet/api/system.icomparable-1), [IEquatable&lt;PgInet&gt;](https://learn.microsoft.com/dotnet/api/system.iequatable-1)

## Constructors

<a id="member-b75dbbadb06f0d12"></a>

### PgInet(IPAddress, int?)

Copies an IP address, using its full width when no prefix is supplied. Scoped IPv6 addresses are rejected.

```csharp
public PgInet(IPAddress address, int? prefixLength = null)
```

Parameters:

`address` — [IPAddress](https://learn.microsoft.com/dotnet/api/system.net.ipaddress)

The IPv4 or IPv6 host address.

`prefixLength` — [int?](https://learn.microsoft.com/dotnet/api/system.int32)

The prefix length, or null for a single host.


## Properties

<a id="member-d8514124f148d9b9"></a>

### Address

Gets a detached IPAddress copy of the host, without its network prefix.

```csharp
public IPAddress Address { get; }
```

Value: [IPAddress](https://learn.microsoft.com/dotnet/api/system.net.ipaddress)

<a id="member-8e299388f9ecd1e8"></a>

### AddressFamily

Gets the preserved IPv4 or IPv6 address family; mapped IPv6 remains IPv6.

```csharp
public AddressFamily AddressFamily { get; }
```

Value: [AddressFamily](https://learn.microsoft.com/dotnet/api/system.net.sockets.addressfamily)

<a id="member-22eb504a863de38d"></a>

### Broadcast

Gets the address with every host bit set, retaining its prefix.

```csharp
public PgInet Broadcast { get; }
```

Value: [PgInet](/api/ankus.pginet/)

<a id="member-563e6443f2e0303f"></a>

### Hostmask

Gets the full-width host mask address.

```csharp
public PgInet Hostmask { get; }
```

Value: [PgInet](/api/ankus.pginet/)

<a id="member-14e1cdb294a4fe64"></a>

### Netmask

Gets the full-width network mask address.

```csharp
public PgInet Netmask { get; }
```

Value: [PgInet](/api/ankus.pginet/)

<a id="member-d6420ba3e0984c70"></a>

### Network

Gets the network with host bits cleared, like PostgreSQL network(inet).

```csharp
public PgCidr Network { get; }
```

Value: [PgCidr](/api/ankus.pgcidr/)

<a id="member-c00d469066c67a5a"></a>

### PrefixLength

Gets the number of significant network bits.

```csharp
public int PrefixLength { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)


## Methods

<a id="member-6c2c462e630c5b3b"></a>

### CompareTo(PgInet)

Compares family, common network bits, prefix length, then host bits, matching PostgreSQL inet ordering.

```csharp
public int CompareTo(PgInet other)
```

Parameters:

`other` — [PgInet](/api/ankus.pginet/)

The value to compare.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

A negative, zero or positive comparison result.

<a id="member-1859427967251f79"></a>

### Contains(PgInet, bool)

Tests subnet containment, including equal prefixes by default, like PostgreSQL &gt;&gt;=.

```csharp
public bool Contains(PgInet other, bool includeEqual = true)
```

Parameters:

`other` — [PgInet](/api/ankus.pginet/)

The address and subnet to compare.

`includeEqual` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether equal prefix lengths count as contained.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the other subnet is contained within this one.

<a id="member-df070cdad972b654"></a>

### Equals(PgInet)

```csharp
public bool Equals(PgInet other)
```

Parameters:

`other` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-94b51d499ad7c46c"></a>

### Equals(object)

```csharp
public override bool Equals(object obj)
```

Parameters:

`obj` — [object](https://learn.microsoft.com/dotnet/api/system.object)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-1747ab6a98ab35cd"></a>

### GetHashCode()

```csharp
public override int GetHashCode()
```

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-ebb143642e486743"></a>

### Parse(string)

Parses PostgreSQL inet input on the active backend, including PostgreSQL's abbreviated IPv4 forms.

```csharp
public static PgInet Parse(string text)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The PostgreSQL address text.

Returns: [PgInet](/api/ankus.pginet/)

The parsed address and prefix.

<a id="member-ca6220f845ad65cb"></a>

### ToIPAddress()

Returns an IPAddress only when discarding the prefix would lose no network information.
Use Address to deliberately extract the host from a subnet value.

```csharp
public IPAddress ToIPAddress()
```

Returns: [IPAddress](https://learn.microsoft.com/dotnet/api/system.net.ipaddress)

The detached, full-width host address.

<a id="member-49a01cfc0d80e194"></a>

### ToString()

Formats a detached address, omitting a full-width prefix. No backend access is required.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

Round-trippable IPv4 or IPv6 text.

<a id="member-cb973c08f905f8dd"></a>

### TryParse(string?, out PgInet)

Tries PostgreSQL parsing; input errors return false while backend-access and operational errors propagate.

```csharp
public static bool TryParse(string? text, out PgInet value)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The address text.

`value` — [PgInet](/api/ankus.pginet/)

The parsed address, or default.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the input is valid.

<a id="member-48446c89a236aec8"></a>

### WithPrefixLength(int)

Changes the prefix without clearing host bits, like set_masklen(inet, integer).

```csharp
public PgInet WithPrefixLength(int prefixLength)
```

Parameters:

`prefixLength` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The new prefix length.

Returns: [PgInet](/api/ankus.pginet/)

The address with its new prefix.


## Operators

<a id="member-f2d57ead27ef5d30"></a>

### operator ==(PgInet, PgInet)

```csharp
public static bool operator ==(PgInet left, PgInet right)
```

Parameters:

`left` — [PgInet](/api/ankus.pginet/)

`right` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-2f6d701cb542af33"></a>

### operator &gt;(PgInet, PgInet)

Compares two inet values in reverse PostgreSQL order.

```csharp
public static bool operator >(PgInet left, PgInet right)
```

Parameters:

`left` — [PgInet](/api/ankus.pginet/)

`right` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-9bfa99e96111b5a3"></a>

### operator &gt;=(PgInet, PgInet)

Compares two inet values in reverse PostgreSQL order, including equality.

```csharp
public static bool operator >=(PgInet left, PgInet right)
```

Parameters:

`left` — [PgInet](/api/ankus.pginet/)

`right` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-bf6ebdddc3cec043"></a>

### operator !=(PgInet, PgInet)

```csharp
public static bool operator !=(PgInet left, PgInet right)
```

Parameters:

`left` — [PgInet](/api/ankus.pginet/)

`right` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-340927340af99924"></a>

### operator &lt;(PgInet, PgInet)

Compares two inet values in PostgreSQL order.

```csharp
public static bool operator <(PgInet left, PgInet right)
```

Parameters:

`left` — [PgInet](/api/ankus.pginet/)

`right` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-1cd7fd3cba489c20"></a>

### operator &lt;=(PgInet, PgInet)

Compares two inet values in PostgreSQL order, including equality.

```csharp
public static bool operator <=(PgInet left, PgInet right)
```

Parameters:

`left` — [PgInet](/api/ankus.pginet/)

`right` — [PgInet](/api/ankus.pginet/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)
