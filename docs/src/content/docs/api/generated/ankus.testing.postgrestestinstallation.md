---
title: 'PostgresTestInstallation'
description: 'API reference for Ankus.Testing.PostgresTestInstallation.'
slug: api/ankus.testing.postgrestestinstallation
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus.Testing](/api/ankus.testing/)

Assembly: `Ankus.Testing.dll`

Owns a relocatable copy of a PostgreSQL installation for isolated extension tests.

```csharp
public sealed class PostgresTestInstallation : IAsyncDisposable
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IAsyncDisposable](https://learn.microsoft.com/dotnet/api/system.iasyncdisposable)

## Methods

<a id="member-a14777c6b3ca81c2"></a>

### DisposeAsync()

Deletes the staged installation.

```csharp
public ValueTask DisposeAsync()
```

Returns: [ValueTask](https://learn.microsoft.com/dotnet/api/system.threading.tasks.valuetask)

A completed task after synchronous file cleanup.

<a id="member-d3ffa611949c5efb"></a>

### InstallExtensionFiles(string)

Copies published extension control and SQL files into this isolated installation.
Native libraries can remain in the publish directory and be selected with <code>dynamic_library_path</code>.

```csharp
public void InstallExtensionFiles(string publishDirectory)
```

Parameters:

`publishDirectory` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The Ankus publish directory.

<a id="member-6860535ef7ae2b96"></a>

### StageAsync(PostgresInstallation, string, CancellationToken)

Copies PostgreSQL into a relocatable staging root without changing the source installation.

```csharp
public static Task<PostgresTestInstallation> StageAsync(PostgresInstallation installation, string rootDirectory, CancellationToken cancellationToken = default)
```

Parameters:

`installation` — [PostgresInstallation](/api/ankus.pgconfig.postgresinstallation/)

The source PostgreSQL installation.

`rootDirectory` — [string](https://learn.microsoft.com/dotnet/api/system.string)

An empty directory to own as the staging root.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancels file staging and PostgreSQL inspection.

Returns: [Task&lt;PostgresTestInstallation&gt;](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task-1)

The staged installation owner.


## Properties

<a id="member-d0ca4cf111298936"></a>

### Installation

Gets the relocated PostgreSQL installation.

```csharp
public PostgresInstallation Installation { get; }
```

Value: [PostgresInstallation](/api/ankus.pgconfig.postgresinstallation/)

<a id="member-e35a81ceb90cdb63"></a>

### RootDirectory

Gets the staging root owned by this instance.

```csharp
public string RootDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)
