---
title: 'PostgresRegistry'
description: 'API reference for Ankus.PgConfig.PostgresRegistry.'
slug: api/ankus.pgconfig.postgresregistry
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus.PgConfig](/api/ankus.pgconfig/)

Assembly: `Ankus.PgConfig.dll`

Persists explicitly registered PostgreSQL installations in the Ankus home directory.

```csharp
public sealed class PostgresRegistry
```

Creates a registry at the specified home, or at the current user's ~/.ankus directory.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Constructors

<a id="member-994225cabf192eff"></a>

### PostgresRegistry(string?)

Persists explicitly registered PostgreSQL installations in the Ankus home directory.

```csharp
public PostgresRegistry(string? homeDirectory = null)
```

Parameters:

`homeDirectory` — [string](https://learn.microsoft.com/dotnet/api/system.string)

An optional Ankus home directory.

Creates a registry at the specified home, or at the current user's ~/.ankus directory.


## Properties

<a id="member-46dd191e92f27758"></a>

### ConfigurationPath

Gets the persisted configuration path.

```csharp
public string ConfigurationPath { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-6d333f31490202e2"></a>

### HomeDirectory

Gets the directory that contains configuration and managed installations.

```csharp
public string HomeDirectory { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)


## Methods

<a id="member-8d97e279fca19dfc"></a>

### GetAsync(int, CancellationToken)

Loads an explicitly registered installation, rejecting missing or mismatched registrations.

```csharp
public Task<PostgresInstallation> GetAsync(int major, CancellationToken cancellationToken = default)
```

Parameters:

`major` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The PostgreSQL major version.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancels installation queries.

Returns: [Task&lt;PostgresInstallation&gt;](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task-1)

The registered installation.

<a id="member-12accc41fd9f57a2"></a>

### GetPath(int)

Gets a registered pg_config path, resolving relative entries against the Ankus home.

```csharp
public string? GetPath(int major)
```

Parameters:

`major` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The PostgreSQL major version.

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The registered path, or null if this version has not been registered.

<a id="member-239fbf610865374e"></a>

### RegisterAsync(IReadOnlyDictionary&lt;int, string&gt;, CancellationToken)

Validates all requested installations before atomically updating the registry, preserving other settings.

```csharp
public Task<IReadOnlyList<PostgresInstallation>> RegisterAsync(IReadOnlyDictionary<int, string> paths, CancellationToken cancellationToken = default)
```

Parameters:

`paths` — [IReadOnlyDictionary&lt;int, string&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlydictionary-2)

PostgreSQL majors and their pg_config executable paths.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancels validation or writing before the commit.

Returns: [Task&lt;IReadOnlyList&lt;PostgresInstallation&gt;&gt;](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task-1)

The validated registrations.
