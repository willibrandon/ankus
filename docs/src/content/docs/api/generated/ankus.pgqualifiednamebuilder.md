---
title: 'PgQualifiedNameBuilder'
description: 'API reference for Ankus.PgQualifiedNameBuilder.'
slug: api/ankus.pgqualifiednamebuilder
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Builds an exact sequence of PostgreSQL name components for native operator lookup.

```csharp
public sealed class PgQualifiedNameBuilder : IReadOnlyList<string>, IReadOnlyCollection<string>, IEnumerable<string>, IEnumerable
```

Components are unquoted catalog identifiers, never parsed, split, trimmed or case-folded.
The managed builder is reusable and needs no backend until lookup. It is not thread-safe.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IReadOnlyList&lt;string&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1), [IReadOnlyCollection&lt;string&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlycollection-1), [IEnumerable&lt;string&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerable-1), [IEnumerable](https://learn.microsoft.com/dotnet/api/system.collections.ienumerable)

## Constructors

<a id="member-738f497015208c06"></a>

### PgQualifiedNameBuilder()

```csharp
public PgQualifiedNameBuilder()
```


## Methods

<a id="member-ed0981ec5b8b719b"></a>

### Add(string)

Appends one exact name component without interpreting SQL quoting or punctuation.

```csharp
public PgQualifiedNameBuilder Add(string value)
```

Parameters:

`value` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The component, including an empty string if desired.

Returns: [PgQualifiedNameBuilder](/api/ankus.pgqualifiednamebuilder/)

This builder for fluent construction.

Exceptions:

- [ArgumentNullException](https://learn.microsoft.com/dotnet/api/system.argumentnullexception): The component is null.
- [ArgumentException](https://learn.microsoft.com/dotnet/api/system.argumentexception): The component contains a zero character.
- [EncoderFallbackException](https://learn.microsoft.com/dotnet/api/system.text.encoderfallbackexception): The component contains invalid UTF-16.

<a id="member-6c746db86ee41fd3"></a>

### GetEnumerator()

Enumerates components in insertion order.

```csharp
public IEnumerator<string> GetEnumerator()
```

Returns: [IEnumerator&lt;string&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerator-1)

The component enumerator.

<a id="member-abecbf0ba1251bef"></a>

### GetOperatorOid(uint, uint)

Resolves an operator with exactly the supplied argument type OIDs using PostgreSQL's current namespace rules.

```csharp
public uint GetOperatorOid(uint leftTypeOid, uint rightTypeOid)
```

Parameters:

`leftTypeOid` — [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The left argument type, or zero for a prefix operator.

`rightTypeOid` — [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The right argument type; zero permits legacy postfix lookup where the server supports it.

Returns: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The current operator OID, or zero when the exact operator or explicit schema is absent.

One component follows search_path, two specify schema and operator, and three also specify the current database.
PostgreSQL reports invalid name counts, foreign databases and schema permission errors as PgException.
Lookup does not coerce argument types, consume this builder, cache OIDs, or retain native storage.


## Properties

<a id="member-89656dfa2acb684b"></a>

### Count

Gets the number of name components.

```csharp
public int Count { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-799e7f1f96f72110"></a>

### this[int]

Gets one exact component in insertion order.

```csharp
public string this[int index] { get; }
```

Parameters:

`index` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based component index.

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

The component.
