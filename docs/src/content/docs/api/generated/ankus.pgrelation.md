---
title: 'PgRelation'
description: 'API reference for Ankus.PgRelation.'
slug: api/ankus.pgrelation
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Holds a checked PostgreSQL relation cache reference with explicit native ownership and locking.

```csharp
public sealed class PgRelation : IDisposable
```

Use on the owning backend thread and dispose before its PostgreSQL resource owner ends.
PostgreSQL also reclaims references during statement, portal, or transaction cleanup; later checked access then fails.
Metadata is read from the live relation. Copied names and tuple descriptors remain usable after disposal.
Garbage collection never calls PostgreSQL. A relation may describe any pg_class object, including an index or sequence.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IDisposable](https://learn.microsoft.com/dotnet/api/system.idisposable)

## Methods

<a id="member-90181a14d377d312"></a>

### Clone()

Reopens the same OID with an independent AccessShare lock and cache reference.

```csharp
public PgRelation Clone()
```

Returns: [PgRelation](/api/ankus.pgrelation/)

An independently disposable owner.

<a id="member-91a454d1227e3344"></a>

### CountBufferHit()

Counts one buffer hit when relation statistics are enabled.

```csharp
public void CountBufferHit()
```

<a id="member-b7e99d485edf0028"></a>

### CountBufferRead()

Counts one buffer read when relation statistics are enabled.

```csharp
public void CountBufferRead()
```

<a id="member-c92e6fa17dcc3016"></a>

### CountHeapFetch()

Counts one fetched heap tuple when relation statistics are enabled.

```csharp
public void CountHeapFetch()
```

<a id="member-89abe41b01204a15"></a>

### CountHeapGetNext()

Counts one tuple returned by heap iteration when relation statistics are enabled.

```csharp
public void CountHeapGetNext()
```

<a id="member-f72a38aad81d3f2a"></a>

### CountHeapScan()

Increments PostgreSQL's heap scan counter when relation statistics are enabled.

```csharp
public void CountHeapScan()
```

<a id="member-cd1d14ed50e608cc"></a>

### CountIndexScan()

Increments PostgreSQL's index scan counter when relation statistics are enabled.

```csharp
public void CountIndexScan()
```

<a id="member-4bb234ef486776af"></a>

### CountIndexTuples(long)

Adds a signed count to PostgreSQL's tuples-returned counter, preserving pgrx's native counter semantics.

```csharp
public void CountIndexTuples(long count)
```

Parameters:

`count` — [long](https://learn.microsoft.com/dotnet/api/system.int64)

The signed tuple count.

<a id="member-13863666d780851c"></a>

### DangerousAdopt(void*)

Adopts one caller-owned Relation reference without incrementing it; disposal calls RelationClose without unlocking.

```csharp
public static PgRelation? DangerousAdopt(void* address)
```

Parameters:

`address` — [void*](https://learn.microsoft.com/dotnet/api/system.void)

A selected-version Relation pointer, or null.

Returns: [PgRelation](/api/ankus.pgrelation/)

The exclusive managed owner, or null for a null pointer.

The caller transfers exactly one close obligation registered with the current PostgreSQL resource owner.
It must hold appropriate locks and must not independently close or adopt that same reference again.
Failed adoption leaves the external close obligation with the caller.

<a id="member-eda5c35bcbdb87e4"></a>

### DangerousBorrow(void*)

Borrows a caller-proven live Relation pointer without incrementing or closing its native reference.

```csharp
public static PgRelation? DangerousBorrow(void* address)
```

Parameters:

`address` — [void*](https://learn.microsoft.com/dotnet/api/system.void)

A selected-version Relation pointer, or null.

Returns: [PgRelation](/api/ankus.pgrelation/)

A checked view, or null for a null pointer.

The caller must preserve the external reference and required locks until disposal or ownership transfer.
Its resource owner must be the current PostgreSQL owner. Checks cannot detect an earlier external close.

<a id="member-9ec328d6c49061d8"></a>

### DangerousGetIndicesWithoutLock()

Opens every attached index without acquiring or releasing locks. The caller must already protect each index.

```csharp
public IReadOnlyList<PgRelation> DangerousGetIndicesWithoutLock()
```

Returns: [IReadOnlyList&lt;PgRelation&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1)

Independent cache-reference owners; dispose every element when finished.

<a id="member-481aec5077c17bbc"></a>

### DangerousGetPointer()

Returns a borrowed selected-version Relation pointer after validating this handle.

```csharp
public void* DangerousGetPointer()
```

Returns: [void*](https://learn.microsoft.com/dotnet/api/system.void)

The native pointer, valid only while this reference and its required locks remain live.

<a id="member-e066d71e1e16b1af"></a>

### DangerousOpenHeap()

Opens the index's owning heap without locking it. The caller must already hold the heap's required lock.

```csharp
public PgRelation? DangerousOpenHeap()
```

Returns: [PgRelation](/api/ankus.pgrelation/)

An independent owned heap reference, or null when this relation has no native index metadata.

<a id="member-97a85f30e5c4e9fc"></a>

### DangerousOpenWithoutLock(string)

Resolves a name and opens without acquiring a lock. The caller must already protect the resolved relation.

```csharp
public static PgRelation? DangerousOpenWithoutLock(string name)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The PostgreSQL relation name or numeric OID syntax.

Returns: [PgRelation](/api/ankus.pgrelation/)

An owned cache reference, or null when native to_regclass returns SQL NULL.

<a id="member-186cae32ef94bc8b"></a>

### DangerousOpenWithoutLock(uint)

Opens without acquiring or later releasing a lock. The caller must already hold at least AccessShare.

```csharp
public static PgRelation DangerousOpenWithoutLock(uint oid)
```

Parameters:

`oid` — [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The relation OID protected by the caller's lock.

Returns: [PgRelation](/api/ankus.pgrelation/)

An owned cache reference whose disposal only closes that reference.

<a id="member-f3865c1405171d59"></a>

### DangerousTakeOwnership()

Transfers an external borrowed close obligation into an owned wrapper without incrementing the reference.

```csharp
public PgRelation DangerousTakeOwnership()
```

Returns: [PgRelation](/api/ankus.pgrelation/)

This object when already owned, or a new owner that consumes the borrowed wrapper.

For a borrowed view, the caller must exclusively transfer the external close obligation and never close it again.
This operation does not acquire or release an external lock.
A failed transfer leaves the borrowed wrapper and external close obligation unchanged.

<a id="member-298ef5af70bef2ad"></a>

### Dispose()

Releases this wrapper and its owned reference and lock. Borrowed wrappers do not close the external reference.
Repeated disposal and disposal after native resource-owner cleanup are harmless on the owning backend thread.

```csharp
public void Dispose()
```

<a id="member-52dcc6cdf2f8be1c"></a>

### GetIndices(PgLockMode)

Eagerly opens every attached index, closing previously opened references if any later open fails.

```csharp
public IReadOnlyList<PgRelation> GetIndices(PgLockMode lockMode = PgLockMode.AccessShare)
```

Parameters:

`lockMode` — [PgLockMode](/api/ankus.pglockmode/)

The lock to acquire separately for each index.

Returns: [IReadOnlyList&lt;PgRelation&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1)

A read-only list of independent owners; dispose every element when finished.

<a id="member-91009731203835c0"></a>

### Open(string, PgLockMode)

Resolves a relation using native to_regclass syntax and search-path rules, then opens and locks it.

```csharp
public static PgRelation Open(string name, PgLockMode lockMode = PgLockMode.AccessShare)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The PostgreSQL relation name or numeric OID syntax.

`lockMode` — [PgLockMode](/api/ankus.pglockmode/)

The required lock, defaulting to AccessShare.

Returns: [PgRelation](/api/ankus.pgrelation/)

An independently disposable reference. Missing names raise a PostgreSQL error.

<a id="member-8836cbdfc6dac0b9"></a>

### Open(uint, PgLockMode)

Opens a relation by its exact OID and acquires the requested lock. Disposal releases this lock acquisition.
Automatic resource cleanup follows PostgreSQL's transaction lock rules.

```csharp
public static PgRelation Open(uint oid, PgLockMode lockMode = PgLockMode.AccessShare)
```

Parameters:

`oid` — [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The relation OID; nonexistent OIDs raise a PostgreSQL error.

`lockMode` — [PgLockMode](/api/ankus.pglockmode/)

The required lock, defaulting to AccessShare. None is not accepted.

Returns: [PgRelation](/api/ankus.pgrelation/)

An independently disposable relation reference.

<a id="member-00b7dbc33ccf2d25"></a>

### ToDatum(PgMemoryContext)

Copies the relation OID into an explicitly context-bound regclass datum without transferring this reference.

```csharp
public PgDatum ToDatum(PgMemoryContext context)
```

Parameters:

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The datum's lifetime anchor.

Returns: [PgDatum](/api/ankus.pgdatum/)

A checked, nonnull regclass datum with the exact relation identity.

<a id="member-142f3c98d26779ee"></a>

### TryOpen(string, PgLockMode)

Resolves and opens a relation, returning null when native to_regclass returns SQL NULL.

```csharp
public static PgRelation? TryOpen(string name, PgLockMode lockMode = PgLockMode.AccessShare)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The PostgreSQL relation name or numeric OID syntax.

`lockMode` — [PgLockMode](/api/ankus.pglockmode/)

The required lock, defaulting to AccessShare.

Returns: [PgRelation](/api/ankus.pgrelation/)

The owned reference or null. Lock, permission, and concurrent-deletion errors still propagate.


## Properties

<a id="member-e7305e3eaed07a7e"></a>

### EstimatedTupleCount

Gets the native reltuples estimate, mapping exactly zero to null as pgrx does and preserving minus one.

```csharp
public float? EstimatedTupleCount { get; }
```

Value: [float?](https://learn.microsoft.com/dotnet/api/system.single)

<a id="member-8be6aaf987ed4566"></a>

### IsCompositeType

Gets whether this describes a standalone composite type.

```csharp
public bool IsCompositeType { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-7e5aaac437a421a3"></a>

### IsForeignTable

Gets whether this is a foreign table.

```csharp
public bool IsForeignTable { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-9ddaa33ef6cf6703"></a>

### IsIndex

Gets whether this is an ordinary index. A partitioned index has kind I instead.

```csharp
public bool IsIndex { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-29c413dfe55a3dfd"></a>

### IsMaterializedView

Gets whether this is a materialized view.

```csharp
public bool IsMaterializedView { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-5cfde0c33368921b"></a>

### IsPartitionedTable

Gets whether this is a partitioned table.

```csharp
public bool IsPartitionedTable { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-abc7bf75421dbc40"></a>

### IsSequence

Gets whether this is a sequence.

```csharp
public bool IsSequence { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-1b7813cbfae88083"></a>

### IsTable

Gets whether this is an ordinary table, including an ordinary table used as a partition.

```csharp
public bool IsTable { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-7b6deb8fc8692498"></a>

### IsToast

Gets whether this is a TOAST table.

```csharp
public bool IsToast { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-3a220474388b8ad8"></a>

### IsView

Gets whether this is a view.

```csharp
public bool IsView { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-d4e571e167634248"></a>

### Kind

Gets the exact PostgreSQL relkind character, including kinds without a convenience predicate.

```csharp
public char Kind { get; }
```

Value: [char](https://learn.microsoft.com/dotnet/api/system.char)

<a id="member-8af7c07a9398a585"></a>

### Name

Gets a copy of the relation's current unqualified name.

```csharp
public string Name { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-556de718095f71ca"></a>

### NamespaceName

Gets a copy of the current namespace name.

```csharp
public string NamespaceName { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-f9ae84832bbd89b2"></a>

### NamespaceOid

Gets the current namespace OID.

```csharp
public uint NamespaceOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

<a id="member-f1e1687282900f3a"></a>

### Oid

Gets the exact relation OID, distinct from its row type OID.

```csharp
public uint Oid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

<a id="member-740d8723c196c4d6"></a>

### TupleDescriptor

Gets an owned copy of the actual relation descriptor, including physical dropped slots and index columns.

```csharp
public PgTupleDescriptor TupleDescriptor { get; }
```

Value: [PgTupleDescriptor](/api/ankus.pgtupledescriptor/)
