---
title: 'PgNumeric'
description: 'API reference for Ankus.PgNumeric.'
slug: api/ankus.pgnumeric
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns a full-range PostgreSQL numeric value, including display scale, NaN, and infinities.
The default value is zero. Equality ignores trailing fractional zeroes and treats NaN as equal to NaN.

```csharp
[JsonConverter(typeof(PgNumericConverter))]
public readonly struct PgNumeric : IComparable<PgNumeric>, IEquatable<PgNumeric>
```

Implements: [IComparable&lt;PgNumeric&gt;](https://learn.microsoft.com/dotnet/api/system.icomparable-1), [IEquatable&lt;PgNumeric&gt;](https://learn.microsoft.com/dotnet/api/system.iequatable-1)

## Methods

<a id="member-b8a9920c9735a8ec"></a>

### Abs()

Gets the absolute value using PostgreSQL numeric semantics.

```csharp
public PgNumeric Abs()
```

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The absolute value.

<a id="member-7e991c711bb9241d"></a>

### Ceiling()

Rounds toward positive infinity.

```csharp
public PgNumeric Ceiling()
```

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The least integer greater than or equal to this value.

<a id="member-5b127deb162f74e2"></a>

### CompareTo(PgNumeric)

Compares with PostgreSQL's ordering: negative infinity, finite values, positive infinity, then NaN.

```csharp
public int CompareTo(PgNumeric other)
```

Parameters:

`other` — [PgNumeric](/api/ankus.pgnumeric/)

The other value.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

A negative value, zero, or a positive value for lesser, equal, or greater values.

<a id="member-4b5837f786d7ee42"></a>

### Equals(PgNumeric)

Compares numeric values independently of display scale and backend access.

```csharp
public bool Equals(PgNumeric other)
```

Parameters:

`other` — [PgNumeric](/api/ankus.pgnumeric/)

The other value.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the values compare equal, including NaN compared with NaN.

<a id="member-01c478af61ec9ef5"></a>

### Equals(object)

```csharp
public override bool Equals(object obj)
```

Parameters:

`obj` — [object](https://learn.microsoft.com/dotnet/api/system.object)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-382bcf4ce19ad58e"></a>

### Exp()

Computes e raised to this value using PostgreSQL numeric precision.

```csharp
public PgNumeric Exp()
```

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The exponential.

<a id="member-96ed580a6c879b10"></a>

### Floor()

Rounds toward negative infinity.

```csharp
public PgNumeric Floor()
```

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The greatest integer less than or equal to this value.

<a id="member-a4ec37f5079bcb5f"></a>

### FromBigInteger(BigInteger)

Converts an arbitrary-precision integer within PostgreSQL's 131072-digit limit.

```csharp
public static PgNumeric FromBigInteger(BigInteger value)
```

Parameters:

`value` — [BigInteger](https://learn.microsoft.com/dotnet/api/system.numerics.biginteger)

The integer.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The numeric value without a fractional part.

<a id="member-aa39eacc13950a5e"></a>

### FromDecimal(decimal)

Converts a decimal exactly without requiring an active backend.

```csharp
public static PgNumeric FromDecimal(decimal value)
```

Parameters:

`value` — [decimal](https://learn.microsoft.com/dotnet/api/system.decimal)

The decimal, including its stored scale.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The exact numeric value.

<a id="member-6cf34a9bf91a77e5"></a>

### FromDouble(double)

Converts a floating-point value using PostgreSQL's float8-to-numeric conversion.

```csharp
public static PgNumeric FromDouble(double value)
```

Parameters:

`value` — [double](https://learn.microsoft.com/dotnet/api/system.double)

The floating-point value.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The numeric value, with the server's conversion precision.

<a id="member-26f3a4588bbf8b38"></a>

### GetHashCode()

Gets a hash consistent with scale-independent numeric equality.

```csharp
public override int GetHashCode()
```

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The numeric hash.

<a id="member-4649201c0774e5b5"></a>

### GreatestCommonDivisor(PgNumeric)

Computes the greatest common divisor with PostgreSQL numeric semantics.

```csharp
public PgNumeric GreatestCommonDivisor(PgNumeric other)
```

Parameters:

`other` — [PgNumeric](/api/ankus.pgnumeric/)

The other value.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The greatest common divisor.

<a id="member-8422419e101587fd"></a>

### LeastCommonMultiple(PgNumeric)

Computes the least common multiple with PostgreSQL numeric semantics.

```csharp
public PgNumeric LeastCommonMultiple(PgNumeric other)
```

Parameters:

`other` — [PgNumeric](/api/ankus.pgnumeric/)

The other value.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The least common multiple.

<a id="member-b782e3e6bf2c2281"></a>

### Log()

Computes the natural logarithm.

```csharp
public PgNumeric Log()
```

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The natural logarithm.

<a id="member-9a51d19cc576caae"></a>

### Log(PgNumeric)

Computes the logarithm in the specified base.

```csharp
public PgNumeric Log(PgNumeric @base)
```

Parameters:

`base` — [PgNumeric](/api/ankus.pgnumeric/)

The logarithm base.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The logarithm.

<a id="member-025af00bba88f312"></a>

### Parse(string)

Parses PostgreSQL numeric syntax on the active backend thread, including exponents and special values.

```csharp
public static PgNumeric Parse(string text)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The PostgreSQL numeric input.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The parsed value with PostgreSQL's display scale.

<a id="member-482fb683de93bfff"></a>

### Pow(PgNumeric)

Raises this value to a numeric power.

```csharp
public PgNumeric Pow(PgNumeric exponent)
```

Parameters:

`exponent` — [PgNumeric](/api/ankus.pgnumeric/)

The exponent.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The power.

<a id="member-5d09729865701466"></a>

### Rescale(int, int)

Rounds to a declared precision and scale with PostgreSQL's numeric typmod rules.

```csharp
public PgNumeric Rescale(int precision, int scale)
```

Parameters:

`precision` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The maximum significant digits, one through 1000.

`scale` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The declared scale. Negative scales and scales above precision require PostgreSQL 15 or later.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The rescaled numeric; a range error is raised if it does not fit.

<a id="member-b63415cc9e0e2bcd"></a>

### Round(int)

Rounds to a fractional scale, breaking ties away from zero as PostgreSQL does.

```csharp
public PgNumeric Round(int scale = 0)
```

Parameters:

`scale` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The fractional scale; negative values round to the left of the decimal point.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The rounded numeric.

<a id="member-9da016aeeea9a29c"></a>

### Sqrt()

Computes the PostgreSQL numeric square root.

```csharp
public PgNumeric Sqrt()
```

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The square root.

<a id="member-4ea8fce5afe3ebbb"></a>

### ToBigInteger()

Converts a finite integer exactly, rejecting fractional values and nonfinite values.

```csharp
public BigInteger ToBigInteger()
```

Returns: [BigInteger](https://learn.microsoft.com/dotnet/api/system.numerics.biginteger)

The arbitrary-precision integer.

<a id="member-857dda2a3650c9a8"></a>

### ToDecimal()

Converts exactly to decimal, rejecting overflow, nonfinite values, and any change in numeric value.

```csharp
public decimal ToDecimal()
```

Returns: [decimal](https://learn.microsoft.com/dotnet/api/system.decimal)

The decimal value.

Exceptions:

- [OverflowException](https://learn.microsoft.com/dotnet/api/system.overflowexception): The value cannot be represented exactly as a decimal.

<a id="member-2a065a437ea6344a"></a>

### ToDouble()

Converts to double using PostgreSQL's precision and range rules.

```csharp
public double ToDouble()
```

Returns: [double](https://learn.microsoft.com/dotnet/api/system.double)

The floating-point approximation.

<a id="member-1fd31efc96b58926"></a>

### ToNormalizedString()

Returns culture-independent text without insignificant fractional zeroes.

```csharp
public string ToNormalizedString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The normalized numeric text.

<a id="member-6faac9b93116c892"></a>

### ToString()

Returns the owned PostgreSQL text, retaining display scale.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The numeric text.

<a id="member-6777637743430bfa"></a>

### Truncate(int)

Truncates toward zero to a fractional scale.

```csharp
public PgNumeric Truncate(int scale = 0)
```

Parameters:

`scale` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The fractional scale; negative values truncate to the left of the decimal point.

Returns: [PgNumeric](/api/ankus.pgnumeric/)

The truncated numeric.

<a id="member-d5822f946c23322a"></a>

### TryParse(string?, out PgNumeric)

Tries to parse numeric input on the active backend thread.

```csharp
public static bool TryParse(string? text, out PgNumeric value)
```

Parameters:

`text` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The candidate input.

`value` — [PgNumeric](/api/ankus.pgnumeric/)

The parsed numeric, or zero on invalid input.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the input was valid. Backend-access and operational errors still throw.


## Properties

<a id="member-d4b9e43948ebb789"></a>

### IsFinite

Gets whether the value is finite.

```csharp
public bool IsFinite { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-f8e60074e6100fa8"></a>

### IsNaN

Gets whether the value is PostgreSQL NaN.

```csharp
public bool IsNaN { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-c6307246b5c76996"></a>

### NaN

Gets PostgreSQL's not-a-number value, which sorts above all other numeric values.

```csharp
public static PgNumeric NaN { get; }
```

Value: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-e278a2301ecd0432"></a>

### NegativeInfinity

Gets negative infinity. Native numeric infinities require PostgreSQL 14 or later.

```csharp
public static PgNumeric NegativeInfinity { get; }
```

Value: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-3e9ca4645a492e77"></a>

### PositiveInfinity

Gets positive infinity. Native numeric infinities require PostgreSQL 14 or later.

```csharp
public static PgNumeric PositiveInfinity { get; }
```

Value: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-93e613efd0c23fd9"></a>

### Scale

Gets the display scale, or null for NaN and infinities.

```csharp
public int? Scale { get; }
```

Value: [int?](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-96adf3f8af81edd1"></a>

### Sign

Gets -1, 0, or 1 for negative, zero, or positive values, or null for NaN.

```csharp
public int? Sign { get; }
```

Value: [int?](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-f75c9028fbc2f504"></a>

### Text

Gets the owned, culture-independent PostgreSQL output text, retaining display scale.

```csharp
public string Text { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)


## Operators

<a id="member-e7fec7982912ea09"></a>

### operator +(PgNumeric, PgNumeric)

Adds numeric values in PostgreSQL.

```csharp
public static PgNumeric operator +(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-6fafd43de3a1f08b"></a>

### operator /(PgNumeric, PgNumeric)

Divides numeric values using PostgreSQL's result-scale rules.

```csharp
public static PgNumeric operator /(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-beb9be3ff6cf0e8c"></a>

### operator ==(PgNumeric, PgNumeric)

```csharp
public static bool operator ==(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-73338a57b1c30464"></a>

### operator &gt;(PgNumeric, PgNumeric)

Tests whether the left numeric is greater than the right numeric.

```csharp
public static bool operator >(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-88fc6db4d4fbd792"></a>

### operator &gt;=(PgNumeric, PgNumeric)

Tests whether the left numeric is greater than or equal to the right numeric.

```csharp
public static bool operator >=(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-3d0e12a509884dce"></a>

### operator !=(PgNumeric, PgNumeric)

```csharp
public static bool operator !=(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-bac24cb80998f496"></a>

### operator &lt;(PgNumeric, PgNumeric)

Tests whether the left numeric is less than the right numeric.

```csharp
public static bool operator <(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-01600fc9d868dd9b"></a>

### operator &lt;=(PgNumeric, PgNumeric)

Tests whether the left numeric is less than or equal to the right numeric.

```csharp
public static bool operator <=(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-18810b5c513916e5"></a>

### operator %(PgNumeric, PgNumeric)

Computes PostgreSQL's numeric remainder.

```csharp
public static PgNumeric operator %(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-fab58bf0709d2509"></a>

### operator *(PgNumeric, PgNumeric)

Multiplies numeric values in PostgreSQL.

```csharp
public static PgNumeric operator *(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-ca52edfde62fbb20"></a>

### operator -(PgNumeric, PgNumeric)

Subtracts numeric values in PostgreSQL.

```csharp
public static PgNumeric operator -(PgNumeric left, PgNumeric right)
```

Parameters:

`left` — [PgNumeric](/api/ankus.pgnumeric/)

`right` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [PgNumeric](/api/ankus.pgnumeric/)

<a id="member-dd3a4076c4851afb"></a>

### operator -(PgNumeric)

Negates a numeric value in PostgreSQL.

```csharp
public static PgNumeric operator -(PgNumeric value)
```

Parameters:

`value` — [PgNumeric](/api/ankus.pgnumeric/)

Returns: [PgNumeric](/api/ankus.pgnumeric/)
