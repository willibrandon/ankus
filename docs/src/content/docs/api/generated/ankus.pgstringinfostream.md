---
title: 'PgStringInfoStream'
description: 'API reference for Ankus.PgStringInfoStream.'
slug: api/ankus.pgstringinfostream
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Appends binary data and UTF-8 text to a checked PostgreSQL StringInfo buffer.

```csharp
public sealed class PgStringInfoStream : Stream, IDisposable, IAsyncDisposable
```

This write-only stream requires the active backend thread. Native buffers retain their
PostgreSQL context ownership across ambient context switches. Disposal frees owned storage;
borrowed storage remains with its native owner. No finalizer calls PostgreSQL.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object), [MarshalByRefObject](https://learn.microsoft.com/dotnet/api/system.marshalbyrefobject), [Stream](https://learn.microsoft.com/dotnet/api/system.io.stream)

Implements: [IDisposable](https://learn.microsoft.com/dotnet/api/system.idisposable), [IAsyncDisposable](https://learn.microsoft.com/dotnet/api/system.iasyncdisposable)

## Properties

<a id="member-96aaf5ca41cb5664"></a>

### CanRead

Gets false because stream reads are unsupported; use checked byte copies instead.

```csharp
public override bool CanRead { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-2177e89e4a75b3a6"></a>

### CanSeek

Gets false because writes always append to the native buffer.

```csharp
public override bool CanSeek { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-5fcbf7462134d760"></a>

### CanWrite

Gets whether this undisposed wrapper was created for a writable native buffer.

```csharp
public override bool CanWrite { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

This capability does not prove that its native lifetime remains live; each operation checks that lifetime.

<a id="member-e355ed138644d55a"></a>

### Capacity

Gets the payload capacity excluding NUL, or the payload length for a read-only external buffer.

```csharp
public int Capacity { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-f7d79d40a75b19e9"></a>

### IsEmpty

Gets whether the checked native buffer currently contains no payload bytes.

```csharp
public bool IsEmpty { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-1db23e73bb0d1957"></a>

### IsReadOnly

Gets whether the native buffer is marked read-only.

```csharp
public bool IsReadOnly { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-0fa96e17eed084c1"></a>

### Length

Gets the current payload byte length, excluding any trailing NUL.

```csharp
public override long Length { get; }
```

Value: [long](https://learn.microsoft.com/dotnet/api/system.int64)

<a id="member-33bace7e4618f18a"></a>

### LifetimeContext

Gets the native owner for an owned buffer or the explicit lifetime anchor for a borrowed buffer.

```csharp
public PgMemoryContext LifetimeContext { get; }
```

Value: [PgMemoryContext](/api/ankus.pgmemorycontext/)

<a id="member-81923730cd70cec0"></a>

### Position

Rejects stream positioning because writes always append.

```csharp
public override long Position { get; set; }
```

Value: [long](https://learn.microsoft.com/dotnet/api/system.int64)


## Methods

<a id="member-31cf6c4b5ebf4b27"></a>

### CopyTo(Span&lt;byte&gt;, int)

Copies an exact payload range without exposing a span over resizable native storage.

```csharp
public void CopyTo(Span<byte> destination, int offset = 0)
```

Parameters:

`destination` — [Span&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.span-1)

The managed destination; its length selects the byte count.

`offset` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The first native payload byte to copy.

<a id="member-d5602354c9cbdd37"></a>

### Create(int, PgMemoryContext?)

Creates an empty PostgreSQL buffer with at least the requested payload capacity.

```csharp
public static PgStringInfoStream Create(int capacity = 0, PgMemoryContext? context = null)
```

Parameters:

`capacity` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The minimum byte capacity, excluding the trailing NUL.

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination context, or null for the current context.

Returns: [PgStringInfoStream](/api/ankus.pgstringinfostream/)

An individually owned native buffer.

<a id="member-3dad2ec8c13e0166"></a>

### Create(ReadOnlySpan&lt;byte&gt;, PgMemoryContext?)

Creates an owned buffer containing an independent copy of arbitrary bytes.

```csharp
public static PgStringInfoStream Create(ReadOnlySpan<byte> value, PgMemoryContext? context = null)
```

Parameters:

`value` — [ReadOnlySpan&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The initial bytes, including any embedded NUL or invalid UTF-8.

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination context, or null for the current context.

Returns: [PgStringInfoStream](/api/ankus.pgstringinfostream/)

The initialized native buffer.

<a id="member-b91996a45a12a9f1"></a>

### Create(string, PgMemoryContext?)

Creates an owned buffer containing strict UTF-8, independently of the database encoding.

```csharp
public static PgStringInfoStream Create(string value, PgMemoryContext? context = null)
```

Parameters:

`value` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The initial text; embedded NUL characters are preserved.

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination context, or null for the current context.

Returns: [PgStringInfoStream](/api/ankus.pgstringinfostream/)

The initialized native buffer.

Exceptions:

- [EncoderFallbackException](https://learn.microsoft.com/dotnet/api/system.text.encoderfallbackexception): The text contains an unpaired UTF-16 surrogate.

<a id="member-2e670fb3d783dcff"></a>

### DangerousAppend(void*, int)

Appends an explicitly accessible native byte range, including a slice of this buffer's payload.

```csharp
public void DangerousAppend(void* data, int length)
```

Parameters:

`data` — [void*](https://learn.microsoft.com/dotnet/api/system.void)

The accessible source pointer, or null for an empty append.

`length` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The source byte count.

The caller guarantees the source range remains readable until copied. A slice of the current
payload is resolved again after growth, so self-appending does not retain an obsolete pointer.

<a id="member-0da61fc49c2ce4e1"></a>

### DangerousBorrow(void*, PgMemoryContext)

Borrows an external PostgreSQL StringInfo without acquiring release rights.

```csharp
public static PgStringInfoStream? DangerousBorrow(void* address, PgMemoryContext lifetimeContext)
```

Parameters:

`address` — [void*](https://learn.microsoft.com/dotnet/api/system.void)

The initialized native StringInfoData address, or null.

`lifetimeContext` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The explicit reset-sensitive lifetime anchor.

Returns: [PgStringInfoStream](/api/ankus.pgstringinfostream/)

A borrowed buffer, or null for a null address.

The caller guarantees accessible struct and payload storage until the shorter of its
external lifetime and the anchor lifetime. Mutable buffers must support PostgreSQL's
repalloc contract. Read-only buffers may use non-palloc storage without a trailing NUL.
The anchor does not establish allocator provenance or detect external free or resize.

<a id="member-88a4f3db334c8704"></a>

### DangerousDetach()

Consumes this wrapper and returns the whole native StringInfo without freeing its storage.

```csharp
public void* DangerousDetach()
```

Returns: [void*](https://learn.microsoft.com/dotnet/api/system.void)

The native StringInfoData pointer with its original PostgreSQL lifetime.

<a id="member-ab4ab48bb0ef77a7"></a>

### DangerousDetachCString()

Consumes the buffer only after proving its payload contains exactly one trailing NUL and no interior NUL.

```csharp
public byte* DangerousDetachCString()
```

Returns: [byte*](https://learn.microsoft.com/dotnet/api/system.byte)

The original NUL-terminated data pointer, with the same ownership rules as DangerousDetachData.

For a read-only external buffer, the unsafe caller must prove Length plus one bytes are
accessible. Rejection leaves this wrapper live and its bytes unchanged.

<a id="member-396435f829e6ebde"></a>

### DangerousDetachData()

Consumes this wrapper and returns its payload without freeing the payload storage.

```csharp
public byte* DangerousDetachData()
```

Returns: [byte*](https://learn.microsoft.com/dotnet/api/system.byte)

The original payload pointer with its existing native lifetime.

For owned buffers, the now-unneeded native struct is freed. Borrowed structs and payloads
remain untouched. This operation does not promise C-string termination or transfer a borrowed allocator's rights.

<a id="member-cc17dfa6f4e33f2f"></a>

### Dispose(bool)

Frees both native allocations when owned; borrowed disposal only closes this wrapper.

```csharp
protected override void Dispose(bool disposing)
```

Parameters:

`disposing` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether explicit managed disposal is occurring.

<a id="member-934947136f2b8492"></a>

### DisposeAsync()

Releases owned storage synchronously on the invoking backend thread.

```csharp
public override ValueTask DisposeAsync()
```

Returns: [ValueTask](https://learn.microsoft.com/dotnet/api/system.threading.tasks.valuetask)

A completed operation.

<a id="member-026e9b14b772d754"></a>

### Enlarge(int)

Ensures capacity for at least the requested number of additional payload bytes.

```csharp
public void Enlarge(int additionalByteCount)
```

Parameters:

`additionalByteCount` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

Additional bytes beyond the current length.

<a id="member-fead0d7ee47b4e83"></a>

### EnsureCapacity(int)

Ensures an absolute minimum payload capacity while preserving all existing bytes.

```csharp
public int EnsureCapacity(int capacity)
```

Parameters:

`capacity` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The minimum payload capacity excluding NUL.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The resulting payload capacity.

<a id="member-479686fae440de78"></a>

### Flush()

Validates the buffer; appended bytes already reside in PostgreSQL storage.

```csharp
public override void Flush()
```

<a id="member-e0968c2952ac0eab"></a>

### FlushAsync(CancellationToken)

Validates the buffer synchronously on the invoking backend thread.

```csharp
public override Task FlushAsync(CancellationToken cancellationToken)
```

Parameters:

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancellation checked before validation.

Returns: [Task](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task)

A completed task.

<a id="member-0958406734178f20"></a>

### Read(byte[], int, int)

Rejects stream reads; use ToArray or CopyTo with a destination span.

```csharp
public override int Read(byte[] buffer, int offset, int count)
```

Parameters:

`buffer` — [byte[]](https://learn.microsoft.com/dotnet/api/system.byte)

The unused destination.

`offset` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The unused destination offset.

`count` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The unused requested length.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

No value; this method throws NotSupportedException.

<a id="member-6d04256c94b6d7f0"></a>

### Reset()

Clears the payload and native cursor while retaining the allocated capacity.

```csharp
public void Reset()
```

<a id="member-f00cf71085fb0fb5"></a>

### Seek(long, SeekOrigin)

Rejects seeking because stream writes always append.

```csharp
public override long Seek(long offset, SeekOrigin origin)
```

Parameters:

`offset` — [long](https://learn.microsoft.com/dotnet/api/system.int64)

The unused offset.

`origin` — [SeekOrigin](https://learn.microsoft.com/dotnet/api/system.io.seekorigin)

The unused origin.

Returns: [long](https://learn.microsoft.com/dotnet/api/system.int64)

No value; this method throws NotSupportedException.

<a id="member-703469c16c8b88e4"></a>

### SetLength(long)

Rejects arbitrary length changes; use Reset to clear the payload.

```csharp
public override void SetLength(long value)
```

Parameters:

`value` — [long](https://learn.microsoft.com/dotnet/api/system.int64)

The unused length.

<a id="member-12e5e95bc9e7e56d"></a>

### ToArray()

Copies the complete payload into independent managed storage.

```csharp
public byte[] ToArray()
```

Returns: [byte[]](https://learn.microsoft.com/dotnet/api/system.byte)

The exact payload bytes without the native terminator.

<a id="member-feeba15a7bd2254d"></a>

### ToString()

Decodes the complete payload as strict UTF-8, preserving embedded NUL characters.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The decoded text.

Exceptions:

- [DecoderFallbackException](https://learn.microsoft.com/dotnet/api/system.text.decoderfallbackexception): The payload is not valid UTF-8.

<a id="member-12b6f922697cf95c"></a>

### ToStringLossy()

Formats arbitrary bytes for display by explicitly replacing malformed UTF-8 sequences.

```csharp
public string ToStringLossy()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

Display text containing replacement characters for invalid sequences.

This matches pgrx's lossy Display formatting. Use ToArray or strict ToString when preserving values.

<a id="member-39c80abb6e17818f"></a>

### Write(byte[], int, int)

Appends the selected managed byte range.

```csharp
public override void Write(byte[] buffer, int offset, int count)
```

Parameters:

`buffer` — [byte[]](https://learn.microsoft.com/dotnet/api/system.byte)

The source array.

`offset` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The first source byte.

`count` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The number of bytes to append.

<a id="member-6f3458d6b246df8d"></a>

### Write(char)

Appends one non-surrogate UTF-16 character as UTF-8.

```csharp
public void Write(char value)
```

Parameters:

`value` — [char](https://learn.microsoft.com/dotnet/api/system.char)

The character to append; use a string or Rune for a surrogate pair.

<a id="member-631118ab56b9a1c6"></a>

### Write(ReadOnlySpan&lt;byte&gt;)

Appends arbitrary bytes, retaining embedded NUL and invalid UTF-8.

```csharp
public override void Write(ReadOnlySpan<byte> buffer)
```

Parameters:

`buffer` — [ReadOnlySpan&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The payload to append.

<a id="member-dc4b20db84dd8eec"></a>

### Write(string)

Appends strict UTF-8 text without database-encoding conversion.

```csharp
public void Write(string value)
```

Parameters:

`value` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The text, including any embedded NUL.

Exceptions:

- [EncoderFallbackException](https://learn.microsoft.com/dotnet/api/system.text.encoderfallbackexception): The text contains an unpaired UTF-16 surrogate.

<a id="member-c8da0283106e39a0"></a>

### Write(Rune)

Appends one complete Unicode scalar as UTF-8.

```csharp
public void Write(Rune value)
```

Parameters:

`value` — [Rune](https://learn.microsoft.com/dotnet/api/system.text.rune)

The scalar to append.

<a id="member-111ccca8621c269b"></a>

### WriteAsync(byte[], int, int, CancellationToken)

Writes synchronously on the current backend thread and returns a completed task.

```csharp
public override Task WriteAsync(byte[] buffer, int offset, int count, CancellationToken cancellationToken)
```

Parameters:

`buffer` — [byte[]](https://learn.microsoft.com/dotnet/api/system.byte)

The source array.

`offset` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The first source byte.

`count` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The number of bytes to append.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancellation checked before accessing native storage.

Returns: [Task](https://learn.microsoft.com/dotnet/api/system.threading.tasks.task)

A completed task; no work is queued to another thread.

<a id="member-d8b0a19896409ac7"></a>

### WriteAsync(ReadOnlyMemory&lt;byte&gt;, CancellationToken)

Writes synchronously on the current backend thread and returns a completed operation.

```csharp
public override ValueTask WriteAsync(ReadOnlyMemory<byte> buffer, CancellationToken cancellationToken = default)
```

Parameters:

`buffer` — [ReadOnlyMemory&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.readonlymemory-1)

The source bytes.

`cancellationToken` — [CancellationToken](https://learn.microsoft.com/dotnet/api/system.threading.cancellationtoken)

Cancellation checked before accessing native storage.

Returns: [ValueTask](https://learn.microsoft.com/dotnet/api/system.threading.tasks.valuetask)

A completed operation; no native capability flows to another thread.

<a id="member-bce0196b09f240cd"></a>

### WriteAt(int, ReadOnlySpan&lt;byte&gt;)

Replaces an existing payload range without appending or changing its length.

```csharp
public void WriteAt(int offset, ReadOnlySpan<byte> source)
```

Parameters:

`offset` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The first native payload byte to replace.

`source` — [ReadOnlySpan&lt;byte&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The replacement bytes.

<a id="member-69e029d7a3591a4a"></a>

### WriteByte(byte)

Appends one byte, including zero.

```csharp
public override void WriteByte(byte value)
```

Parameters:

`value` — [byte](https://learn.microsoft.com/dotnet/api/system.byte)

The byte to append.
