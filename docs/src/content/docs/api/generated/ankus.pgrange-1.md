---
title: 'PgRange<T>'
description: 'API reference for Ankus.PgRange<T>.'
slug: api/ankus.pgrange-1
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns the bounds of a built-in PostgreSQL range. A null reference is SQL NULL; a parameterless instance is empty.
Construction retains the requested bounds. PostgreSQL validates and canonicalizes them on datum conversion.

```csharp
public sealed class PgRange<T> : IEquatable<PgRange<T>> where T : struct
```

Type parameters:

`T`

Int32, Int64, PgNumeric/Decimal, PgDate/DateOnly, PgTimestamp/DateTime, or PgTimestampTz/DateTimeOffset.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IEquatable&lt;PgRange&lt;T&gt;&gt;](https://learn.microsoft.com/dotnet/api/system.iequatable-1)

## Constructors

<a id="member-fa9f1241ac5ccff4"></a>

### PgRange()

Creates an empty range of the supported bound type.

```csharp
public PgRange()
```

<a id="member-0329931d217bd2a6"></a>

### PgRange(T?, T?, bool, bool)

Creates a bounded or unbounded range without backend access. Null bounds mean unbounded ends.

```csharp
public PgRange(T? lower, T? upper, bool lowerInclusive = true, bool upperInclusive = false)
```

Parameters:

`lower` — <code>T?</code>

The lower value, or null for an unbounded end.

`upper` — <code>T?</code>

The upper value, or null for an unbounded end.

`lowerInclusive` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the lower value is included; ignored when unbounded.

`upperInclusive` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the upper value is included; ignored when unbounded.


## Methods

<a id="member-6fd74ccf83ddd147"></a>

### Canonicalize()

Validates and canonicalizes the bounds using PostgreSQL on the active backend.

```csharp
public PgRange<T> Canonicalize()
```

Returns: [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The canonical range, which can be empty even when this instance is not.

<a id="member-f8ad24aa6204e0ee"></a>

### Contains(PgRange&lt;T&gt;)

Tests whether this range contains another range on the active backend.

```csharp
public bool Contains(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The candidate range.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether it is contained.

<a id="member-082303cc30f1d07b"></a>

### Contains(T)

Tests value containment using PostgreSQL's subtype ordering and canonical range semantics.

```csharp
public bool Contains(T value)
```

Parameters:

`value` — <code>T</code>

The candidate value.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the range contains the value.

<a id="member-a38a4c1caa178a7e"></a>

### Equals(PgRange&lt;T&gt;?)

Compares stored bounds and inclusion flags without backend access or canonicalization.

```csharp
public bool Equals(PgRange<T>? other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The other range.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether their stored representations are equal.

<a id="member-466332c96002f5eb"></a>

### Equals(object?)

```csharp
public override bool Equals(object? obj)
```

Parameters:

`obj` — [object](https://learn.microsoft.com/dotnet/api/system.object)

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-80bd18cce2265abd"></a>

### Except(PgRange&lt;T&gt;)

Subtracts a range. PostgreSQL raises an error if the result would require two separate ranges.

```csharp
public PgRange<T> Except(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The range to subtract.

Returns: [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The canonical difference.

<a id="member-33b5bd475bcb7b98"></a>

### GetHashCode()

```csharp
public override int GetHashCode()
```

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-c0c0d3697a38b549"></a>

### Intersect(PgRange&lt;T&gt;)

Intersects two ranges on the active backend.

```csharp
public PgRange<T> Intersect(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The other range.

Returns: [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The intersection, possibly empty.

<a id="member-168eba0bca1ae4b0"></a>

### IsAdjacentTo(PgRange&lt;T&gt;)

Tests whether two ranges are adjacent on the active backend.

```csharp
public bool IsAdjacentTo(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The other range.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the ranges are adjacent.

<a id="member-19c9ff3a76a695a9"></a>

### Merge(PgRange&lt;T&gt;)

Returns the smallest range spanning both operands, including any gap between them.

```csharp
public PgRange<T> Merge(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The other range.

Returns: [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The spanning range.

<a id="member-8b48f6d24c30b0a1"></a>

### Overlaps(PgRange&lt;T&gt;)

Tests whether two ranges overlap on the active backend.

```csharp
public bool Overlaps(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The other range.

Returns: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether the ranges overlap.

<a id="member-29f68dec74a0f351"></a>

### ToPostgresString()

Formats the canonical range using PostgreSQL on the active backend, including session temporal settings.

```csharp
public string ToPostgresString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

The PostgreSQL range text.

<a id="member-60ff031ea8a544ed"></a>

### ToString()

Describes the stored bounds without backend access or canonicalization. Use ToPostgresString for SQL input text.

```csharp
public override string ToString()
```

Returns: [string](https://learn.microsoft.com/dotnet/api/system.string)

A diagnostic representation using the bound types' formatting.

<a id="member-81392a6e29721b86"></a>

### Union(PgRange&lt;T&gt;)

Unites overlapping or adjacent ranges. PostgreSQL raises an error if the result would be disjoint.

```csharp
public PgRange<T> Union(PgRange<T> other)
```

Parameters:

`other` — [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The other range.

Returns: [PgRange&lt;T&gt;](/api/ankus.pgrange-1/)

The canonical union.


## Properties

<a id="member-82253ee77d7333a4"></a>

### IsEmpty

Gets whether this value represents an empty range rather than an interval with bounds.

```csharp
public bool IsEmpty { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-6b3f9e7ee55d7e01"></a>

### IsUnbounded

Gets whether the nonempty range has no lower or upper bound.

```csharp
public bool IsUnbounded { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-40d0deaed129a8ea"></a>

### Lower

Gets the lower bound, or null for an empty range or unbounded lower end.

```csharp
public T? Lower { get; }
```

Value: <code>T?</code>

<a id="member-7f77beb7540d284f"></a>

### LowerInclusive

Gets whether the present lower bound is included, including an explicit subtype infinity value.

```csharp
public bool LowerInclusive { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-765646e641917786"></a>

### Upper

Gets the upper bound, or null for an empty range or unbounded upper end.

```csharp
public T? Upper { get; }
```

Value: <code>T?</code>

<a id="member-5d5551250ad4bbcb"></a>

### UpperInclusive

Gets whether the present upper bound is included, including an explicit subtype infinity value.

```csharp
public bool UpperInclusive { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)
