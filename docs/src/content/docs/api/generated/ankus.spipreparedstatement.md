---
title: 'SpiPreparedStatement'
description: 'API reference for Ankus.SpiPreparedStatement.'
slug: api/ankus.spipreparedstatement
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns a reusable PostgreSQL SPI plan. Execution and disposal require the owning backend thread.
Use a using declaration for local statements; explicitly dispose cached statements when replacing them.
Statements created by SpiSession expire with that session unless Keep transfers ownership out of the scope.

```csharp
public sealed class SpiPreparedStatement : IDisposable
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IDisposable](https://learn.microsoft.com/dotnet/api/system.idisposable)

## Properties

<a id="member-c8173fe7772f60ac"></a>

### CommandText

Gets the SQL text used to prepare this statement.

```csharp
public string CommandText { get; }
```

Value: [string](https://learn.microsoft.com/dotnet/api/system.string)

<a id="member-6eab79c28a60e484"></a>

### ParameterCount

Gets the number of positional parameters required by this statement.

```csharp
public int ParameterCount { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)


## Methods

<a id="member-637867acf5d8a681"></a>

### Dispose()

Frees the native plan through a guarded backend call. Repeated disposal is harmless.
PostgreSQL APIs cannot run on the finalizer thread. Independent plans require explicit disposal;
session-owned plans are also released when their session ends.

```csharp
public void Dispose()
```

<a id="member-9372987e0a9683e6"></a>

### Execute(params ReadOnlySpan&lt;SpiParameter&gt;)

Executes the statement and returns the final command's processed-row count.

```csharp
public long Execute(params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values whose PostgreSQL types must match the declared parameter types.

Returns: [long](https://learn.microsoft.com/dotnet/api/system.int64)

The processed-row count.

<a id="member-2e17af102c2c384d"></a>

### ExecuteScalar&lt;T&gt;(params ReadOnlySpan&lt;SpiParameter&gt;)

Reads the first cell without limiting command execution or applying implicit type conversions.
SQL NULL or an absent cell requires a nullable value type or reference type.

```csharp
public T ExecuteScalar<T>(params ReadOnlySpan<SpiParameter> parameters)
```

Type parameters:

`T`

The expected managed result type.

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: <code>T</code>

The scalar value.

<a id="member-239e43263637d9db"></a>

### ExecuteScalars&lt;TFirst, TSecond&gt;(params ReadOnlySpan&lt;SpiParameter&gt;)

Reads the first two columns of the first row without limiting command execution.
SQL NULL or an empty result requires nullable value types or reference types.

```csharp
public (TFirst First, TSecond Second) ExecuteScalars<TFirst, TSecond>(params ReadOnlySpan<SpiParameter> parameters)
```

Type parameters:

`TFirst`

The first column's managed type.

`TSecond`

The second column's managed type.

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: <code>(TFirst First, TSecond Second)</code>

The first two values from the final statement, without implicit type conversions.

Exceptions:

- [InvalidOperationException](https://learn.microsoft.com/dotnet/api/system.invalidoperationexception): A returned row has fewer than two columns, or SQL NULL is read as a non-nullable value type.
- [InvalidCastException](https://learn.microsoft.com/dotnet/api/system.invalidcastexception): A column cannot be read as its requested managed type.

<a id="member-9683e69519896742"></a>

### ExecuteScalars&lt;TFirst, TSecond, TThird&gt;(params ReadOnlySpan&lt;SpiParameter&gt;)

Reads the first three columns of the first row without limiting command execution.
SQL NULL or an empty result requires nullable value types or reference types.

```csharp
public (TFirst First, TSecond Second, TThird Third) ExecuteScalars<TFirst, TSecond, TThird>(params ReadOnlySpan<SpiParameter> parameters)
```

Type parameters:

`TFirst`

The first column's managed type.

`TSecond`

The second column's managed type.

`TThird`

The third column's managed type.

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: <code>(TFirst First, TSecond Second, TThird Third)</code>

The first three values from the final statement, without implicit type conversions.

Exceptions:

- [InvalidOperationException](https://learn.microsoft.com/dotnet/api/system.invalidoperationexception): A returned row has fewer than three columns, or SQL NULL is read as a non-nullable value type.
- [InvalidCastException](https://learn.microsoft.com/dotnet/api/system.invalidcastexception): A column cannot be read as its requested managed type.

<a id="member-1bc56f024c323ae3"></a>

### Keep()

Retains this statement beyond its session's lifetime and returns the same explicitly disposable owner.
Calling Keep on an already retained statement is harmless.

```csharp
public SpiPreparedStatement Keep()
```

Returns: [SpiPreparedStatement](/api/ankus.spipreparedstatement/)

This statement, now independent of its original session.

<a id="member-9ac4c6644ca511df"></a>

### OpenCursor(bool, params ReadOnlySpan&lt;SpiParameter&gt;)

Opens a cursor from this plan with explicit read-only execution mode.

```csharp
public SpiCursor OpenCursor(bool readOnly, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`readOnly` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether PostgreSQL should use read-only execution.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: [SpiCursor](/api/ankus.spicursor/)

An owned cursor independent of the prepared statement's lifetime.

<a id="member-5a215d0d616df381"></a>

### OpenCursor(params ReadOnlySpan&lt;SpiParameter&gt;)

Opens a transaction-bound cursor from this plan. The cursor remains valid after the plan is disposed.

```csharp
public SpiCursor OpenCursor(params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: [SpiCursor](/api/ankus.spicursor/)

An owned cursor.

<a id="member-c59730fabcd10552"></a>

### Query(bool, int, params ReadOnlySpan&lt;SpiParameter&gt;)

Executes the statement with explicit read-only mode and row limit.

```csharp
public SpiResult Query(bool readOnly, int limit, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`readOnly` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether PostgreSQL should use read-only SPI execution.

`limit` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The maximum returned rows, or zero for no limit.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: [SpiResult](/api/ankus.spiresult/)

The managed result.

<a id="member-b3b7200b60284af9"></a>

### Query(params ReadOnlySpan&lt;SpiParameter&gt;)

Executes the statement and materializes its result rows and metadata.

```csharp
public SpiResult Query(params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: [SpiResult](/api/ankus.spiresult/)

The managed result.

<a id="member-bd31ffee346830be"></a>

### QueryRaw(bool, int, params ReadOnlySpan&lt;SpiParameter&gt;)

Copies raw native results with explicit snapshot mode and row limit.

```csharp
public SpiRawResult QueryRaw(bool readOnly, int limit, params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`readOnly` — [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

Whether PostgreSQL should use read-only SPI execution.

`limit` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The maximum returned rows, or zero for no limit.

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: [SpiRawResult](/api/ankus.spirawresult/)

A result to dispose before leaving the backend callback.

<a id="member-b21caebc18e6afaa"></a>

### QueryRaw(params ReadOnlySpan&lt;SpiParameter&gt;)

Copies native result values without requiring a managed mapping for their PostgreSQL types.

```csharp
public SpiRawResult QueryRaw(params ReadOnlySpan<SpiParameter> parameters)
```

Parameters:

`parameters` — [ReadOnlySpan&lt;SpiParameter&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

Values matching the declared parameter types.

Returns: [SpiRawResult](/api/ankus.spirawresult/)

An owned result that survives this plan and expires on disposal or callback-context cleanup.
