---
title: 'PgSqlStates'
description: 'API reference for Ankus.PgSqlStates.'
slug: api/ankus.pgsqlstates
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Provides named PostgreSQL SQLSTATE strings for error reporting, diagnostics and exception filters.

```csharp
public static class PgSqlStates
```

Includes the union of the PostgreSQL 13 through 18 and 19 beta catalogs, including aliases and retired names.
A constant does not imply that its associated server feature exists in every PostgreSQL version.
These names supplement the string-based diagnostic APIs; extension-specific SQLSTATE strings remain supported.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Fields

<a id="member-321ba9575d371c64"></a>

### ActiveSqlTransaction

The PostgreSQL <code>active_sql_transaction</code> SQLSTATE (<code>25001</code>).

```csharp
public const string ActiveSqlTransaction = "25001"
```

PostgreSQL macro <code>ERRCODE_ACTIVE_SQL_TRANSACTION</code>.

<a id="member-572834b4f322a084"></a>

### AdminShutdown

The PostgreSQL <code>admin_shutdown</code> SQLSTATE (<code>57P01</code>).

```csharp
public const string AdminShutdown = "57P01"
```

PostgreSQL macro <code>ERRCODE_ADMIN_SHUTDOWN</code>.

<a id="member-e2b92dc1a2dc6f09"></a>

### AmbiguousAlias

The PostgreSQL <code>ambiguous_alias</code> SQLSTATE (<code>42P09</code>).

```csharp
public const string AmbiguousAlias = "42P09"
```

PostgreSQL macro <code>ERRCODE_AMBIGUOUS_ALIAS</code>.

<a id="member-4fbb675787d52af0"></a>

### AmbiguousColumn

The PostgreSQL <code>ambiguous_column</code> SQLSTATE (<code>42702</code>).

```csharp
public const string AmbiguousColumn = "42702"
```

PostgreSQL macro <code>ERRCODE_AMBIGUOUS_COLUMN</code>.

<a id="member-653abf6e955c7c95"></a>

### AmbiguousFunction

The PostgreSQL <code>ambiguous_function</code> SQLSTATE (<code>42725</code>).

```csharp
public const string AmbiguousFunction = "42725"
```

PostgreSQL macro <code>ERRCODE_AMBIGUOUS_FUNCTION</code>.

<a id="member-dfe3eda9540caaae"></a>

### AmbiguousParameter

The PostgreSQL <code>ambiguous_parameter</code> SQLSTATE (<code>42P08</code>).

```csharp
public const string AmbiguousParameter = "42P08"
```

PostgreSQL macro <code>ERRCODE_AMBIGUOUS_PARAMETER</code>.

<a id="member-4b8650da1df81d84"></a>

### ArrayElementError

The PostgreSQL <code>array_element_error</code> SQLSTATE (<code>2202E</code>).

```csharp
public const string ArrayElementError = "2202E"
```

PostgreSQL macro <code>ERRCODE_ARRAY_ELEMENT_ERROR</code>.

<a id="member-6849b46e21a32a42"></a>

### ArraySubscriptError

The PostgreSQL <code>array_subscript_error</code> SQLSTATE (<code>2202E</code>).

```csharp
public const string ArraySubscriptError = "2202E"
```

PostgreSQL macro <code>ERRCODE_ARRAY_SUBSCRIPT_ERROR</code>.

<a id="member-00b2bd4450d9b8f4"></a>

### AssertFailure

The PostgreSQL <code>assert_failure</code> SQLSTATE (<code>P0004</code>).

```csharp
public const string AssertFailure = "P0004"
```

PostgreSQL macro <code>ERRCODE_ASSERT_FAILURE</code>.

<a id="member-3e49cb4e1a729b61"></a>

### BadCopyFileFormat

The PostgreSQL <code>bad_copy_file_format</code> SQLSTATE (<code>22P04</code>).

```csharp
public const string BadCopyFileFormat = "22P04"
```

PostgreSQL macro <code>ERRCODE_BAD_COPY_FILE_FORMAT</code>.

<a id="member-31aebbef19294e14"></a>

### BranchTransactionAlreadyActive

The PostgreSQL <code>branch_transaction_already_active</code> SQLSTATE (<code>25002</code>).

```csharp
public const string BranchTransactionAlreadyActive = "25002"
```

PostgreSQL macro <code>ERRCODE_BRANCH_TRANSACTION_ALREADY_ACTIVE</code>.

<a id="member-f10493da57737181"></a>

### CannotCoerce

The PostgreSQL <code>cannot_coerce</code> SQLSTATE (<code>42846</code>).

```csharp
public const string CannotCoerce = "42846"
```

PostgreSQL macro <code>ERRCODE_CANNOT_COERCE</code>.

<a id="member-53fb8b598eebe3e8"></a>

### CannotConnectNow

The PostgreSQL <code>cannot_connect_now</code> SQLSTATE (<code>57P03</code>).

```csharp
public const string CannotConnectNow = "57P03"
```

PostgreSQL macro <code>ERRCODE_CANNOT_CONNECT_NOW</code>.

<a id="member-533ee524f4778e04"></a>

### CantChangeRuntimeParam

The PostgreSQL <code>cant_change_runtime_param</code> SQLSTATE (<code>55P02</code>).

```csharp
public const string CantChangeRuntimeParam = "55P02"
```

PostgreSQL macro <code>ERRCODE_CANT_CHANGE_RUNTIME_PARAM</code>.

<a id="member-57c56e2ede34feed"></a>

### CardinalityViolation

The PostgreSQL <code>cardinality_violation</code> SQLSTATE (<code>21000</code>).

```csharp
public const string CardinalityViolation = "21000"
```

PostgreSQL macro <code>ERRCODE_CARDINALITY_VIOLATION</code>.

<a id="member-8bd3c52f8b9f4297"></a>

### CaseNotFound

The PostgreSQL <code>case_not_found</code> SQLSTATE (<code>20000</code>).

```csharp
public const string CaseNotFound = "20000"
```

PostgreSQL macro <code>ERRCODE_CASE_NOT_FOUND</code>.

<a id="member-355de55de2da6c43"></a>

### CharacterNotInRepertoire

The PostgreSQL <code>character_not_in_repertoire</code> SQLSTATE (<code>22021</code>).

```csharp
public const string CharacterNotInRepertoire = "22021"
```

PostgreSQL macro <code>ERRCODE_CHARACTER_NOT_IN_REPERTOIRE</code>.

<a id="member-020c3d010a63588e"></a>

### CheckViolation

The PostgreSQL <code>check_violation</code> SQLSTATE (<code>23514</code>).

```csharp
public const string CheckViolation = "23514"
```

PostgreSQL macro <code>ERRCODE_CHECK_VIOLATION</code>.

<a id="member-c1ad41d9b876c9f6"></a>

### CollationMismatch

The PostgreSQL <code>collation_mismatch</code> SQLSTATE (<code>42P21</code>).

```csharp
public const string CollationMismatch = "42P21"
```

PostgreSQL macro <code>ERRCODE_COLLATION_MISMATCH</code>.

<a id="member-cff96dd02c0d9abc"></a>

### ConfigFileError

The PostgreSQL <code>config_file_error</code> SQLSTATE (<code>F0000</code>).

```csharp
public const string ConfigFileError = "F0000"
```

PostgreSQL macro <code>ERRCODE_CONFIG_FILE_ERROR</code>.

<a id="member-0f9b73639f96fef1"></a>

### ConfigurationLimitExceeded

The PostgreSQL <code>configuration_limit_exceeded</code> SQLSTATE (<code>53400</code>).

```csharp
public const string ConfigurationLimitExceeded = "53400"
```

PostgreSQL macro <code>ERRCODE_CONFIGURATION_LIMIT_EXCEEDED</code>.

<a id="member-d89446388e7b6e7c"></a>

### ConnectionDoesNotExist

The PostgreSQL <code>connection_does_not_exist</code> SQLSTATE (<code>08003</code>).

```csharp
public const string ConnectionDoesNotExist = "08003"
```

PostgreSQL macro <code>ERRCODE_CONNECTION_DOES_NOT_EXIST</code>.

<a id="member-54700e6f95f4c7bf"></a>

### ConnectionException

The PostgreSQL <code>connection_exception</code> SQLSTATE (<code>08000</code>).

```csharp
public const string ConnectionException = "08000"
```

PostgreSQL macro <code>ERRCODE_CONNECTION_EXCEPTION</code>.

<a id="member-f16be81958dcd127"></a>

### ConnectionFailure

The PostgreSQL <code>connection_failure</code> SQLSTATE (<code>08006</code>).

```csharp
public const string ConnectionFailure = "08006"
```

PostgreSQL macro <code>ERRCODE_CONNECTION_FAILURE</code>.

<a id="member-cea5baa3ff330722"></a>

### CrashShutdown

The PostgreSQL <code>crash_shutdown</code> SQLSTATE (<code>57P02</code>).

```csharp
public const string CrashShutdown = "57P02"
```

PostgreSQL macro <code>ERRCODE_CRASH_SHUTDOWN</code>.

<a id="member-a8c37506aef7b81e"></a>

### DataCorrupted

The PostgreSQL <code>data_corrupted</code> SQLSTATE (<code>XX001</code>).

```csharp
public const string DataCorrupted = "XX001"
```

PostgreSQL macro <code>ERRCODE_DATA_CORRUPTED</code>.

<a id="member-aac7b69e1695a806"></a>

### DataException

The PostgreSQL <code>data_exception</code> SQLSTATE (<code>22000</code>).

```csharp
public const string DataException = "22000"
```

PostgreSQL macro <code>ERRCODE_DATA_EXCEPTION</code>.

<a id="member-af4b3e6a7027e459"></a>

### DatabaseDropped

The PostgreSQL <code>database_dropped</code> SQLSTATE (<code>57P04</code>).

```csharp
public const string DatabaseDropped = "57P04"
```

PostgreSQL macro <code>ERRCODE_DATABASE_DROPPED</code>.

<a id="member-9bb92cbda733dfae"></a>

### DatatypeMismatch

The PostgreSQL <code>datatype_mismatch</code> SQLSTATE (<code>42804</code>).

```csharp
public const string DatatypeMismatch = "42804"
```

PostgreSQL macro <code>ERRCODE_DATATYPE_MISMATCH</code>.

<a id="member-6abdf9e048652a56"></a>

### DatetimeFieldOverflow

The PostgreSQL <code>datetime_field_overflow</code> SQLSTATE (<code>22008</code>).

```csharp
public const string DatetimeFieldOverflow = "22008"
```

PostgreSQL macro <code>ERRCODE_DATETIME_FIELD_OVERFLOW</code>.

<a id="member-f08fad71963c4cd2"></a>

### DatetimeValueOutOfRange

The PostgreSQL <code>datetime_value_out_of_range</code> SQLSTATE (<code>22008</code>).

```csharp
public const string DatetimeValueOutOfRange = "22008"
```

PostgreSQL macro <code>ERRCODE_DATETIME_VALUE_OUT_OF_RANGE</code>.

<a id="member-f73dfa4d485ecb36"></a>

### DeadlockDetected

The PostgreSQL <code>deadlock_detected</code> SQLSTATE (<code>40P01</code>).

```csharp
public const string DeadlockDetected = "40P01"
```

PostgreSQL macro <code>ERRCODE_T_R_DEADLOCK_DETECTED</code>.

<a id="member-cc79d430d645fae4"></a>

### DependentObjectsStillExist

The PostgreSQL <code>dependent_objects_still_exist</code> SQLSTATE (<code>2BP01</code>).

```csharp
public const string DependentObjectsStillExist = "2BP01"
```

PostgreSQL macro <code>ERRCODE_DEPENDENT_OBJECTS_STILL_EXIST</code>.

<a id="member-73d34eac514999c0"></a>

### DependentPrivilegeDescriptorsStillExist

The PostgreSQL <code>dependent_privilege_descriptors_still_exist</code> SQLSTATE (<code>2B000</code>).

```csharp
public const string DependentPrivilegeDescriptorsStillExist = "2B000"
```

PostgreSQL macro <code>ERRCODE_DEPENDENT_PRIVILEGE_DESCRIPTORS_STILL_EXIST</code>.

<a id="member-e214373a95fc3b44"></a>

### DiagnosticsException

The PostgreSQL <code>diagnostics_exception</code> SQLSTATE (<code>0Z000</code>).

```csharp
public const string DiagnosticsException = "0Z000"
```

PostgreSQL macro <code>ERRCODE_DIAGNOSTICS_EXCEPTION</code>.

<a id="member-dfc336bf857640fa"></a>

### DiskFull

The PostgreSQL <code>disk_full</code> SQLSTATE (<code>53100</code>).

```csharp
public const string DiskFull = "53100"
```

PostgreSQL macro <code>ERRCODE_DISK_FULL</code>.

<a id="member-74d2570f5e0a6869"></a>

### DivisionByZero

The PostgreSQL <code>division_by_zero</code> SQLSTATE (<code>22012</code>).

```csharp
public const string DivisionByZero = "22012"
```

PostgreSQL macro <code>ERRCODE_DIVISION_BY_ZERO</code>.

<a id="member-93b8967d3b7e7418"></a>

### DuplicateAlias

The PostgreSQL <code>duplicate_alias</code> SQLSTATE (<code>42712</code>).

```csharp
public const string DuplicateAlias = "42712"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_ALIAS</code>.

<a id="member-62e7f20bb4ca83f2"></a>

### DuplicateColumn

The PostgreSQL <code>duplicate_column</code> SQLSTATE (<code>42701</code>).

```csharp
public const string DuplicateColumn = "42701"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_COLUMN</code>.

<a id="member-05e81d989c0d0c44"></a>

### DuplicateCursor

The PostgreSQL <code>duplicate_cursor</code> SQLSTATE (<code>42P03</code>).

```csharp
public const string DuplicateCursor = "42P03"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_CURSOR</code>.

<a id="member-86ad3de1b8ff4706"></a>

### DuplicateDatabase

The PostgreSQL <code>duplicate_database</code> SQLSTATE (<code>42P04</code>).

```csharp
public const string DuplicateDatabase = "42P04"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_DATABASE</code>.

<a id="member-0d1486d5de52a4cb"></a>

### DuplicateFile

The PostgreSQL <code>duplicate_file</code> SQLSTATE (<code>58P02</code>).

```csharp
public const string DuplicateFile = "58P02"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_FILE</code>.

<a id="member-8601493952021086"></a>

### DuplicateFunction

The PostgreSQL <code>duplicate_function</code> SQLSTATE (<code>42723</code>).

```csharp
public const string DuplicateFunction = "42723"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_FUNCTION</code>.

<a id="member-2d524f4c31e7c9f1"></a>

### DuplicateJsonObjectKeyValue

The PostgreSQL <code>duplicate_json_object_key_value</code> SQLSTATE (<code>22030</code>).

```csharp
public const string DuplicateJsonObjectKeyValue = "22030"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_JSON_OBJECT_KEY_VALUE</code>.

<a id="member-9c7181dbbbe0e0db"></a>

### DuplicateObject

The PostgreSQL <code>duplicate_object</code> SQLSTATE (<code>42710</code>).

```csharp
public const string DuplicateObject = "42710"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_OBJECT</code>.

<a id="member-d2701830d251a4ce"></a>

### DuplicatePreparedStatement

The PostgreSQL <code>duplicate_prepared_statement</code> SQLSTATE (<code>42P05</code>).

```csharp
public const string DuplicatePreparedStatement = "42P05"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_PSTATEMENT</code>.

<a id="member-be0146ef704e48f7"></a>

### DuplicateSchema

The PostgreSQL <code>duplicate_schema</code> SQLSTATE (<code>42P06</code>).

```csharp
public const string DuplicateSchema = "42P06"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_SCHEMA</code>.

<a id="member-af6a6453b28856b8"></a>

### DuplicateTable

The PostgreSQL <code>duplicate_table</code> SQLSTATE (<code>42P07</code>).

```csharp
public const string DuplicateTable = "42P07"
```

PostgreSQL macro <code>ERRCODE_DUPLICATE_TABLE</code>.

<a id="member-54003fec1f4adf97"></a>

### ErrorInAssignment

The PostgreSQL <code>error_in_assignment</code> SQLSTATE (<code>22005</code>).

```csharp
public const string ErrorInAssignment = "22005"
```

PostgreSQL macro <code>ERRCODE_ERROR_IN_ASSIGNMENT</code>.

<a id="member-56ae76adf04d3c34"></a>

### EscapeCharacterConflict

The PostgreSQL <code>escape_character_conflict</code> SQLSTATE (<code>2200B</code>).

```csharp
public const string EscapeCharacterConflict = "2200B"
```

PostgreSQL macro <code>ERRCODE_ESCAPE_CHARACTER_CONFLICT</code>.

<a id="member-cbeb68c1076c84ca"></a>

### ExclusionViolation

The PostgreSQL <code>exclusion_violation</code> SQLSTATE (<code>23P01</code>).

```csharp
public const string ExclusionViolation = "23P01"
```

PostgreSQL macro <code>ERRCODE_EXCLUSION_VIOLATION</code>.

<a id="member-ba8f9ab39e4b8b66"></a>

### ExternalRoutineContainingSqlNotPermitted

The PostgreSQL <code>containing_sql_not_permitted</code> SQLSTATE (<code>38001</code>).

```csharp
public const string ExternalRoutineContainingSqlNotPermitted = "38001"
```

PostgreSQL macro <code>ERRCODE_E_R_E_CONTAINING_SQL_NOT_PERMITTED</code>.

<a id="member-059972299bae3ff7"></a>

### ExternalRoutineException

The PostgreSQL <code>external_routine_exception</code> SQLSTATE (<code>38000</code>).

```csharp
public const string ExternalRoutineException = "38000"
```

PostgreSQL macro <code>ERRCODE_EXTERNAL_ROUTINE_EXCEPTION</code>.

<a id="member-35fa1d7e6253a40a"></a>

### ExternalRoutineInvocationEventTriggerProtocolViolated

The PostgreSQL <code>event_trigger_protocol_violated</code> SQLSTATE (<code>39P03</code>).

```csharp
public const string ExternalRoutineInvocationEventTriggerProtocolViolated = "39P03"
```

PostgreSQL macro <code>ERRCODE_E_R_I_E_EVENT_TRIGGER_PROTOCOL_VIOLATED</code>.

<a id="member-6768e2864d2e21c7"></a>

### ExternalRoutineInvocationException

The PostgreSQL <code>external_routine_invocation_exception</code> SQLSTATE (<code>39000</code>).

```csharp
public const string ExternalRoutineInvocationException = "39000"
```

PostgreSQL macro <code>ERRCODE_EXTERNAL_ROUTINE_INVOCATION_EXCEPTION</code>.

<a id="member-66c9d543edc3f3ac"></a>

### ExternalRoutineInvocationInvalidSqlStateReturned

The PostgreSQL <code>invalid_sqlstate_returned</code> SQLSTATE (<code>39001</code>).

```csharp
public const string ExternalRoutineInvocationInvalidSqlStateReturned = "39001"
```

PostgreSQL macro <code>ERRCODE_E_R_I_E_INVALID_SQLSTATE_RETURNED</code>.

<a id="member-d9e9dc198ae7b567"></a>

### ExternalRoutineInvocationNullValueNotAllowed

The PostgreSQL <code>null_value_not_allowed</code> SQLSTATE (<code>39004</code>).

```csharp
public const string ExternalRoutineInvocationNullValueNotAllowed = "39004"
```

PostgreSQL macro <code>ERRCODE_E_R_I_E_NULL_VALUE_NOT_ALLOWED</code>.

<a id="member-07dd0400c7ef41c7"></a>

### ExternalRoutineInvocationSrfProtocolViolated

The PostgreSQL <code>srf_protocol_violated</code> SQLSTATE (<code>39P02</code>).

```csharp
public const string ExternalRoutineInvocationSrfProtocolViolated = "39P02"
```

PostgreSQL macro <code>ERRCODE_E_R_I_E_SRF_PROTOCOL_VIOLATED</code>.

<a id="member-30948eff565ea9b3"></a>

### ExternalRoutineInvocationTriggerProtocolViolated

The PostgreSQL <code>trigger_protocol_violated</code> SQLSTATE (<code>39P01</code>).

```csharp
public const string ExternalRoutineInvocationTriggerProtocolViolated = "39P01"
```

PostgreSQL macro <code>ERRCODE_E_R_I_E_TRIGGER_PROTOCOL_VIOLATED</code>.

<a id="member-21da9eca2fa465b1"></a>

### ExternalRoutineModifyingSqlDataNotPermitted

The PostgreSQL <code>modifying_sql_data_not_permitted</code> SQLSTATE (<code>38002</code>).

```csharp
public const string ExternalRoutineModifyingSqlDataNotPermitted = "38002"
```

PostgreSQL macro <code>ERRCODE_E_R_E_MODIFYING_SQL_DATA_NOT_PERMITTED</code>.

<a id="member-ffe31ca3f9be5739"></a>

### ExternalRoutineProhibitedSqlStatementAttempted

The PostgreSQL <code>prohibited_sql_statement_attempted</code> SQLSTATE (<code>38003</code>).

```csharp
public const string ExternalRoutineProhibitedSqlStatementAttempted = "38003"
```

PostgreSQL macro <code>ERRCODE_E_R_E_PROHIBITED_SQL_STATEMENT_ATTEMPTED</code>.

<a id="member-85448d8ad85dbc01"></a>

### ExternalRoutineReadingSqlDataNotPermitted

The PostgreSQL <code>reading_sql_data_not_permitted</code> SQLSTATE (<code>38004</code>).

```csharp
public const string ExternalRoutineReadingSqlDataNotPermitted = "38004"
```

PostgreSQL macro <code>ERRCODE_E_R_E_READING_SQL_DATA_NOT_PERMITTED</code>.

<a id="member-e125a835c567d53c"></a>

### FdwColumnNameNotFound

The PostgreSQL <code>fdw_column_name_not_found</code> SQLSTATE (<code>HV005</code>).

```csharp
public const string FdwColumnNameNotFound = "HV005"
```

PostgreSQL macro <code>ERRCODE_FDW_COLUMN_NAME_NOT_FOUND</code>.

<a id="member-95a6ab7a2bd4fa65"></a>

### FdwDynamicParameterValueNeeded

The PostgreSQL <code>fdw_dynamic_parameter_value_needed</code> SQLSTATE (<code>HV002</code>).

```csharp
public const string FdwDynamicParameterValueNeeded = "HV002"
```

PostgreSQL macro <code>ERRCODE_FDW_DYNAMIC_PARAMETER_VALUE_NEEDED</code>.

<a id="member-2ed1e65e768a030f"></a>

### FdwError

The PostgreSQL <code>fdw_error</code> SQLSTATE (<code>HV000</code>).

```csharp
public const string FdwError = "HV000"
```

PostgreSQL macro <code>ERRCODE_FDW_ERROR</code>.

<a id="member-c4401c83a20b488c"></a>

### FdwFunctionSequenceError

The PostgreSQL <code>fdw_function_sequence_error</code> SQLSTATE (<code>HV010</code>).

```csharp
public const string FdwFunctionSequenceError = "HV010"
```

PostgreSQL macro <code>ERRCODE_FDW_FUNCTION_SEQUENCE_ERROR</code>.

<a id="member-29915910de093a70"></a>

### FdwInconsistentDescriptorInformation

The PostgreSQL <code>fdw_inconsistent_descriptor_information</code> SQLSTATE (<code>HV021</code>).

```csharp
public const string FdwInconsistentDescriptorInformation = "HV021"
```

PostgreSQL macro <code>ERRCODE_FDW_INCONSISTENT_DESCRIPTOR_INFORMATION</code>.

<a id="member-49d377e52863fe22"></a>

### FdwInvalidAttributeValue

The PostgreSQL <code>fdw_invalid_attribute_value</code> SQLSTATE (<code>HV024</code>).

```csharp
public const string FdwInvalidAttributeValue = "HV024"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_ATTRIBUTE_VALUE</code>.

<a id="member-06685278a91ef1b2"></a>

### FdwInvalidColumnName

The PostgreSQL <code>fdw_invalid_column_name</code> SQLSTATE (<code>HV007</code>).

```csharp
public const string FdwInvalidColumnName = "HV007"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_COLUMN_NAME</code>.

<a id="member-b2342e0e1b98ccdf"></a>

### FdwInvalidColumnNumber

The PostgreSQL <code>fdw_invalid_column_number</code> SQLSTATE (<code>HV008</code>).

```csharp
public const string FdwInvalidColumnNumber = "HV008"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_COLUMN_NUMBER</code>.

<a id="member-6d8651abc177d763"></a>

### FdwInvalidDataType

The PostgreSQL <code>fdw_invalid_data_type</code> SQLSTATE (<code>HV004</code>).

```csharp
public const string FdwInvalidDataType = "HV004"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_DATA_TYPE</code>.

<a id="member-e67ed3bea72afc82"></a>

### FdwInvalidDataTypeDescriptors

The PostgreSQL <code>fdw_invalid_data_type_descriptors</code> SQLSTATE (<code>HV006</code>).

```csharp
public const string FdwInvalidDataTypeDescriptors = "HV006"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_DATA_TYPE_DESCRIPTORS</code>.

<a id="member-07487132e9080272"></a>

### FdwInvalidDescriptorFieldIdentifier

The PostgreSQL <code>fdw_invalid_descriptor_field_identifier</code> SQLSTATE (<code>HV091</code>).

```csharp
public const string FdwInvalidDescriptorFieldIdentifier = "HV091"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_DESCRIPTOR_FIELD_IDENTIFIER</code>.

<a id="member-4a0fc08ab3fd12ac"></a>

### FdwInvalidHandle

The PostgreSQL <code>fdw_invalid_handle</code> SQLSTATE (<code>HV00B</code>).

```csharp
public const string FdwInvalidHandle = "HV00B"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_HANDLE</code>.

<a id="member-a4ed91b71f8a9b7a"></a>

### FdwInvalidOptionIndex

The PostgreSQL <code>fdw_invalid_option_index</code> SQLSTATE (<code>HV00C</code>).

```csharp
public const string FdwInvalidOptionIndex = "HV00C"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_OPTION_INDEX</code>.

<a id="member-1a748f915e5856eb"></a>

### FdwInvalidOptionName

The PostgreSQL <code>fdw_invalid_option_name</code> SQLSTATE (<code>HV00D</code>).

```csharp
public const string FdwInvalidOptionName = "HV00D"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_OPTION_NAME</code>.

<a id="member-a5ecee17ecccaa6f"></a>

### FdwInvalidStringFormat

The PostgreSQL <code>fdw_invalid_string_format</code> SQLSTATE (<code>HV00A</code>).

```csharp
public const string FdwInvalidStringFormat = "HV00A"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_STRING_FORMAT</code>.

<a id="member-aed631546be664a6"></a>

### FdwInvalidStringLengthOrBufferLength

The PostgreSQL <code>fdw_invalid_string_length_or_buffer_length</code> SQLSTATE (<code>HV090</code>).

```csharp
public const string FdwInvalidStringLengthOrBufferLength = "HV090"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_STRING_LENGTH_OR_BUFFER_LENGTH</code>.

<a id="member-84cbcac60741b074"></a>

### FdwInvalidUseOfNullPointer

The PostgreSQL <code>fdw_invalid_use_of_null_pointer</code> SQLSTATE (<code>HV009</code>).

```csharp
public const string FdwInvalidUseOfNullPointer = "HV009"
```

PostgreSQL macro <code>ERRCODE_FDW_INVALID_USE_OF_NULL_POINTER</code>.

<a id="member-27bb8f8f02cfa996"></a>

### FdwNoSchemas

The PostgreSQL <code>fdw_no_schemas</code> SQLSTATE (<code>HV00P</code>).

```csharp
public const string FdwNoSchemas = "HV00P"
```

PostgreSQL macro <code>ERRCODE_FDW_NO_SCHEMAS</code>.

<a id="member-14ba11748e23772a"></a>

### FdwOptionNameNotFound

The PostgreSQL <code>fdw_option_name_not_found</code> SQLSTATE (<code>HV00J</code>).

```csharp
public const string FdwOptionNameNotFound = "HV00J"
```

PostgreSQL macro <code>ERRCODE_FDW_OPTION_NAME_NOT_FOUND</code>.

<a id="member-bd1aab2bf5de5cf5"></a>

### FdwOutOfMemory

The PostgreSQL <code>fdw_out_of_memory</code> SQLSTATE (<code>HV001</code>).

```csharp
public const string FdwOutOfMemory = "HV001"
```

PostgreSQL macro <code>ERRCODE_FDW_OUT_OF_MEMORY</code>.

<a id="member-c22f34df02151e5d"></a>

### FdwReplyHandle

The PostgreSQL <code>fdw_reply_handle</code> SQLSTATE (<code>HV00K</code>).

```csharp
public const string FdwReplyHandle = "HV00K"
```

PostgreSQL macro <code>ERRCODE_FDW_REPLY_HANDLE</code>.

<a id="member-76a4e628336c7984"></a>

### FdwSchemaNotFound

The PostgreSQL <code>fdw_schema_not_found</code> SQLSTATE (<code>HV00Q</code>).

```csharp
public const string FdwSchemaNotFound = "HV00Q"
```

PostgreSQL macro <code>ERRCODE_FDW_SCHEMA_NOT_FOUND</code>.

<a id="member-8246d7cb95599ff2"></a>

### FdwTableNotFound

The PostgreSQL <code>fdw_table_not_found</code> SQLSTATE (<code>HV00R</code>).

```csharp
public const string FdwTableNotFound = "HV00R"
```

PostgreSQL macro <code>ERRCODE_FDW_TABLE_NOT_FOUND</code>.

<a id="member-5a845781ab9ff5ac"></a>

### FdwTooManyHandles

The PostgreSQL <code>fdw_too_many_handles</code> SQLSTATE (<code>HV014</code>).

```csharp
public const string FdwTooManyHandles = "HV014"
```

PostgreSQL macro <code>ERRCODE_FDW_TOO_MANY_HANDLES</code>.

<a id="member-f8e7e0d75503b7f8"></a>

### FdwUnableToCreateExecution

The PostgreSQL <code>fdw_unable_to_create_execution</code> SQLSTATE (<code>HV00L</code>).

```csharp
public const string FdwUnableToCreateExecution = "HV00L"
```

PostgreSQL macro <code>ERRCODE_FDW_UNABLE_TO_CREATE_EXECUTION</code>.

<a id="member-0276ce2fe99f10e0"></a>

### FdwUnableToCreateReply

The PostgreSQL <code>fdw_unable_to_create_reply</code> SQLSTATE (<code>HV00M</code>).

```csharp
public const string FdwUnableToCreateReply = "HV00M"
```

PostgreSQL macro <code>ERRCODE_FDW_UNABLE_TO_CREATE_REPLY</code>.

<a id="member-5a4062d6e29cfd33"></a>

### FdwUnableToEstablishConnection

The PostgreSQL <code>fdw_unable_to_establish_connection</code> SQLSTATE (<code>HV00N</code>).

```csharp
public const string FdwUnableToEstablishConnection = "HV00N"
```

PostgreSQL macro <code>ERRCODE_FDW_UNABLE_TO_ESTABLISH_CONNECTION</code>.

<a id="member-793ea989507b371f"></a>

### FeatureNotSupported

The PostgreSQL <code>feature_not_supported</code> SQLSTATE (<code>0A000</code>).

```csharp
public const string FeatureNotSupported = "0A000"
```

PostgreSQL macro <code>ERRCODE_FEATURE_NOT_SUPPORTED</code>.

<a id="member-5fd75984171119dc"></a>

### FileNameTooLong

The PostgreSQL <code>file_name_too_long</code> SQLSTATE (<code>58P03</code>).

```csharp
public const string FileNameTooLong = "58P03"
```

PostgreSQL macro <code>ERRCODE_FILE_NAME_TOO_LONG</code>. Present in the PostgreSQL 18, 19 beta source catalogs.

<a id="member-47376240efb544c0"></a>

### FloatingPointException

The PostgreSQL <code>floating_point_exception</code> SQLSTATE (<code>22P01</code>).

```csharp
public const string FloatingPointException = "22P01"
```

PostgreSQL macro <code>ERRCODE_FLOATING_POINT_EXCEPTION</code>.

<a id="member-ff79fc7ddf20bd14"></a>

### ForeignKeyViolation

The PostgreSQL <code>foreign_key_violation</code> SQLSTATE (<code>23503</code>).

```csharp
public const string ForeignKeyViolation = "23503"
```

PostgreSQL macro <code>ERRCODE_FOREIGN_KEY_VIOLATION</code>.

<a id="member-d2ac0408791934ad"></a>

### GeneratedAlways

The PostgreSQL <code>generated_always</code> SQLSTATE (<code>428C9</code>).

```csharp
public const string GeneratedAlways = "428C9"
```

PostgreSQL macro <code>ERRCODE_GENERATED_ALWAYS</code>.

<a id="member-658ce6aa78464d6c"></a>

### GroupingError

The PostgreSQL <code>grouping_error</code> SQLSTATE (<code>42803</code>).

```csharp
public const string GroupingError = "42803"
```

PostgreSQL macro <code>ERRCODE_GROUPING_ERROR</code>.

<a id="member-fb9b416c03f09925"></a>

### HeldCursorRequiresSameIsolationLevel

The PostgreSQL <code>held_cursor_requires_same_isolation_level</code> SQLSTATE (<code>25008</code>).

```csharp
public const string HeldCursorRequiresSameIsolationLevel = "25008"
```

PostgreSQL macro <code>ERRCODE_HELD_CURSOR_REQUIRES_SAME_ISOLATION_LEVEL</code>.

<a id="member-d4af193d0706b487"></a>

### IdleInTransactionSessionTimeout

The PostgreSQL <code>idle_in_transaction_session_timeout</code> SQLSTATE (<code>25P03</code>).

```csharp
public const string IdleInTransactionSessionTimeout = "25P03"
```

PostgreSQL macro <code>ERRCODE_IDLE_IN_TRANSACTION_SESSION_TIMEOUT</code>.

<a id="member-e973af8396cd355b"></a>

### IdleSessionTimeout

The PostgreSQL <code>idle_session_timeout</code> SQLSTATE (<code>57P05</code>).

```csharp
public const string IdleSessionTimeout = "57P05"
```

PostgreSQL macro <code>ERRCODE_IDLE_SESSION_TIMEOUT</code>. Present in the PostgreSQL 14, 15, 16, 17, 18, 19 beta source catalogs.

<a id="member-91808915b66a361b"></a>

### InFailedSqlTransaction

The PostgreSQL <code>in_failed_sql_transaction</code> SQLSTATE (<code>25P02</code>).

```csharp
public const string InFailedSqlTransaction = "25P02"
```

PostgreSQL macro <code>ERRCODE_IN_FAILED_SQL_TRANSACTION</code>.

<a id="member-2e8f3387b4a7dc5a"></a>

### InappropriateAccessModeForBranchTransaction

The PostgreSQL <code>inappropriate_access_mode_for_branch_transaction</code> SQLSTATE (<code>25003</code>).

```csharp
public const string InappropriateAccessModeForBranchTransaction = "25003"
```

PostgreSQL macro <code>ERRCODE_INAPPROPRIATE_ACCESS_MODE_FOR_BRANCH_TRANSACTION</code>.

<a id="member-5702981601cb0ccb"></a>

### InappropriateIsolationLevelForBranchTransaction

The PostgreSQL <code>inappropriate_isolation_level_for_branch_transaction</code> SQLSTATE (<code>25004</code>).

```csharp
public const string InappropriateIsolationLevelForBranchTransaction = "25004"
```

PostgreSQL macro <code>ERRCODE_INAPPROPRIATE_ISOLATION_LEVEL_FOR_BRANCH_TRANSACTION</code>.

<a id="member-cb6bcadcf154b77c"></a>

### IndeterminateCollation

The PostgreSQL <code>indeterminate_collation</code> SQLSTATE (<code>42P22</code>).

```csharp
public const string IndeterminateCollation = "42P22"
```

PostgreSQL macro <code>ERRCODE_INDETERMINATE_COLLATION</code>.

<a id="member-1d769459af2390d7"></a>

### IndeterminateDatatype

The PostgreSQL <code>indeterminate_datatype</code> SQLSTATE (<code>42P18</code>).

```csharp
public const string IndeterminateDatatype = "42P18"
```

PostgreSQL macro <code>ERRCODE_INDETERMINATE_DATATYPE</code>.

<a id="member-ca888deb6ceadf13"></a>

### IndexCorrupted

The PostgreSQL <code>index_corrupted</code> SQLSTATE (<code>XX002</code>).

```csharp
public const string IndexCorrupted = "XX002"
```

PostgreSQL macro <code>ERRCODE_INDEX_CORRUPTED</code>.

<a id="member-73d7f12dcbec3d39"></a>

### IndicatorOverflow

The PostgreSQL <code>indicator_overflow</code> SQLSTATE (<code>22022</code>).

```csharp
public const string IndicatorOverflow = "22022"
```

PostgreSQL macro <code>ERRCODE_INDICATOR_OVERFLOW</code>.

<a id="member-ec8ee08654ca6571"></a>

### InsufficientPrivilege

The PostgreSQL <code>insufficient_privilege</code> SQLSTATE (<code>42501</code>).

```csharp
public const string InsufficientPrivilege = "42501"
```

PostgreSQL macro <code>ERRCODE_INSUFFICIENT_PRIVILEGE</code>.

<a id="member-e450ba8955d70999"></a>

### InsufficientResources

The PostgreSQL <code>insufficient_resources</code> SQLSTATE (<code>53000</code>).

```csharp
public const string InsufficientResources = "53000"
```

PostgreSQL macro <code>ERRCODE_INSUFFICIENT_RESOURCES</code>.

<a id="member-e174048908b4af1f"></a>

### IntegrityConstraintViolation

The PostgreSQL <code>integrity_constraint_violation</code> SQLSTATE (<code>23000</code>).

```csharp
public const string IntegrityConstraintViolation = "23000"
```

PostgreSQL macro <code>ERRCODE_INTEGRITY_CONSTRAINT_VIOLATION</code>.

<a id="member-6be687900baa39ee"></a>

### InternalError

The PostgreSQL <code>internal_error</code> SQLSTATE (<code>XX000</code>).

```csharp
public const string InternalError = "XX000"
```

PostgreSQL macro <code>ERRCODE_INTERNAL_ERROR</code>.

<a id="member-7249dea8e2e03ae1"></a>

### IntervalFieldOverflow

The PostgreSQL <code>interval_field_overflow</code> SQLSTATE (<code>22015</code>).

```csharp
public const string IntervalFieldOverflow = "22015"
```

PostgreSQL macro <code>ERRCODE_INTERVAL_FIELD_OVERFLOW</code>.

<a id="member-b222fb893b282057"></a>

### InvalidArgumentForLogarithm

The PostgreSQL <code>invalid_argument_for_logarithm</code> SQLSTATE (<code>2201E</code>).

```csharp
public const string InvalidArgumentForLogarithm = "2201E"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_LOG</code>.

<a id="member-d682d5cdcd2c717a"></a>

### InvalidArgumentForNthValueFunction

The PostgreSQL <code>invalid_argument_for_nth_value_function</code> SQLSTATE (<code>22016</code>).

```csharp
public const string InvalidArgumentForNthValueFunction = "22016"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_NTH_VALUE</code>.

<a id="member-e61b9938835e3ee0"></a>

### InvalidArgumentForNtileFunction

The PostgreSQL <code>invalid_argument_for_ntile_function</code> SQLSTATE (<code>22014</code>).

```csharp
public const string InvalidArgumentForNtileFunction = "22014"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_NTILE</code>.

<a id="member-5e9339efb3b934e1"></a>

### InvalidArgumentForPowerFunction

The PostgreSQL <code>invalid_argument_for_power_function</code> SQLSTATE (<code>2201F</code>).

```csharp
public const string InvalidArgumentForPowerFunction = "2201F"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_POWER_FUNCTION</code>.

<a id="member-5b0d0452303b8bda"></a>

### InvalidArgumentForSqlJsonDatetimeFunction

The PostgreSQL <code>invalid_argument_for_sql_json_datetime_function</code> SQLSTATE (<code>22031</code>).

```csharp
public const string InvalidArgumentForSqlJsonDatetimeFunction = "22031"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_SQL_JSON_DATETIME_FUNCTION</code>.

<a id="member-ed3cf1232c8ffe03"></a>

### InvalidArgumentForWidthBucketFunction

The PostgreSQL <code>invalid_argument_for_width_bucket_function</code> SQLSTATE (<code>2201G</code>).

```csharp
public const string InvalidArgumentForWidthBucketFunction = "2201G"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_WIDTH_BUCKET_FUNCTION</code>.

<a id="member-17339fa3ee74e677"></a>

### InvalidArgumentForXQuery

The PostgreSQL <code>invalid_argument_for_xquery</code> SQLSTATE (<code>10608</code>).

```csharp
public const string InvalidArgumentForXQuery = "10608"
```

PostgreSQL macro <code>ERRCODE_INVALID_ARGUMENT_FOR_XQUERY</code>. Present in the PostgreSQL 18, 19 beta source catalogs.

<a id="member-85407a1924361b30"></a>

### InvalidAuthorizationSpecification

The PostgreSQL <code>invalid_authorization_specification</code> SQLSTATE (<code>28000</code>).

```csharp
public const string InvalidAuthorizationSpecification = "28000"
```

PostgreSQL macro <code>ERRCODE_INVALID_AUTHORIZATION_SPECIFICATION</code>.

<a id="member-e787d6f1ad0f4ed2"></a>

### InvalidBinaryRepresentation

The PostgreSQL <code>invalid_binary_representation</code> SQLSTATE (<code>22P03</code>).

```csharp
public const string InvalidBinaryRepresentation = "22P03"
```

PostgreSQL macro <code>ERRCODE_INVALID_BINARY_REPRESENTATION</code>.

<a id="member-ee9ad3abfca724d6"></a>

### InvalidCatalogName

The PostgreSQL <code>invalid_catalog_name</code> SQLSTATE (<code>3D000</code>).

```csharp
public const string InvalidCatalogName = "3D000"
```

PostgreSQL macro <code>ERRCODE_INVALID_CATALOG_NAME</code>.

<a id="member-a0ded93738b18d6a"></a>

### InvalidCharacterValueForCast

The PostgreSQL <code>invalid_character_value_for_cast</code> SQLSTATE (<code>22018</code>).

```csharp
public const string InvalidCharacterValueForCast = "22018"
```

PostgreSQL macro <code>ERRCODE_INVALID_CHARACTER_VALUE_FOR_CAST</code>.

<a id="member-0812607626ffb9d0"></a>

### InvalidColumnDefinition

The PostgreSQL <code>invalid_column_definition</code> SQLSTATE (<code>42611</code>).

```csharp
public const string InvalidColumnDefinition = "42611"
```

PostgreSQL macro <code>ERRCODE_INVALID_COLUMN_DEFINITION</code>.

<a id="member-039ad33113c5b753"></a>

### InvalidColumnReference

The PostgreSQL <code>invalid_column_reference</code> SQLSTATE (<code>42P10</code>).

```csharp
public const string InvalidColumnReference = "42P10"
```

PostgreSQL macro <code>ERRCODE_INVALID_COLUMN_REFERENCE</code>.

<a id="member-24a14b0faacc30a7"></a>

### InvalidCursorDefinition

The PostgreSQL <code>invalid_cursor_definition</code> SQLSTATE (<code>42P11</code>).

```csharp
public const string InvalidCursorDefinition = "42P11"
```

PostgreSQL macro <code>ERRCODE_INVALID_CURSOR_DEFINITION</code>.

<a id="member-e45b297c3db3af5e"></a>

### InvalidCursorName

The PostgreSQL <code>invalid_cursor_name</code> SQLSTATE (<code>34000</code>).

```csharp
public const string InvalidCursorName = "34000"
```

PostgreSQL macro <code>ERRCODE_INVALID_CURSOR_NAME</code>.

<a id="member-4f515e37342aef40"></a>

### InvalidCursorState

The PostgreSQL <code>invalid_cursor_state</code> SQLSTATE (<code>24000</code>).

```csharp
public const string InvalidCursorState = "24000"
```

PostgreSQL macro <code>ERRCODE_INVALID_CURSOR_STATE</code>.

<a id="member-1bd783a971ba129f"></a>

### InvalidDatabaseDefinition

The PostgreSQL <code>invalid_database_definition</code> SQLSTATE (<code>42P12</code>).

```csharp
public const string InvalidDatabaseDefinition = "42P12"
```

PostgreSQL macro <code>ERRCODE_INVALID_DATABASE_DEFINITION</code>.

<a id="member-38607d8cd3ae63e8"></a>

### InvalidDatetimeFormat

The PostgreSQL <code>invalid_datetime_format</code> SQLSTATE (<code>22007</code>).

```csharp
public const string InvalidDatetimeFormat = "22007"
```

PostgreSQL macro <code>ERRCODE_INVALID_DATETIME_FORMAT</code>.

<a id="member-c01bd81a8ee76148"></a>

### InvalidEscapeCharacter

The PostgreSQL <code>invalid_escape_character</code> SQLSTATE (<code>22019</code>).

```csharp
public const string InvalidEscapeCharacter = "22019"
```

PostgreSQL macro <code>ERRCODE_INVALID_ESCAPE_CHARACTER</code>.

<a id="member-c0aba8171008ef7c"></a>

### InvalidEscapeOctet

The PostgreSQL <code>invalid_escape_octet</code> SQLSTATE (<code>2200D</code>).

```csharp
public const string InvalidEscapeOctet = "2200D"
```

PostgreSQL macro <code>ERRCODE_INVALID_ESCAPE_OCTET</code>.

<a id="member-97e0c78ce3037caa"></a>

### InvalidEscapeSequence

The PostgreSQL <code>invalid_escape_sequence</code> SQLSTATE (<code>22025</code>).

```csharp
public const string InvalidEscapeSequence = "22025"
```

PostgreSQL macro <code>ERRCODE_INVALID_ESCAPE_SEQUENCE</code>.

<a id="member-4197405d1b3a8ed3"></a>

### InvalidForeignKey

The PostgreSQL <code>invalid_foreign_key</code> SQLSTATE (<code>42830</code>).

```csharp
public const string InvalidForeignKey = "42830"
```

PostgreSQL macro <code>ERRCODE_INVALID_FOREIGN_KEY</code>.

<a id="member-1ee85dffbafd942c"></a>

### InvalidFunctionDefinition

The PostgreSQL <code>invalid_function_definition</code> SQLSTATE (<code>42P13</code>).

```csharp
public const string InvalidFunctionDefinition = "42P13"
```

PostgreSQL macro <code>ERRCODE_INVALID_FUNCTION_DEFINITION</code>.

<a id="member-d1e25aa412aff648"></a>

### InvalidGrantOperation

The PostgreSQL <code>invalid_grant_operation</code> SQLSTATE (<code>0LP01</code>).

```csharp
public const string InvalidGrantOperation = "0LP01"
```

PostgreSQL macro <code>ERRCODE_INVALID_GRANT_OPERATION</code>.

<a id="member-2e0d1611be2564e0"></a>

### InvalidGrantor

The PostgreSQL <code>invalid_grantor</code> SQLSTATE (<code>0L000</code>).

```csharp
public const string InvalidGrantor = "0L000"
```

PostgreSQL macro <code>ERRCODE_INVALID_GRANTOR</code>.

<a id="member-d1d5316df9bf9529"></a>

### InvalidIndicatorParameterValue

The PostgreSQL <code>invalid_indicator_parameter_value</code> SQLSTATE (<code>22010</code>).

```csharp
public const string InvalidIndicatorParameterValue = "22010"
```

PostgreSQL macro <code>ERRCODE_INVALID_INDICATOR_PARAMETER_VALUE</code>.

<a id="member-efcb7bf7a7b97fb8"></a>

### InvalidJsonText

The PostgreSQL <code>invalid_json_text</code> SQLSTATE (<code>22032</code>).

```csharp
public const string InvalidJsonText = "22032"
```

PostgreSQL macro <code>ERRCODE_INVALID_JSON_TEXT</code>.

<a id="member-da1167dfb948157a"></a>

### InvalidLocatorSpecification

The PostgreSQL <code>invalid_locator_specification</code> SQLSTATE (<code>0F001</code>).

```csharp
public const string InvalidLocatorSpecification = "0F001"
```

PostgreSQL macro <code>ERRCODE_L_E_INVALID_SPECIFICATION</code>.

<a id="member-953d4c22554863c6"></a>

### InvalidName

The PostgreSQL <code>invalid_name</code> SQLSTATE (<code>42602</code>).

```csharp
public const string InvalidName = "42602"
```

PostgreSQL macro <code>ERRCODE_INVALID_NAME</code>.

<a id="member-af2df6456df6c142"></a>

### InvalidObjectDefinition

The PostgreSQL <code>invalid_object_definition</code> SQLSTATE (<code>42P17</code>).

```csharp
public const string InvalidObjectDefinition = "42P17"
```

PostgreSQL macro <code>ERRCODE_INVALID_OBJECT_DEFINITION</code>.

<a id="member-c03aae334f8886f7"></a>

### InvalidParameterValue

The PostgreSQL <code>invalid_parameter_value</code> SQLSTATE (<code>22023</code>).

```csharp
public const string InvalidParameterValue = "22023"
```

PostgreSQL macro <code>ERRCODE_INVALID_PARAMETER_VALUE</code>.

<a id="member-8ba0cb38bd35f4c2"></a>

### InvalidPassword

The PostgreSQL <code>invalid_password</code> SQLSTATE (<code>28P01</code>).

```csharp
public const string InvalidPassword = "28P01"
```

PostgreSQL macro <code>ERRCODE_INVALID_PASSWORD</code>.

<a id="member-eaf681d90df702ec"></a>

### InvalidPrecedingOrFollowingSize

The PostgreSQL <code>invalid_preceding_or_following_size</code> SQLSTATE (<code>22013</code>).

```csharp
public const string InvalidPrecedingOrFollowingSize = "22013"
```

PostgreSQL macro <code>ERRCODE_INVALID_PRECEDING_OR_FOLLOWING_SIZE</code>.

<a id="member-c6fbf87783ca33de"></a>

### InvalidPreparedStatementDefinition

The PostgreSQL <code>invalid_prepared_statement_definition</code> SQLSTATE (<code>42P14</code>).

```csharp
public const string InvalidPreparedStatementDefinition = "42P14"
```

PostgreSQL macro <code>ERRCODE_INVALID_PSTATEMENT_DEFINITION</code>.

<a id="member-31d42555d3a0d6d6"></a>

### InvalidRecursion

The PostgreSQL <code>invalid_recursion</code> SQLSTATE (<code>42P19</code>).

```csharp
public const string InvalidRecursion = "42P19"
```

PostgreSQL macro <code>ERRCODE_INVALID_RECURSION</code>.

<a id="member-c263f5cf219be6d3"></a>

### InvalidRegularExpression

The PostgreSQL <code>invalid_regular_expression</code> SQLSTATE (<code>2201B</code>).

```csharp
public const string InvalidRegularExpression = "2201B"
```

PostgreSQL macro <code>ERRCODE_INVALID_REGULAR_EXPRESSION</code>.

<a id="member-e4569c43ea89f465"></a>

### InvalidRoleSpecification

The PostgreSQL <code>invalid_role_specification</code> SQLSTATE (<code>0P000</code>).

```csharp
public const string InvalidRoleSpecification = "0P000"
```

PostgreSQL macro <code>ERRCODE_INVALID_ROLE_SPECIFICATION</code>.

<a id="member-07014ef3ad194fce"></a>

### InvalidRowCountInLimitClause

The PostgreSQL <code>invalid_row_count_in_limit_clause</code> SQLSTATE (<code>2201W</code>).

```csharp
public const string InvalidRowCountInLimitClause = "2201W"
```

PostgreSQL macro <code>ERRCODE_INVALID_ROW_COUNT_IN_LIMIT_CLAUSE</code>.

<a id="member-340e42a2ab47058c"></a>

### InvalidRowCountInResultOffsetClause

The PostgreSQL <code>invalid_row_count_in_result_offset_clause</code> SQLSTATE (<code>2201X</code>).

```csharp
public const string InvalidRowCountInResultOffsetClause = "2201X"
```

PostgreSQL macro <code>ERRCODE_INVALID_ROW_COUNT_IN_RESULT_OFFSET_CLAUSE</code>.

<a id="member-3b156e7313684e32"></a>

### InvalidSavepointSpecification

The PostgreSQL <code>invalid_savepoint_specification</code> SQLSTATE (<code>3B001</code>).

```csharp
public const string InvalidSavepointSpecification = "3B001"
```

PostgreSQL macro <code>ERRCODE_S_E_INVALID_SPECIFICATION</code>.

<a id="member-141c095c244741a2"></a>

### InvalidSchemaDefinition

The PostgreSQL <code>invalid_schema_definition</code> SQLSTATE (<code>42P15</code>).

```csharp
public const string InvalidSchemaDefinition = "42P15"
```

PostgreSQL macro <code>ERRCODE_INVALID_SCHEMA_DEFINITION</code>.

<a id="member-8b5bdb2786716c55"></a>

### InvalidSchemaName

The PostgreSQL <code>invalid_schema_name</code> SQLSTATE (<code>3F000</code>).

```csharp
public const string InvalidSchemaName = "3F000"
```

PostgreSQL macro <code>ERRCODE_INVALID_SCHEMA_NAME</code>.

<a id="member-5f3ddfcae7391e3a"></a>

### InvalidSqlJsonSubscript

The PostgreSQL <code>invalid_sql_json_subscript</code> SQLSTATE (<code>22033</code>).

```csharp
public const string InvalidSqlJsonSubscript = "22033"
```

PostgreSQL macro <code>ERRCODE_INVALID_SQL_JSON_SUBSCRIPT</code>.

<a id="member-ea19dcb8fd6b3f79"></a>

### InvalidSqlStatementName

The PostgreSQL <code>invalid_sql_statement_name</code> SQLSTATE (<code>26000</code>).

```csharp
public const string InvalidSqlStatementName = "26000"
```

PostgreSQL macro <code>ERRCODE_INVALID_SQL_STATEMENT_NAME</code>.

<a id="member-1d858a88f0f6f001"></a>

### InvalidTableDefinition

The PostgreSQL <code>invalid_table_definition</code> SQLSTATE (<code>42P16</code>).

```csharp
public const string InvalidTableDefinition = "42P16"
```

PostgreSQL macro <code>ERRCODE_INVALID_TABLE_DEFINITION</code>.

<a id="member-c0899546eca5bf34"></a>

### InvalidTablesampleArgument

The PostgreSQL <code>invalid_tablesample_argument</code> SQLSTATE (<code>2202H</code>).

```csharp
public const string InvalidTablesampleArgument = "2202H"
```

PostgreSQL macro <code>ERRCODE_INVALID_TABLESAMPLE_ARGUMENT</code>.

<a id="member-5f7a7e0fab0ac181"></a>

### InvalidTablesampleRepeat

The PostgreSQL <code>invalid_tablesample_repeat</code> SQLSTATE (<code>2202G</code>).

```csharp
public const string InvalidTablesampleRepeat = "2202G"
```

PostgreSQL macro <code>ERRCODE_INVALID_TABLESAMPLE_REPEAT</code>.

<a id="member-c4a005a567d9a6ba"></a>

### InvalidTextRepresentation

The PostgreSQL <code>invalid_text_representation</code> SQLSTATE (<code>22P02</code>).

```csharp
public const string InvalidTextRepresentation = "22P02"
```

PostgreSQL macro <code>ERRCODE_INVALID_TEXT_REPRESENTATION</code>.

<a id="member-3d4859542f593640"></a>

### InvalidTimeZoneDisplacementValue

The PostgreSQL <code>invalid_time_zone_displacement_value</code> SQLSTATE (<code>22009</code>).

```csharp
public const string InvalidTimeZoneDisplacementValue = "22009"
```

PostgreSQL macro <code>ERRCODE_INVALID_TIME_ZONE_DISPLACEMENT_VALUE</code>.

<a id="member-4ce7281a0b315757"></a>

### InvalidTransactionInitiation

The PostgreSQL <code>invalid_transaction_initiation</code> SQLSTATE (<code>0B000</code>).

```csharp
public const string InvalidTransactionInitiation = "0B000"
```

PostgreSQL macro <code>ERRCODE_INVALID_TRANSACTION_INITIATION</code>.

<a id="member-e9b0df16e910e51c"></a>

### InvalidTransactionState

The PostgreSQL <code>invalid_transaction_state</code> SQLSTATE (<code>25000</code>).

```csharp
public const string InvalidTransactionState = "25000"
```

PostgreSQL macro <code>ERRCODE_INVALID_TRANSACTION_STATE</code>.

<a id="member-79cf8adc4fdcc559"></a>

### InvalidTransactionTermination

The PostgreSQL <code>invalid_transaction_termination</code> SQLSTATE (<code>2D000</code>).

```csharp
public const string InvalidTransactionTermination = "2D000"
```

PostgreSQL macro <code>ERRCODE_INVALID_TRANSACTION_TERMINATION</code>.

<a id="member-6a62cae86975c26c"></a>

### InvalidUseOfEscapeCharacter

The PostgreSQL <code>invalid_use_of_escape_character</code> SQLSTATE (<code>2200C</code>).

```csharp
public const string InvalidUseOfEscapeCharacter = "2200C"
```

PostgreSQL macro <code>ERRCODE_INVALID_USE_OF_ESCAPE_CHARACTER</code>.

<a id="member-bba37536cbf5f724"></a>

### InvalidXmlComment

The PostgreSQL <code>invalid_xml_comment</code> SQLSTATE (<code>2200S</code>).

```csharp
public const string InvalidXmlComment = "2200S"
```

PostgreSQL macro <code>ERRCODE_INVALID_XML_COMMENT</code>.

<a id="member-77d0c8fcc5eaab96"></a>

### InvalidXmlContent

The PostgreSQL <code>invalid_xml_content</code> SQLSTATE (<code>2200N</code>).

```csharp
public const string InvalidXmlContent = "2200N"
```

PostgreSQL macro <code>ERRCODE_INVALID_XML_CONTENT</code>.

<a id="member-dbf97e56014c17f7"></a>

### InvalidXmlDocument

The PostgreSQL <code>invalid_xml_document</code> SQLSTATE (<code>2200M</code>).

```csharp
public const string InvalidXmlDocument = "2200M"
```

PostgreSQL macro <code>ERRCODE_INVALID_XML_DOCUMENT</code>.

<a id="member-01e0108b9b742260"></a>

### InvalidXmlProcessingInstruction

The PostgreSQL <code>invalid_xml_processing_instruction</code> SQLSTATE (<code>2200T</code>).

```csharp
public const string InvalidXmlProcessingInstruction = "2200T"
```

PostgreSQL macro <code>ERRCODE_INVALID_XML_PROCESSING_INSTRUCTION</code>.

<a id="member-1e01e2768a62eaae"></a>

### IoError

The PostgreSQL <code>io_error</code> SQLSTATE (<code>58030</code>).

```csharp
public const string IoError = "58030"
```

PostgreSQL macro <code>ERRCODE_IO_ERROR</code>.

<a id="member-ec470b2d2ca79332"></a>

### LocatorException

The PostgreSQL <code>locator_exception</code> SQLSTATE (<code>0F000</code>).

```csharp
public const string LocatorException = "0F000"
```

PostgreSQL macro <code>ERRCODE_LOCATOR_EXCEPTION</code>.

<a id="member-0e6b25a51eeb7201"></a>

### LockFileExists

The PostgreSQL <code>lock_file_exists</code> SQLSTATE (<code>F0001</code>).

```csharp
public const string LockFileExists = "F0001"
```

PostgreSQL macro <code>ERRCODE_LOCK_FILE_EXISTS</code>.

<a id="member-31a16d6e5448804e"></a>

### LockNotAvailable

The PostgreSQL <code>lock_not_available</code> SQLSTATE (<code>55P03</code>).

```csharp
public const string LockNotAvailable = "55P03"
```

PostgreSQL macro <code>ERRCODE_LOCK_NOT_AVAILABLE</code>.

<a id="member-45218e5cb2359a30"></a>

### MoreThanOneSqlJsonItem

The PostgreSQL <code>more_than_one_sql_json_item</code> SQLSTATE (<code>22034</code>).

```csharp
public const string MoreThanOneSqlJsonItem = "22034"
```

PostgreSQL macro <code>ERRCODE_MORE_THAN_ONE_SQL_JSON_ITEM</code>.

<a id="member-9d5dd1e45a6489a7"></a>

### MostSpecificTypeMismatch

The PostgreSQL <code>most_specific_type_mismatch</code> SQLSTATE (<code>2200G</code>).

```csharp
public const string MostSpecificTypeMismatch = "2200G"
```

PostgreSQL macro <code>ERRCODE_MOST_SPECIFIC_TYPE_MISMATCH</code>.

<a id="member-3cb16288151f3fef"></a>

### NameTooLong

The PostgreSQL <code>name_too_long</code> SQLSTATE (<code>42622</code>).

```csharp
public const string NameTooLong = "42622"
```

PostgreSQL macro <code>ERRCODE_NAME_TOO_LONG</code>.

<a id="member-7b14957837dc245c"></a>

### NoActiveSqlTransaction

The PostgreSQL <code>no_active_sql_transaction</code> SQLSTATE (<code>25P01</code>).

```csharp
public const string NoActiveSqlTransaction = "25P01"
```

PostgreSQL macro <code>ERRCODE_NO_ACTIVE_SQL_TRANSACTION</code>.

<a id="member-184edfe5b419d1f1"></a>

### NoActiveSqlTransactionForBranchTransaction

The PostgreSQL <code>no_active_sql_transaction_for_branch_transaction</code> SQLSTATE (<code>25005</code>).

```csharp
public const string NoActiveSqlTransactionForBranchTransaction = "25005"
```

PostgreSQL macro <code>ERRCODE_NO_ACTIVE_SQL_TRANSACTION_FOR_BRANCH_TRANSACTION</code>.

<a id="member-b813bed8f91df1b6"></a>

### NoAdditionalDynamicResultSetsReturned

The PostgreSQL <code>no_additional_dynamic_result_sets_returned</code> SQLSTATE (<code>02001</code>).

```csharp
public const string NoAdditionalDynamicResultSetsReturned = "02001"
```

PostgreSQL macro <code>ERRCODE_NO_ADDITIONAL_DYNAMIC_RESULT_SETS_RETURNED</code>.

<a id="member-4c442c0a7445e6b9"></a>

### NoData

The PostgreSQL <code>no_data</code> SQLSTATE (<code>02000</code>).

```csharp
public const string NoData = "02000"
```

PostgreSQL macro <code>ERRCODE_NO_DATA</code>.

<a id="member-e23957adb2b0a196"></a>

### NoDataFound

The PostgreSQL <code>no_data_found</code> SQLSTATE (<code>P0002</code>).

```csharp
public const string NoDataFound = "P0002"
```

PostgreSQL macro <code>ERRCODE_NO_DATA_FOUND</code>.

<a id="member-193b7e86e0a97b83"></a>

### NoSqlJsonItem

The PostgreSQL <code>no_sql_json_item</code> SQLSTATE (<code>22035</code>).

```csharp
public const string NoSqlJsonItem = "22035"
```

PostgreSQL macro <code>ERRCODE_NO_SQL_JSON_ITEM</code>.

<a id="member-9558c3bc3b7d8939"></a>

### NonNumericSqlJsonItem

The PostgreSQL <code>non_numeric_sql_json_item</code> SQLSTATE (<code>22036</code>).

```csharp
public const string NonNumericSqlJsonItem = "22036"
```

PostgreSQL macro <code>ERRCODE_NON_NUMERIC_SQL_JSON_ITEM</code>.

<a id="member-75c9e57d5601a0ba"></a>

### NonUniqueKeysInAJsonObject

The PostgreSQL <code>non_unique_keys_in_a_json_object</code> SQLSTATE (<code>22037</code>).

```csharp
public const string NonUniqueKeysInAJsonObject = "22037"
```

PostgreSQL macro <code>ERRCODE_NON_UNIQUE_KEYS_IN_A_JSON_OBJECT</code>.

<a id="member-f6dab47bed3c386f"></a>

### NonstandardUseOfEscapeCharacter

The PostgreSQL <code>nonstandard_use_of_escape_character</code> SQLSTATE (<code>22P06</code>).

```csharp
public const string NonstandardUseOfEscapeCharacter = "22P06"
```

PostgreSQL macro <code>ERRCODE_NONSTANDARD_USE_OF_ESCAPE_CHARACTER</code>.

<a id="member-1ab46e08ed5c3cf0"></a>

### NotAnXmlDocument

The PostgreSQL <code>not_an_xml_document</code> SQLSTATE (<code>2200L</code>).

```csharp
public const string NotAnXmlDocument = "2200L"
```

PostgreSQL macro <code>ERRCODE_NOT_AN_XML_DOCUMENT</code>.

<a id="member-cd9f6c8317870b07"></a>

### NotNullViolation

The PostgreSQL <code>not_null_violation</code> SQLSTATE (<code>23502</code>).

```csharp
public const string NotNullViolation = "23502"
```

PostgreSQL macro <code>ERRCODE_NOT_NULL_VIOLATION</code>.

<a id="member-0179367ff22b43c8"></a>

### NullValueNoIndicatorParameter

The PostgreSQL <code>null_value_no_indicator_parameter</code> SQLSTATE (<code>22002</code>).

```csharp
public const string NullValueNoIndicatorParameter = "22002"
```

PostgreSQL macro <code>ERRCODE_NULL_VALUE_NO_INDICATOR_PARAMETER</code>.

<a id="member-6a0618f8ff43bc92"></a>

### NullValueNotAllowed

The PostgreSQL <code>null_value_not_allowed</code> SQLSTATE (<code>22004</code>).

```csharp
public const string NullValueNotAllowed = "22004"
```

PostgreSQL macro <code>ERRCODE_NULL_VALUE_NOT_ALLOWED</code>.

<a id="member-ada857bb657a5eee"></a>

### NumericValueOutOfRange

The PostgreSQL <code>numeric_value_out_of_range</code> SQLSTATE (<code>22003</code>).

```csharp
public const string NumericValueOutOfRange = "22003"
```

PostgreSQL macro <code>ERRCODE_NUMERIC_VALUE_OUT_OF_RANGE</code>.

<a id="member-9db520c1c31d35ef"></a>

### ObjectInUse

The PostgreSQL <code>object_in_use</code> SQLSTATE (<code>55006</code>).

```csharp
public const string ObjectInUse = "55006"
```

PostgreSQL macro <code>ERRCODE_OBJECT_IN_USE</code>.

<a id="member-ecd86c3519c789dc"></a>

### ObjectNotInPrerequisiteState

The PostgreSQL <code>object_not_in_prerequisite_state</code> SQLSTATE (<code>55000</code>).

```csharp
public const string ObjectNotInPrerequisiteState = "55000"
```

PostgreSQL macro <code>ERRCODE_OBJECT_NOT_IN_PREREQUISITE_STATE</code>.

<a id="member-1d1bbc3210d4b803"></a>

### OperatorIntervention

The PostgreSQL <code>operator_intervention</code> SQLSTATE (<code>57000</code>).

```csharp
public const string OperatorIntervention = "57000"
```

PostgreSQL macro <code>ERRCODE_OPERATOR_INTERVENTION</code>.

<a id="member-22b78ab0cd945141"></a>

### OutOfMemory

The PostgreSQL <code>out_of_memory</code> SQLSTATE (<code>53200</code>).

```csharp
public const string OutOfMemory = "53200"
```

PostgreSQL macro <code>ERRCODE_OUT_OF_MEMORY</code>.

<a id="member-ee150d7ee9db769e"></a>

### PlpgsqlError

The PostgreSQL <code>plpgsql_error</code> SQLSTATE (<code>P0000</code>).

```csharp
public const string PlpgsqlError = "P0000"
```

PostgreSQL macro <code>ERRCODE_PLPGSQL_ERROR</code>.

<a id="member-e054c7c93661de56"></a>

### ProgramLimitExceeded

The PostgreSQL <code>program_limit_exceeded</code> SQLSTATE (<code>54000</code>).

```csharp
public const string ProgramLimitExceeded = "54000"
```

PostgreSQL macro <code>ERRCODE_PROGRAM_LIMIT_EXCEEDED</code>.

<a id="member-1621bdee5f0d7d97"></a>

### ProtocolViolation

The PostgreSQL <code>protocol_violation</code> SQLSTATE (<code>08P01</code>).

```csharp
public const string ProtocolViolation = "08P01"
```

PostgreSQL macro <code>ERRCODE_PROTOCOL_VIOLATION</code>.

<a id="member-336447eaacd8ea46"></a>

### QueryCanceled

The PostgreSQL <code>query_canceled</code> SQLSTATE (<code>57014</code>).

```csharp
public const string QueryCanceled = "57014"
```

PostgreSQL macro <code>ERRCODE_QUERY_CANCELED</code>.

<a id="member-1ca60393e60975ee"></a>

### RaiseException

The PostgreSQL <code>raise_exception</code> SQLSTATE (<code>P0001</code>).

```csharp
public const string RaiseException = "P0001"
```

PostgreSQL macro <code>ERRCODE_RAISE_EXCEPTION</code>.

<a id="member-c84661826f4f38b5"></a>

### ReadOnlySqlTransaction

The PostgreSQL <code>read_only_sql_transaction</code> SQLSTATE (<code>25006</code>).

```csharp
public const string ReadOnlySqlTransaction = "25006"
```

PostgreSQL macro <code>ERRCODE_READ_ONLY_SQL_TRANSACTION</code>.

<a id="member-401a85b0b2806762"></a>

### ReservedName

The PostgreSQL <code>reserved_name</code> SQLSTATE (<code>42939</code>).

```csharp
public const string ReservedName = "42939"
```

PostgreSQL macro <code>ERRCODE_RESERVED_NAME</code>.

<a id="member-d54ceeb3432c2eae"></a>

### RestrictViolation

The PostgreSQL <code>restrict_violation</code> SQLSTATE (<code>23001</code>).

```csharp
public const string RestrictViolation = "23001"
```

PostgreSQL macro <code>ERRCODE_RESTRICT_VIOLATION</code>.

<a id="member-0ab95f9bb373358b"></a>

### SavepointException

The PostgreSQL <code>savepoint_exception</code> SQLSTATE (<code>3B000</code>).

```csharp
public const string SavepointException = "3B000"
```

PostgreSQL macro <code>ERRCODE_SAVEPOINT_EXCEPTION</code>.

<a id="member-40774309a9a1b9a4"></a>

### SchemaAndDataStatementMixingNotSupported

The PostgreSQL <code>schema_and_data_statement_mixing_not_supported</code> SQLSTATE (<code>25007</code>).

```csharp
public const string SchemaAndDataStatementMixingNotSupported = "25007"
```

PostgreSQL macro <code>ERRCODE_SCHEMA_AND_DATA_STATEMENT_MIXING_NOT_SUPPORTED</code>.

<a id="member-7637a35589c81d75"></a>

### SequenceGeneratorLimitExceeded

The PostgreSQL <code>sequence_generator_limit_exceeded</code> SQLSTATE (<code>2200H</code>).

```csharp
public const string SequenceGeneratorLimitExceeded = "2200H"
```

PostgreSQL macro <code>ERRCODE_SEQUENCE_GENERATOR_LIMIT_EXCEEDED</code>.

<a id="member-e14a0091475a5150"></a>

### SerializationFailure

The PostgreSQL <code>serialization_failure</code> SQLSTATE (<code>40001</code>).

```csharp
public const string SerializationFailure = "40001"
```

PostgreSQL macro <code>ERRCODE_T_R_SERIALIZATION_FAILURE</code>.

<a id="member-ee1f8a4c6128dbdf"></a>

### SingletonSqlJsonItemRequired

The PostgreSQL <code>singleton_sql_json_item_required</code> SQLSTATE (<code>22038</code>).

```csharp
public const string SingletonSqlJsonItemRequired = "22038"
```

PostgreSQL macro <code>ERRCODE_SINGLETON_SQL_JSON_ITEM_REQUIRED</code>.

<a id="member-a7a264d45bdfae28"></a>

### SnapshotTooOld

The PostgreSQL <code>snapshot_too_old</code> SQLSTATE (<code>72000</code>).

```csharp
public const string SnapshotTooOld = "72000"
```

PostgreSQL macro <code>ERRCODE_SNAPSHOT_TOO_OLD</code>. Present in the PostgreSQL 13, 14, 15, 16 source catalogs.

<a id="member-3963e9305a430b12"></a>

### SqlClientUnableToEstablishSqlConnection

The PostgreSQL <code>sqlclient_unable_to_establish_sqlconnection</code> SQLSTATE (<code>08001</code>).

```csharp
public const string SqlClientUnableToEstablishSqlConnection = "08001"
```

PostgreSQL macro <code>ERRCODE_SQLCLIENT_UNABLE_TO_ESTABLISH_SQLCONNECTION</code>.

<a id="member-96e32d002002dafc"></a>

### SqlJsonArrayNotFound

The PostgreSQL <code>sql_json_array_not_found</code> SQLSTATE (<code>22039</code>).

```csharp
public const string SqlJsonArrayNotFound = "22039"
```

PostgreSQL macro <code>ERRCODE_SQL_JSON_ARRAY_NOT_FOUND</code>.

<a id="member-45e0456f81728f29"></a>

### SqlJsonItemCannotBeCastToTargetType

The PostgreSQL <code>sql_json_item_cannot_be_cast_to_target_type</code> SQLSTATE (<code>2203G</code>).

```csharp
public const string SqlJsonItemCannotBeCastToTargetType = "2203G"
```

PostgreSQL macro <code>ERRCODE_SQL_JSON_ITEM_CANNOT_BE_CAST_TO_TARGET_TYPE</code>. Present in the PostgreSQL 15, 16, 17, 18, 19 beta source catalogs.

<a id="member-6d9e7a6dfa516483"></a>

### SqlJsonMemberNotFound

The PostgreSQL <code>sql_json_member_not_found</code> SQLSTATE (<code>2203A</code>).

```csharp
public const string SqlJsonMemberNotFound = "2203A"
```

PostgreSQL macro <code>ERRCODE_SQL_JSON_MEMBER_NOT_FOUND</code>.

<a id="member-f9e0588cc08c58fc"></a>

### SqlJsonNumberNotFound

The PostgreSQL <code>sql_json_number_not_found</code> SQLSTATE (<code>2203B</code>).

```csharp
public const string SqlJsonNumberNotFound = "2203B"
```

PostgreSQL macro <code>ERRCODE_SQL_JSON_NUMBER_NOT_FOUND</code>.

<a id="member-9514517a0782d0c3"></a>

### SqlJsonObjectNotFound

The PostgreSQL <code>sql_json_object_not_found</code> SQLSTATE (<code>2203C</code>).

```csharp
public const string SqlJsonObjectNotFound = "2203C"
```

PostgreSQL macro <code>ERRCODE_SQL_JSON_OBJECT_NOT_FOUND</code>.

<a id="member-552ca812a8b7832f"></a>

### SqlJsonScalarRequired

The PostgreSQL <code>sql_json_scalar_required</code> SQLSTATE (<code>2203F</code>).

```csharp
public const string SqlJsonScalarRequired = "2203F"
```

PostgreSQL macro <code>ERRCODE_SQL_JSON_SCALAR_REQUIRED</code>.

<a id="member-f592804566dc258b"></a>

### SqlRoutineException

The PostgreSQL <code>sql_routine_exception</code> SQLSTATE (<code>2F000</code>).

```csharp
public const string SqlRoutineException = "2F000"
```

PostgreSQL macro <code>ERRCODE_SQL_ROUTINE_EXCEPTION</code>.

<a id="member-6be06bcfa1c21316"></a>

### SqlRoutineFunctionExecutedNoReturnStatement

The PostgreSQL <code>function_executed_no_return_statement</code> SQLSTATE (<code>2F005</code>).

```csharp
public const string SqlRoutineFunctionExecutedNoReturnStatement = "2F005"
```

PostgreSQL macro <code>ERRCODE_S_R_E_FUNCTION_EXECUTED_NO_RETURN_STATEMENT</code>.

<a id="member-efd4aca34ff3a702"></a>

### SqlRoutineModifyingSqlDataNotPermitted

The PostgreSQL <code>modifying_sql_data_not_permitted</code> SQLSTATE (<code>2F002</code>).

```csharp
public const string SqlRoutineModifyingSqlDataNotPermitted = "2F002"
```

PostgreSQL macro <code>ERRCODE_S_R_E_MODIFYING_SQL_DATA_NOT_PERMITTED</code>.

<a id="member-38201dc341aa6858"></a>

### SqlRoutineProhibitedSqlStatementAttempted

The PostgreSQL <code>prohibited_sql_statement_attempted</code> SQLSTATE (<code>2F003</code>).

```csharp
public const string SqlRoutineProhibitedSqlStatementAttempted = "2F003"
```

PostgreSQL macro <code>ERRCODE_S_R_E_PROHIBITED_SQL_STATEMENT_ATTEMPTED</code>.

<a id="member-f3e313990d935637"></a>

### SqlRoutineReadingSqlDataNotPermitted

The PostgreSQL <code>reading_sql_data_not_permitted</code> SQLSTATE (<code>2F004</code>).

```csharp
public const string SqlRoutineReadingSqlDataNotPermitted = "2F004"
```

PostgreSQL macro <code>ERRCODE_S_R_E_READING_SQL_DATA_NOT_PERMITTED</code>.

<a id="member-934d5e59e361f2e3"></a>

### SqlServerRejectedEstablishmentOfSqlConnection

The PostgreSQL <code>sqlserver_rejected_establishment_of_sqlconnection</code> SQLSTATE (<code>08004</code>).

```csharp
public const string SqlServerRejectedEstablishmentOfSqlConnection = "08004"
```

PostgreSQL macro <code>ERRCODE_SQLSERVER_REJECTED_ESTABLISHMENT_OF_SQLCONNECTION</code>.

<a id="member-95c1cf4a36a0b06a"></a>

### SqlStatementNotYetComplete

The PostgreSQL <code>sql_statement_not_yet_complete</code> SQLSTATE (<code>03000</code>).

```csharp
public const string SqlStatementNotYetComplete = "03000"
```

PostgreSQL macro <code>ERRCODE_SQL_STATEMENT_NOT_YET_COMPLETE</code>.

<a id="member-d1acb675f1ff3e5d"></a>

### StackedDiagnosticsAccessedWithoutActiveHandler

The PostgreSQL <code>stacked_diagnostics_accessed_without_active_handler</code> SQLSTATE (<code>0Z002</code>).

```csharp
public const string StackedDiagnosticsAccessedWithoutActiveHandler = "0Z002"
```

PostgreSQL macro <code>ERRCODE_STACKED_DIAGNOSTICS_ACCESSED_WITHOUT_ACTIVE_HANDLER</code>.

<a id="member-108fbf302ec844de"></a>

### StatementCompletionUnknown

The PostgreSQL <code>statement_completion_unknown</code> SQLSTATE (<code>40003</code>).

```csharp
public const string StatementCompletionUnknown = "40003"
```

PostgreSQL macro <code>ERRCODE_T_R_STATEMENT_COMPLETION_UNKNOWN</code>.

<a id="member-1c5c9f10aa38e20c"></a>

### StatementTooComplex

The PostgreSQL <code>statement_too_complex</code> SQLSTATE (<code>54001</code>).

```csharp
public const string StatementTooComplex = "54001"
```

PostgreSQL macro <code>ERRCODE_STATEMENT_TOO_COMPLEX</code>.

<a id="member-014feb89e2c9f1db"></a>

### StringDataLengthMismatch

The PostgreSQL <code>string_data_length_mismatch</code> SQLSTATE (<code>22026</code>).

```csharp
public const string StringDataLengthMismatch = "22026"
```

PostgreSQL macro <code>ERRCODE_STRING_DATA_LENGTH_MISMATCH</code>.

<a id="member-69305f572f5c2cb6"></a>

### StringDataRightTruncation

The PostgreSQL <code>string_data_right_truncation</code> SQLSTATE (<code>22001</code>).

```csharp
public const string StringDataRightTruncation = "22001"
```

PostgreSQL macro <code>ERRCODE_STRING_DATA_RIGHT_TRUNCATION</code>.

<a id="member-9f9acbc129c431a5"></a>

### SubstringError

The PostgreSQL <code>substring_error</code> SQLSTATE (<code>22011</code>).

```csharp
public const string SubstringError = "22011"
```

PostgreSQL macro <code>ERRCODE_SUBSTRING_ERROR</code>.

<a id="member-dfe57dd113dc2fc9"></a>

### SuccessfulCompletion

The PostgreSQL <code>successful_completion</code> SQLSTATE (<code>00000</code>).

```csharp
public const string SuccessfulCompletion = "00000"
```

PostgreSQL macro <code>ERRCODE_SUCCESSFUL_COMPLETION</code>.

<a id="member-cebea3954ac13eee"></a>

### SyntaxError

The PostgreSQL <code>syntax_error</code> SQLSTATE (<code>42601</code>).

```csharp
public const string SyntaxError = "42601"
```

PostgreSQL macro <code>ERRCODE_SYNTAX_ERROR</code>.

<a id="member-ae2b1c3db1660b95"></a>

### SyntaxErrorOrAccessRuleViolation

The PostgreSQL <code>syntax_error_or_access_rule_violation</code> SQLSTATE (<code>42000</code>).

```csharp
public const string SyntaxErrorOrAccessRuleViolation = "42000"
```

PostgreSQL macro <code>ERRCODE_SYNTAX_ERROR_OR_ACCESS_RULE_VIOLATION</code>.

<a id="member-d5038a4f186d1786"></a>

### SystemError

The PostgreSQL <code>system_error</code> SQLSTATE (<code>58000</code>).

```csharp
public const string SystemError = "58000"
```

PostgreSQL macro <code>ERRCODE_SYSTEM_ERROR</code>.

<a id="member-041f5243f3c02161"></a>

### TooManyArguments

The PostgreSQL <code>too_many_arguments</code> SQLSTATE (<code>54023</code>).

```csharp
public const string TooManyArguments = "54023"
```

PostgreSQL macro <code>ERRCODE_TOO_MANY_ARGUMENTS</code>.

<a id="member-5cb1e53539a79507"></a>

### TooManyColumns

The PostgreSQL <code>too_many_columns</code> SQLSTATE (<code>54011</code>).

```csharp
public const string TooManyColumns = "54011"
```

PostgreSQL macro <code>ERRCODE_TOO_MANY_COLUMNS</code>.

<a id="member-f6bba152087bad50"></a>

### TooManyConnections

The PostgreSQL <code>too_many_connections</code> SQLSTATE (<code>53300</code>).

```csharp
public const string TooManyConnections = "53300"
```

PostgreSQL macro <code>ERRCODE_TOO_MANY_CONNECTIONS</code>.

<a id="member-37f14a80489b84e2"></a>

### TooManyJsonArrayElements

The PostgreSQL <code>too_many_json_array_elements</code> SQLSTATE (<code>2203D</code>).

```csharp
public const string TooManyJsonArrayElements = "2203D"
```

PostgreSQL macro <code>ERRCODE_TOO_MANY_JSON_ARRAY_ELEMENTS</code>.

<a id="member-e8a054ec10367b65"></a>

### TooManyJsonObjectMembers

The PostgreSQL <code>too_many_json_object_members</code> SQLSTATE (<code>2203E</code>).

```csharp
public const string TooManyJsonObjectMembers = "2203E"
```

PostgreSQL macro <code>ERRCODE_TOO_MANY_JSON_OBJECT_MEMBERS</code>.

<a id="member-3d66393346877d88"></a>

### TooManyRows

The PostgreSQL <code>too_many_rows</code> SQLSTATE (<code>P0003</code>).

```csharp
public const string TooManyRows = "P0003"
```

PostgreSQL macro <code>ERRCODE_TOO_MANY_ROWS</code>.

<a id="member-354ef60f8f181bb2"></a>

### TransactionIntegrityConstraintViolation

The PostgreSQL <code>transaction_integrity_constraint_violation</code> SQLSTATE (<code>40002</code>).

```csharp
public const string TransactionIntegrityConstraintViolation = "40002"
```

PostgreSQL macro <code>ERRCODE_T_R_INTEGRITY_CONSTRAINT_VIOLATION</code>.

<a id="member-948be4129e1f7426"></a>

### TransactionResolutionUnknown

The PostgreSQL <code>transaction_resolution_unknown</code> SQLSTATE (<code>08007</code>).

```csharp
public const string TransactionResolutionUnknown = "08007"
```

PostgreSQL macro <code>ERRCODE_TRANSACTION_RESOLUTION_UNKNOWN</code>.

<a id="member-e13f8870f4d70cef"></a>

### TransactionRollback

The PostgreSQL <code>transaction_rollback</code> SQLSTATE (<code>40000</code>).

```csharp
public const string TransactionRollback = "40000"
```

PostgreSQL macro <code>ERRCODE_TRANSACTION_ROLLBACK</code>.

<a id="member-5b0ded145f270677"></a>

### TransactionTimeout

The PostgreSQL <code>transaction_timeout</code> SQLSTATE (<code>25P04</code>).

```csharp
public const string TransactionTimeout = "25P04"
```

PostgreSQL macro <code>ERRCODE_TRANSACTION_TIMEOUT</code>. Present in the PostgreSQL 17, 18, 19 beta source catalogs.

<a id="member-84b9f86c79253a5f"></a>

### TriggeredActionException

The PostgreSQL <code>triggered_action_exception</code> SQLSTATE (<code>09000</code>).

```csharp
public const string TriggeredActionException = "09000"
```

PostgreSQL macro <code>ERRCODE_TRIGGERED_ACTION_EXCEPTION</code>.

<a id="member-ac1e826372e17c9c"></a>

### TriggeredDataChangeViolation

The PostgreSQL <code>triggered_data_change_violation</code> SQLSTATE (<code>27000</code>).

```csharp
public const string TriggeredDataChangeViolation = "27000"
```

PostgreSQL macro <code>ERRCODE_TRIGGERED_DATA_CHANGE_VIOLATION</code>.

<a id="member-2430025a449c4a9b"></a>

### TrimError

The PostgreSQL <code>trim_error</code> SQLSTATE (<code>22027</code>).

```csharp
public const string TrimError = "22027"
```

PostgreSQL macro <code>ERRCODE_TRIM_ERROR</code>.

<a id="member-dc8a9dcda8184ccf"></a>

### UndefinedColumn

The PostgreSQL <code>undefined_column</code> SQLSTATE (<code>42703</code>).

```csharp
public const string UndefinedColumn = "42703"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_COLUMN</code>.

<a id="member-4dad75893195947f"></a>

### UndefinedCursor

The PostgreSQL <code>undefined_cursor</code> SQLSTATE (<code>34000</code>).

```csharp
public const string UndefinedCursor = "34000"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_CURSOR</code>.

<a id="member-06b5bfea51ac2f62"></a>

### UndefinedDatabase

The PostgreSQL <code>undefined_database</code> SQLSTATE (<code>3D000</code>).

```csharp
public const string UndefinedDatabase = "3D000"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_DATABASE</code>.

<a id="member-0ea3a7b9be48a63a"></a>

### UndefinedFile

The PostgreSQL <code>undefined_file</code> SQLSTATE (<code>58P01</code>).

```csharp
public const string UndefinedFile = "58P01"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_FILE</code>.

<a id="member-7efd4dc91692bb45"></a>

### UndefinedFunction

The PostgreSQL <code>undefined_function</code> SQLSTATE (<code>42883</code>).

```csharp
public const string UndefinedFunction = "42883"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_FUNCTION</code>.

<a id="member-4fee60a194392211"></a>

### UndefinedObject

The PostgreSQL <code>undefined_object</code> SQLSTATE (<code>42704</code>).

```csharp
public const string UndefinedObject = "42704"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_OBJECT</code>.

<a id="member-a98032b8abc33e58"></a>

### UndefinedParameter

The PostgreSQL <code>undefined_parameter</code> SQLSTATE (<code>42P02</code>).

```csharp
public const string UndefinedParameter = "42P02"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_PARAMETER</code>.

<a id="member-6da3a535a4b0655f"></a>

### UndefinedPreparedStatement

The PostgreSQL <code>undefined_pstatement</code> SQLSTATE (<code>26000</code>).

```csharp
public const string UndefinedPreparedStatement = "26000"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_PSTATEMENT</code>.

<a id="member-9a8e8fb51ff18b9a"></a>

### UndefinedSchema

The PostgreSQL <code>undefined_schema</code> SQLSTATE (<code>3F000</code>).

```csharp
public const string UndefinedSchema = "3F000"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_SCHEMA</code>.

<a id="member-70f93a5a7a03a956"></a>

### UndefinedTable

The PostgreSQL <code>undefined_table</code> SQLSTATE (<code>42P01</code>).

```csharp
public const string UndefinedTable = "42P01"
```

PostgreSQL macro <code>ERRCODE_UNDEFINED_TABLE</code>.

<a id="member-be7a5ce6a83a9ee7"></a>

### UniqueViolation

The PostgreSQL <code>unique_violation</code> SQLSTATE (<code>23505</code>).

```csharp
public const string UniqueViolation = "23505"
```

PostgreSQL macro <code>ERRCODE_UNIQUE_VIOLATION</code>.

<a id="member-ca7ba2303d5564db"></a>

### UnsafeNewEnumValueUsage

The PostgreSQL <code>unsafe_new_enum_value_usage</code> SQLSTATE (<code>55P04</code>).

```csharp
public const string UnsafeNewEnumValueUsage = "55P04"
```

PostgreSQL macro <code>ERRCODE_UNSAFE_NEW_ENUM_VALUE_USAGE</code>.

<a id="member-da3b47ee71f1686f"></a>

### UnterminatedCString

The PostgreSQL <code>unterminated_c_string</code> SQLSTATE (<code>22024</code>).

```csharp
public const string UnterminatedCString = "22024"
```

PostgreSQL macro <code>ERRCODE_UNTERMINATED_C_STRING</code>.

<a id="member-1bec35ff429d5f41"></a>

### UntranslatableCharacter

The PostgreSQL <code>untranslatable_character</code> SQLSTATE (<code>22P05</code>).

```csharp
public const string UntranslatableCharacter = "22P05"
```

PostgreSQL macro <code>ERRCODE_UNTRANSLATABLE_CHARACTER</code>.

<a id="member-b5454581af37391f"></a>

### Warning

The PostgreSQL <code>warning</code> SQLSTATE (<code>01000</code>).

```csharp
public const string Warning = "01000"
```

PostgreSQL macro <code>ERRCODE_WARNING</code>.

<a id="member-e6c85776a74ed7f0"></a>

### WarningDeprecatedFeature

The PostgreSQL <code>deprecated_feature</code> SQLSTATE (<code>01P01</code>).

```csharp
public const string WarningDeprecatedFeature = "01P01"
```

PostgreSQL macro <code>ERRCODE_WARNING_DEPRECATED_FEATURE</code>.

<a id="member-61bb5a65c02bb2b9"></a>

### WarningDynamicResultSetsReturned

The PostgreSQL <code>dynamic_result_sets_returned</code> SQLSTATE (<code>0100C</code>).

```csharp
public const string WarningDynamicResultSetsReturned = "0100C"
```

PostgreSQL macro <code>ERRCODE_WARNING_DYNAMIC_RESULT_SETS_RETURNED</code>.

<a id="member-299b90b73f494c5b"></a>

### WarningImplicitZeroBitPadding

The PostgreSQL <code>implicit_zero_bit_padding</code> SQLSTATE (<code>01008</code>).

```csharp
public const string WarningImplicitZeroBitPadding = "01008"
```

PostgreSQL macro <code>ERRCODE_WARNING_IMPLICIT_ZERO_BIT_PADDING</code>.

<a id="member-b49b0eb2b4206a0e"></a>

### WarningNullValueEliminatedInSetFunction

The PostgreSQL <code>null_value_eliminated_in_set_function</code> SQLSTATE (<code>01003</code>).

```csharp
public const string WarningNullValueEliminatedInSetFunction = "01003"
```

PostgreSQL macro <code>ERRCODE_WARNING_NULL_VALUE_ELIMINATED_IN_SET_FUNCTION</code>.

<a id="member-0848536cf46d722e"></a>

### WarningPrivilegeNotGranted

The PostgreSQL <code>privilege_not_granted</code> SQLSTATE (<code>01007</code>).

```csharp
public const string WarningPrivilegeNotGranted = "01007"
```

PostgreSQL macro <code>ERRCODE_WARNING_PRIVILEGE_NOT_GRANTED</code>.

<a id="member-1d21ce99d7aec77e"></a>

### WarningPrivilegeNotRevoked

The PostgreSQL <code>privilege_not_revoked</code> SQLSTATE (<code>01006</code>).

```csharp
public const string WarningPrivilegeNotRevoked = "01006"
```

PostgreSQL macro <code>ERRCODE_WARNING_PRIVILEGE_NOT_REVOKED</code>.

<a id="member-ec0b6c5f609de568"></a>

### WarningStringDataRightTruncation

The PostgreSQL <code>string_data_right_truncation</code> SQLSTATE (<code>01004</code>).

```csharp
public const string WarningStringDataRightTruncation = "01004"
```

PostgreSQL macro <code>ERRCODE_WARNING_STRING_DATA_RIGHT_TRUNCATION</code>.

<a id="member-ece69c7493fabcb9"></a>

### WindowingError

The PostgreSQL <code>windowing_error</code> SQLSTATE (<code>42P20</code>).

```csharp
public const string WindowingError = "42P20"
```

PostgreSQL macro <code>ERRCODE_WINDOWING_ERROR</code>.

<a id="member-794d25478af893ac"></a>

### WithCheckOptionViolation

The PostgreSQL <code>with_check_option_violation</code> SQLSTATE (<code>44000</code>).

```csharp
public const string WithCheckOptionViolation = "44000"
```

PostgreSQL macro <code>ERRCODE_WITH_CHECK_OPTION_VIOLATION</code>.

<a id="member-4f077bc569200a65"></a>

### WrongObjectType

The PostgreSQL <code>wrong_object_type</code> SQLSTATE (<code>42809</code>).

```csharp
public const string WrongObjectType = "42809"
```

PostgreSQL macro <code>ERRCODE_WRONG_OBJECT_TYPE</code>.

<a id="member-fbc51c27a31ca00b"></a>

### ZeroLengthCharacterString

The PostgreSQL <code>zero_length_character_string</code> SQLSTATE (<code>2200F</code>).

```csharp
public const string ZeroLengthCharacterString = "2200F"
```

PostgreSQL macro <code>ERRCODE_ZERO_LENGTH_CHARACTER_STRING</code>.
