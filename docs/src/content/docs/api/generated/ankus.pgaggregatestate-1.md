---
title: 'PgAggregateState<T>'
description: 'API reference for Ankus.PgAggregateState<T>.'
slug: api/ankus.pgaggregatestate-1
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns a nonnull managed aggregate payload until its native memory context resets.
New unattached wrappers support ordinary managed use; attached wrappers belong to their backend thread.

```csharp
public sealed class PgAggregateState<T> where T : notnull
```

Type parameters:

`T`

The managed payload type, optionally implementing IDisposable for exactly-once cleanup.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Constructors

<a id="member-68f768b3425d6531"></a>

### PgAggregateState(T)

Takes ownership of a nonnull payload without attaching it to PostgreSQL until returned by an aggregate callback.

```csharp
public PgAggregateState(T value)
```

Parameters:

`value` — <code>T</code>

The payload owned by this wrapper.


## Properties

<a id="member-944223a6c423be9c"></a>

### Value

Gets the payload while its state remains live, requiring the owning thread after native attachment.
The payload is invalidated before disposal when the native owner resets, including error cleanup.

```csharp
public T Value { get; }
```

Value: <code>T</code>
