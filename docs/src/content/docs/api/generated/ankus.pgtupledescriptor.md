---
title: 'PgTupleDescriptor'
description: 'API reference for Ankus.PgTupleDescriptor.'
slug: api/ankus.pgtupledescriptor
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns immutable PostgreSQL tuple metadata independently of native memory contexts and catalog cache entries.

```csharp
public sealed class PgTupleDescriptor
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Properties

<a id="member-70296f95d296eb83"></a>

### Attributes

Gets the immutable physical attribute list, including dropped slots.

```csharp
public IReadOnlyList<PgTupleAttributeInfo> Attributes { get; }
```

Value: [IReadOnlyList&lt;PgTupleAttributeInfo&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1)

<a id="member-ccfdc2d67aa99630"></a>

### BaseTypeOid

Gets the underlying composite identity when the declared type is a domain, otherwise the declared type OID.

```csharp
public uint BaseTypeOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

<a id="member-a778cddf9bd97033"></a>

### TypeModifier

Gets the anonymous record's registered type modifier, or minus one for a named composite.

```csharp
public int TypeModifier { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-000595d4ad6f4211"></a>

### TypeOid

Gets the composite or explicitly loaded domain OID, or PostgreSQL's record OID for an anonymous tuple.
Tuples read from PostgreSQL datums carry the physical base composite identity.

```csharp
public uint TypeOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)


## Methods

<a id="member-61fc276c0cbd1e49"></a>

### CreateArray(ReadOnlySpan&lt;PgHeapTuple?&gt;)

Creates a vector with this descriptor's explicit type identity, including empty and all-null arrays.

```csharp
public PgArray<PgHeapTuple?> CreateArray(ReadOnlySpan<PgHeapTuple?> values)
```

Parameters:

`values` — [ReadOnlySpan&lt;PgHeapTuple&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The tuple values, copied into the array; nested tuples remain shared references.

Returns: <code>PgArray&lt;PgHeapTuple&gt;</code>

A detached array with lower bound one, or rank zero when empty.

<a id="member-97b3634009417099"></a>

### CreateArray(ReadOnlySpan&lt;PgHeapTuple?&gt;, ReadOnlySpan&lt;int&gt;, ReadOnlySpan&lt;int&gt;)

Copies tuple elements and PostgreSQL shape while retaining an explicit element type for every array state.
Nonnull elements must share this descriptor's underlying composite identity.

```csharp
public PgArray<PgHeapTuple?> CreateArray(ReadOnlySpan<PgHeapTuple?> values, ReadOnlySpan<int> lengths, ReadOnlySpan<int> lowerBounds = default)
```

Parameters:

`values` — [ReadOnlySpan&lt;PgHeapTuple&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The row-major tuple elements, including SQL NULLs.

`lengths` — [ReadOnlySpan&lt;int&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The dimension lengths.

`lowerBounds` — [ReadOnlySpan&lt;int&gt;](https://learn.microsoft.com/dotnet/api/system.readonlyspan-1)

The lower bounds, or an empty span to use one.

Returns: <code>PgArray&lt;PgHeapTuple&gt;</code>

An array whose element OID is this descriptor's type OID.

<a id="member-9891e161b55843c6"></a>

### CreateTuple()

Creates a nonnull tuple whose physical fields are all SQL NULL. It may be populated before native validation.

```csharp
public PgHeapTuple CreateTuple()
```

Returns: [PgHeapTuple](/api/ankus.pgheaptuple/)

A detached mutable tuple.

<a id="member-6582e58202f441a4"></a>

### GetOrdinal(string)

Resolves the first live attribute with the exact case-sensitive name.

```csharp
public int GetOrdinal(string name)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The exact attribute name.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based physical ordinal.

<a id="member-fb77b766f754728d"></a>

### Load(string)

Loads a current composite descriptor using PostgreSQL's identifier and search-path rules.

```csharp
public static PgTupleDescriptor Load(string name)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The optionally schema-qualified PostgreSQL type name.

Returns: [PgTupleDescriptor](/api/ankus.pgtupledescriptor/)

An owned descriptor.

<a id="member-c18a46c76415b16e"></a>

### Load(uint)

Loads a current named composite descriptor by its catalog identity.

```csharp
public static PgTupleDescriptor Load(uint typeOid)
```

Parameters:

`typeOid` — [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The PostgreSQL composite type OID.

Returns: [PgTupleDescriptor](/api/ankus.pgtupledescriptor/)

An owned descriptor.
