---
title: 'PgHeapTuple'
description: 'API reference for Ankus.PgHeapTuple.'
slug: api/ankus.pgheaptuple
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Owns the cells of a PostgreSQL composite or anonymous record independently of backend memory contexts.
Ordinals include dropped physical slots. Nested tuples, arrays, and binary buffers are ordinary shared references.

```csharp
public sealed class PgHeapTuple
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Methods

<a id="member-439cd853f5417292"></a>

### Clone()

Copies the cell slots while sharing immutable metadata and nested reference values.
Replacing a cell in either tuple leaves the other tuple's slots unchanged.

```csharp
public PgHeapTuple Clone()
```

Returns: [PgHeapTuple](/api/ankus.pgheaptuple/)

A shallow detached copy.

<a id="member-5a3fbbbd72856501"></a>

### Create(params (string Name, SpiParameter Value)[])

Creates and registers an anonymous PostgreSQL record from named typed fields in the active backend.
New field names must contain at most sixty-three UTF-8 bytes and cannot contain a zero character.
Relation fields copy their OIDs without retaining the supplied reference's close obligation.

```csharp
public static PgHeapTuple Create(params (string Name, SpiParameter Value)[] fields)
```

Parameters:

`fields` — [(string Name, SpiParameter Value)[]](https://learn.microsoft.com/dotnet/api/system.string)

The field names and typed values in physical order.

Returns: [PgHeapTuple](/api/ankus.pgheaptuple/)

An owned record with a canonical registered type modifier.

<a id="member-22d26ffee02af29d"></a>

### Get&lt;T&gt;(int)

Reads a cell with the same strict type and SQL NULL rules as an SPI row.
Reading PgRelation or its arrays opens fresh references that the caller must dispose.

```csharp
public T Get<T>(int ordinal)
```

Type parameters:

`T`

The requested managed type.

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based physical ordinal.

Returns: <code>T</code>

The converted cell.

<a id="member-a11a262f8ff04f22"></a>

### Get&lt;T&gt;(string)

Reads the first live attribute with the exact name using strict managed conversion.

```csharp
public T Get<T>(string name)
```

Type parameters:

`T`

The requested managed type.

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The exact attribute name.

Returns: <code>T</code>

The converted cell.

<a id="member-39efcad39dc1c243"></a>

### Set(int, SpiParameter)

Replaces a cell using an explicit parameter identity, including a typed NULL composite or array.
Raw datums are copied into independent managed values before assignment.

```csharp
public void Set(int ordinal, SpiParameter value)
```

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based physical ordinal.

`value` — [SpiParameter](/api/ankus.spiparameter/)

The typed replacement.

<a id="member-0fa2a6175d4968ff"></a>

### Set(string, SpiParameter)

Replaces a named cell using an explicit parameter identity.

```csharp
public void Set(string name, SpiParameter value)
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The exact attribute name.

`value` — [SpiParameter](/api/ankus.spiparameter/)

The typed replacement.

<a id="member-fdded883b2105106"></a>

### Set&lt;T&gt;(int, T)

Replaces a cell while preserving declared type, domain, collation, and type-modifier metadata.
Native output applies current catalog and domain constraints before exposing the tuple to PostgreSQL.
Raw datums are copied into independent managed values before assignment; relations retain only their OIDs.

```csharp
public void Set<T>(int ordinal, T value)
```

Type parameters:

`T`

The replacement's declared managed type, including its type when null.

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based physical ordinal.

`value` — <code>T</code>

The replacement cell.

<a id="member-480108d4667463dc"></a>

### Set&lt;T&gt;(string, T)

Replaces the first live attribute with an exact matching name.

```csharp
public void Set<T>(string name, T value)
```

Type parameters:

`T`

The replacement's managed type.

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The exact attribute name.

`value` — <code>T</code>

The replacement value.


## Properties

<a id="member-a2996a201ce45caa"></a>

### Count

Gets the number of physical cells, including dropped attributes.

```csharp
public int Count { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-48461aefd3b84f4e"></a>

### Descriptor

Gets the immutable physical tuple description, including its type identity.

```csharp
public PgTupleDescriptor Descriptor { get; }
```

Value: [PgTupleDescriptor](/api/ankus.pgtupledescriptor/)

<a id="member-ef3712e0b82aa28f"></a>

### this[int]

Gets an owned cell by zero-based physical ordinal. SQL NULL and dropped attributes return null; unavailable fields throw.

```csharp
public object? this[int ordinal] { get; }
```

Parameters:

`ordinal` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The physical ordinal.

Value: [object](https://learn.microsoft.com/dotnet/api/system.object)

The managed cell.

<a id="member-d69b55a3aed26f7a"></a>

### this[string]

Gets the first live cell with the exact case-sensitive name.

```csharp
public object? this[string name] { get; }
```

Parameters:

`name` — [string](https://learn.microsoft.com/dotnet/api/system.string)

The exact attribute name.

Value: [object](https://learn.microsoft.com/dotnet/api/system.object)

The managed cell.
