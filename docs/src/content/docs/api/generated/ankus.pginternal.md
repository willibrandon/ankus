---
title: 'PgInternal'
description: 'API reference for Ankus.PgInternal.'
slug: api/ankus.pginternal
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Carries PostgreSQL internal state as an owned managed value or a borrowed native pointer.

```csharp
public sealed class PgInternal
```

SQL NULL is represented by a null wrapper. A present value can contain a zero native word.
Managed state is rooted until its PostgreSQL owner resets or is deleted. Native pointers
retain their original ownership; passing one does not copy or extend its pointee's lifetime.

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

## Constructors

<a id="member-9e8a79106bb0e899"></a>

### PgInternal(PgDatum)

Wraps a present internal datum, recovering managed ownership when it belongs to this extension.

```csharp
public PgInternal(PgDatum datum)
```

Parameters:

`datum` — [PgDatum](/api/ankus.pgdatum/)

A live, non-null PostgreSQL internal value.


## Methods

<a id="member-94a6700455f3e2ba"></a>

### Create&lt;T&gt;(T, PgMemoryContext?)

Roots a managed value under a PostgreSQL owner and arranges its eventual cleanup.

```csharp
public static PgInternal Create<T>(T value, PgMemoryContext? context = null) where T : notnull
```

Type parameters:

`T`

The exact managed value type.

Parameters:

`value` — <code>T</code>

The non-null value to retain; IDisposable is called once at owner cleanup.

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The storage owner, or the current callback's result owner when omitted.

Returns: [PgInternal](/api/ankus.pginternal/)

The present internal state.

Use mutable classes for state updated across callbacks. An explicit owner controls longer lifetimes.
The native address is an opaque identity and does not point to the managed object's representation.

<a id="member-1d2d7f6fc0994d6a"></a>

### DangerousBorrow&lt;T&gt;()

Borrows a native pointee with an explicit unmanaged representation and checked context lifetime.

```csharp
public PgNativeReference<T>? DangerousBorrow<T>() where T : unmanaged
```

Type parameters:

`T`

The caller-proven native representation.

Returns: <code>PgNativeReference&lt;T&gt;</code>

The borrowed reference, or null for a zero pointer.

The caller guarantees at least sizeof(T) initialized, accessible bytes with the correct layout.
This method rejects managed state, whose opaque identity is not a native value representation.

<a id="member-8c750e0261be7f7b"></a>

### DangerousCreate(nuint, PgMemoryContext)

Borrows a native internal word under an explicit lifetime anchor without acquiring ownership.

```csharp
public static PgInternal DangerousCreate(nuint bits, PgMemoryContext context)
```

Parameters:

`bits` — [nuint](https://learn.microsoft.com/dotnet/api/system.uintptr)

The PostgreSQL internal word, including a present zero pointer.

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The lifetime anchor, which must not outlive the pointed-to storage.

Returns: [PgInternal](/api/ankus.pginternal/)

The borrowed internal value.

The caller guarantees pointer validity and its representation. No earlier native free is detected.

<a id="member-5fedf8ab6d35034c"></a>

### DangerousGetBits()

Returns the native word after checking its owner and backend thread.

```csharp
public nuint DangerousGetBits()
```

Returns: [nuint](https://learn.microsoft.com/dotnet/api/system.uintptr)

The opaque managed identity or borrowed native word.

<a id="member-bcfa81acfaeae3c3"></a>

### Get&lt;T&gt;()

Reads managed state after checking its exact type, owner, and backend thread.

```csharp
public T Get<T>() where T : notnull
```

Type parameters:

`T`

The exact type supplied to Create.

Returns: <code>T</code>

The retained value. Value types are returned by value.


## Properties

<a id="member-3883833c001db269"></a>

### Datum

Gets the raw datum and its checked lifetime without copying the pointed-to state.

```csharp
public PgDatum Datum { get; }
```

Value: [PgDatum](/api/ankus.pgdatum/)

<a id="member-89877566abbe2793"></a>

### IsManaged

Gets whether this wrapper refers to managed state created by this extension.

```csharp
public bool IsManaged { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)
