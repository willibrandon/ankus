---
title: 'PgFunctionArgument'
description: 'API reference for Ankus.PgFunctionArgument.'
slug: api/ankus.pgfunctionargument
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Supplies a typed function argument, SQL NULL, or a request for its declared default.

```csharp
public readonly struct PgFunctionArgument
```

## Methods

<a id="member-779e576193e123f5"></a>

### Create(SpiParameter)

Supplies an existing typed parameter, including explicit composite descriptors and raw datums.

```csharp
public static PgFunctionArgument Create(SpiParameter parameter)
```

Parameters:

`parameter` — [SpiParameter](/api/ankus.spiparameter/)

The parameter to bind.

Returns: [PgFunctionArgument](/api/ankus.pgfunctionargument/)

The typed argument.

<a id="member-ab3250e2ef79d472"></a>

### Create&lt;T&gt;(T)

Supplies a value using its declared managed type. Nullable values preserve SQL NULL.

```csharp
public static PgFunctionArgument Create<T>(T value)
```

Type parameters:

`T`

The managed argument type.

Parameters:

`value` — <code>T</code>

The argument value.

Returns: [PgFunctionArgument](/api/ankus.pgfunctionargument/)

The typed argument.

<a id="member-38a6bc70b756c1eb"></a>

### Default(uint)

Requests a declared default using an exact PostgreSQL type identity.

```csharp
public static PgFunctionArgument Default(uint typeOid)
```

Parameters:

`typeOid` — [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

The nonzero catalog type OID.

Returns: [PgFunctionArgument](/api/ankus.pgfunctionargument/)

The typed default request.

<a id="member-f2c5509c80f31784"></a>

### Default&lt;T&gt;()

Requests a declared default, using the managed type to resolve overloaded functions.

```csharp
public static PgFunctionArgument Default<T>()
```

Type parameters:

`T`

The default argument's expected managed type.

Returns: [PgFunctionArgument](/api/ankus.pgfunctionargument/)

The typed default request.


## Properties

<a id="member-34f22bca903a98fb"></a>

### IsDefault

Gets whether this argument requests the function's declared default expression.

```csharp
public bool IsDefault { get; }
```

Value: [bool](https://learn.microsoft.com/dotnet/api/system.boolean)

<a id="member-fef5acdbd9ab1957"></a>

### TypeOid

Gets the PostgreSQL type used to resolve the function, including for NULL and default arguments.

```csharp
public uint TypeOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)
