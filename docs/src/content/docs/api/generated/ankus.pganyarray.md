---
title: 'PgAnyArray'
description: 'API reference for Ankus.PgAnyArray.'
slug: api/ankus.pganyarray
---
<!-- Generated by Ankus.DocGenerator. -->

Namespace: [Ankus](/api/ankus/)

Assembly: `Ankus.Runtime.dll`

Represents a PostgreSQL anyarray with its actual element type, dimensions, lower bounds, and nullable cells.

```csharp
public sealed class PgAnyArray : IReadOnlyList<PgAnyElement?>, IReadOnlyCollection<PgAnyElement?>, IEnumerable<PgAnyElement?>, IEnumerable
```

Inheritance: [object](https://learn.microsoft.com/dotnet/api/system.object)

Implements: [IReadOnlyList&lt;PgAnyElement&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlylist-1), [IReadOnlyCollection&lt;PgAnyElement&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ireadonlycollection-1), [IEnumerable&lt;PgAnyElement&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerable-1), [IEnumerable](https://learn.microsoft.com/dotnet/api/system.collections.ienumerable)

## Constructors

<a id="member-1f82b13a9911c4ba"></a>

### PgAnyArray(PgDatum)

Reads an array's shape and cells into the supplied datum's checked native owner.

```csharp
public PgAnyArray(PgDatum datum)
```

Parameters:

`datum` — [PgDatum](/api/ankus.pgdatum/)

A present PostgreSQL array value.


## Methods

<a id="member-e5bc1357eb6d147c"></a>

### CopyTo(PgMemoryContext)

Copies the complete array into an independent native owner.

```csharp
public PgAnyArray CopyTo(PgMemoryContext context)
```

Parameters:

`context` — [PgMemoryContext](/api/ankus.pgmemorycontext/)

The destination context.

Returns: [PgAnyArray](/api/ankus.pganyarray/)

The independently owned array.

<a id="member-06f52f530bd95e85"></a>

### GetEnumerator()

Enumerates nullable cells in PostgreSQL row-major order.

```csharp
public IEnumerator<PgAnyElement?> GetEnumerator()
```

Returns: [IEnumerator&lt;PgAnyElement&gt;](https://learn.microsoft.com/dotnet/api/system.collections.generic.ienumerator-1)

The checked cell iterator.

<a id="member-207090239509f553"></a>

### GetLength(int)

Gets the length of a zero-based dimension.

```csharp
public int GetLength(int dimension)
```

Parameters:

`dimension` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based dimension.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The dimension length.

<a id="member-cdb5c4e53ba531c9"></a>

### GetLowerBound(int)

Gets the PostgreSQL lower bound of a zero-based dimension.

```csharp
public int GetLowerBound(int dimension)
```

Parameters:

`dimension` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The zero-based dimension.

Returns: [int](https://learn.microsoft.com/dotnet/api/system.int32)

The actual lower bound.

<a id="member-f4f9895eaebadcb1"></a>

### Read&lt;T&gt;()

Reads a managed array with exact type checking; polymorphic wrappers retain this array's native lifetime.

```csharp
public T Read<T>()
```

Type parameters:

`T`

The requested array representation.

Returns: <code>T</code>

The managed array copy or checked polymorphic wrapper.


## Properties

<a id="member-1de47ca1ed293412"></a>

### Count

Gets the total number of cells in row-major order.

```csharp
public int Count { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-3aa88f9142940354"></a>

### Datum

Gets the array's native value and actual PostgreSQL type.

```csharp
public PgDatum Datum { get; }
```

Value: [PgDatum](/api/ankus.pgdatum/)

<a id="member-403d78c84d658bb5"></a>

### ElementTypeOid

Gets the declared array element type OID, including domains and unregistered enum types.

```csharp
public uint ElementTypeOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)

<a id="member-12234c0483192ed5"></a>

### this[int]

Gets a checked cell by its zero-based flattened ordinal, or null for SQL NULL.

```csharp
public PgAnyElement? this[int index] { get; }
```

Parameters:

`index` — [int](https://learn.microsoft.com/dotnet/api/system.int32)

The flattened ordinal.

Value: [PgAnyElement](/api/ankus.pganyelement/)

<a id="member-197dc8f09401f945"></a>

### Rank

Gets the number of dimensions; an empty PostgreSQL array has zero dimensions.

```csharp
public int Rank { get; }
```

Value: [int](https://learn.microsoft.com/dotnet/api/system.int32)

<a id="member-a9cf7b88b4382b22"></a>

### TypeOid

Gets the actual array type OID, including a domain over an array.

```csharp
public uint TypeOid { get; }
```

Value: [uint](https://learn.microsoft.com/dotnet/api/system.uint32)
